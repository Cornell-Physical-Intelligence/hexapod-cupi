"""Acceptance gates, screen parameters, and evaluation dispatch.

`gates` is the machine-readable single source of truth for every acceptance
threshold and screen parameter in this repository; `dispatch` is the one
documented entry point to the evaluation scripts under `isaaclab/`.
"""

from __future__ import annotations

__all__ = ["dispatch", "gates"]
