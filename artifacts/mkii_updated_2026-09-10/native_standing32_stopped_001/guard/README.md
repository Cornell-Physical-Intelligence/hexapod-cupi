# Canonical standing32 guarded preparation

This is one bounded 32-environment standing screen, using unchanged standing source002 (51 files), host002 (22 files), asset001 and actual coordinate/effort admission. It requires the completed same-source one-environment run native_standing_002. That run passed 1000 controls / 8000 explicit physics steps and the unchanged standing gates; its independently verified terminal audit is included verbatim. No walking, training, hardware or Stage2 admission is asserted here.

The new allocation is native_standing32_001, forecast_pause_008, owner hexapod-canonical-native-standing32-001-20260910.service. The explicit host CLI is `--num-envs 32 --standing-one /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_standing_002/standing`. Source, assets, coordinate/effort admission and standing1 input remain read-only through the unchanged host. Source009 is reused only for exact owned-container supervision, not for its historical physics.

The guard requires prior invocation d9725b51027e44b59ffcb00a2b67763f, completed campaign/native acceptance, all 8000 captured rows / 1000 controls / exactly one reset, exact name and ID absence, and exact pause007 restoration. It binds and remotely rehashes all 38 prior raw payloads, including the 169,991,706-byte contact log. Only small JSON receipts are copied into this bundle; contacts and NPZs remain external immutable inputs. The compact mock tests use the bound audit hashes for absent large fixtures, while the separate actual-input preflight rehashes the full local standing1 tree through the frozen source contract.

Parent weather ancestry, unknown non-weather protection, dual GPU locks, exact owned cleanup and embedded restoration code are unchanged. The NVIDIA argument remains literally `--query-compute-apps=pid,process_name`, with a regression preserving the earlier invalid-flag failure. Bounds remain 600 s per native phase / 90 s AppReady, 720 s outer owner / 180 s stop / 15 min fallback. CPU preparation does not establish that 32-environment runtime fits this bound; root decides actual allocation after its timing review. There is no automatic extension or continuation.

Run `python -B -m unittest discover -s <bundle> -p 'test_*.py'` for focused CPU tests. `check_bound_inputs.py <repo-root>` exercises the real frozen source/host/asset/effort/standing1 inputs with subprocesses forbidden; all original large raw must be present externally. Run `verify_bundle.py` for exact frozen byte verification. Root owns transfer, remote setup, dispatch and central project-site publication.
