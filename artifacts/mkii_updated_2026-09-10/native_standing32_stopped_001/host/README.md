Host002 is a source-pin-only runtime successor. Original host001 and failed standing001 remain immutable; the exact-zero contact interpretation changes no host behavior.

# Canonical provisional standing ownership host

This prepares one explicit native standing allocation using the canonical detailed direct-drive model. A later 32-environment allocation is a separate root dispatch and requires a completed, same-source 1-environment standing result. This host never chains allocations or starts a policy. The source is frozen producer002, 51 payloads, `acb589708fcba8be2f6ef64171795889eca8b6583fdbb4bc2fc25d61717b0467`. Any pending or changed binding refuses before allocation.

The CLI is `python3 -B launch_standing_spark.py --source SOURCE --asset ASSET --admission ACTUAL_ACTUATION_DIRECTORY --num-envs 1 --supervisor-source SOURCE009 --output FRESH_OUTPUT`. Add `--preflight-only` for the standard-library input check. For the later explicit 32 allocation, use `--num-envs 32 --standing-one SAME_SOURCE_STANDING_DIRECTORY`; standing1 rejects that argument. Paths are resolved before overlap checks. Both input directions and dangling/reused output paths reject.

The source contract checks the exact completed actuation002 source result, canonical asset inventory and same-source standing1 where required. The host additionally requires canonical schema `canonical_native_standing_v1`, runtime scope `canonical_provisional_native_standing_only`, 1,000 controls, 8,000 steps at 2.5 ms, eight steps per 20 ms held control, gravity -9.81 m/s² and a real floor. The authoritative corrected-mass asset selector and actual actuation state are pinned. Gains and provisional 48 V effort semantics remain source-owned assumptions, with no hardware calibration or policy admission implied.

Source, asset, actuation admission and optional standing1 evidence mount read-only. Only the new output is writable. The container launches the actual `run_standing.py` with the explicit number of environments and matching admission arguments. It loads no checkpoint. Old source009 supplies unchanged `run_owned` and `owned_container` functions only; its old C-study model and control environment are not loaded. Command, native-source verifier, runtime identity and truthful job metadata are the only callbacks. The original 600-second phase and 90-second actual AppReady bounds remain, with no automatic extension. The one-environment timing will inform whether a later 32 allocation fits that bound.

After process exit, the host rechecks original inputs, calls the source-owned standing validator and seals every output, including shutdown logs. Failed quiet/contact/torque results remain failed even when raw acquisition completed. Terminal input or output changes also force failure. Successful standing acquisition never sets broad physical admission, training permission or Stage2 completion.

The final host has 15 passing focused CPU tests and a passing standard-library setup test that compares actual original ownership CodeTypes without external calls or simulator imports. The setup imports the actual frozen standing contract and compiles its entrypoint. Root executes the separate full host input preflight after freeze. All changes remain temporary preparation; root owns actual Spark setup, allocation and central project publication under `docs/PROJECT_SITE.md`.
