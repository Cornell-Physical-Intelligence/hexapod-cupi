# Detailed robot: one-robot standing with 32/0 passes

Native6 completed **1,000 controls / 8,000 physics steps** and passed every original physical and quiet-standing gate. In the 16-second quiet window, maximum joint SDK velocity RMS was **0.002949383 rad/s** against the unchanged **0.03 rad/s** bound. There were zero missing six-toe support substeps, nonfoot contacts, requested torque saturation, terminations or truncations. Peak applied effort over all controlled substeps was **1.12073946 Nm**. Exact values remain in `RESULT.json` and the original report.

This establishes the bounded one-robot standing prerequisite on source005. It does not establish a 32-replica pass, learned walking, PPO admission, Stage 2 completion or hardware qualification. The next screen must use this exact source and one-robot admission.

Source005 checked unauthored articulation 32 position / 1 velocity iterations, authored **32/0**, then read all values back after reset and controlled steps. Those are USD attribute readbacks, not independent solver-backend introspection. All 18 deprecated legacy friction coefficients were observed as zero; new friction properties are recorded separately. Link-COM velocities and the floor-filtered force matrix are preserved as additional diagnostics. They do not replace the original SDK joint rates, detailed patch classification or any acceptance gate. Geometry, 153 SDF colliders, servo, effort envelope, reset, control timing and thresholds were unchanged. This result does not by itself identify the cause of earlier rejection.

All **40 raw files / 224,058,842 original bytes** are preserved. The contact JSONL has lossless gzip transport encoding; original size/SHA maps retain the exact bytes. Source109, host63, guard70 and auditor14 include their unchanged freezes and complete payloads. Eleven root setup/transfer/dispatch/progress receipts are separately bound. The actual invocation is `64a373e301c24b70aa3790c18aca147c`; native wall time was 118.9413 s, and native, owned phase and host completed successfully.

The independent terminal audit verifies exact source/asset/effort/supervisor inputs, owned container name and ID absence, pause014 per-job cleanup and continued persistent Spark reservation. This is a historical audit, not a live status report. Original actuation admission19, asset9 and ownership-only supervisor926 remain separately published dependencies with full identities in the audit.

Run `python3 -B -S verify_bundle.py`. It hashes all publication and nested frozen payloads, reconstructs all raw bytes into a temporary directory (about 224 MB), invokes the unchanged source005 positive validator, compares its exact accepted receipt and original solver/restoration checks, then removes only its temporary reconstruction. No native simulator, GPU or remote action is used. `REPLAY_RECEIPT.json` records the successful publication-time replay. Root owns central project updates and future allocation under docs/PROJECT_SITE.md.
