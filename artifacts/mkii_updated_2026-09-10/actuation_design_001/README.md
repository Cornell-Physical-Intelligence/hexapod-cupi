# Canonical actuation design: reviewed simulation proposal

Root adopts the corrected proposal002 as preparation for bounded native coordinate/effort tests, followed by a separately validated provisional standing servo and fresh PPO integration. This is an engineering design decision, not a native actuation or policy pass. The selected detailed19-body/18-joint model remains7.466088235kg; no old task, actor, checkpoint or four-bar adapter is reused.

The exact neutral geometry has nearly coplanar toe minima and a first-touch plate height of76.611091mm. An ideal vertical-force static solve requires at most0.581644Nm at a joint; this assumes controllable point-support load distribution and does not establish native standing. The proposed first reset adds5mm clearance. Full inertia, force/moment balance and virtual-work checks are retained.

The first native experiment uses zero gravity, no floor and no position drives. It checks each named coordinate and applies small signed torques while recording native actuation input, joint reaction and actual angles/rates separately at400Hz. The corrected pulse is0.005Nm for20ms followed by20ms coast, with unchanged0.05rad/2rad/s diagnostic bounds. A full floating-base mass-matrix prediction gives0.008968rad and0.286976rad/s. These are planning estimates, not measured motion.

Root rejected the earlier0.02Nm/20ms pulse plus100ms coast before execution: its ideal predicted excursion0.127704rad exceeded its own0.05rad bound. Both the original27-payload draft and corrected32-payload proposal remain byte-for-byte intact. No gate was relaxed after a native failure.

After successful effort/coordinate validation, the candidate servo is explicit400Hz PD with Kp12 and name-bound inertia-derived damping, a50Hz command hold, a provisional1.6Nm software ceiling and the declared48V speed curve. Motor bus voltage has been asked of the user and remains a hardware input to confirm. The numerical gains, symmetric braking and absent extra latency/friction are uncalibrated assumptions.1.6Nm is not guaranteed continuous-stall capability; raw demand, clipping and time above1.2Nm must remain visible. Actual native external force input is distinct from the implicit-drive maximum-force field.

Use a separately versioned external-forces-every-iteration setting and measure SDK velocity against angle increments before damping feedback. Then validate real distal+X toe contacts, one cold neutral hold and32 replicas before a new32×24×2 PPO smoke with fresh weights/optimizer and strict reload. Hardware identification gates physical transfer; declared provisional simulation work can proceed under its own matched native admission. Stage2 smoothness/quiet gates remain unchanged.

Eleven corrected CPU tests passed independently at root. The actual Fable5.1 maximum-effort final response, exact inputs and root/producer corrections are retained; model advice was not adopted automatically. Run `python3 -B -S verify_bundle.py` here to verify all published bytes. Frozen CPU tests expect their directory two levels below the repository; reproduce by copying the accepted proposal into a fresh directory under repo/tmp and running its test discovery with the repository virtual environment. Never overwrite a prior proposal or its reports.
