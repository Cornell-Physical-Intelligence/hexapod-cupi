# Proposed central framework integration

Root owns tracked integration and dispatch. This ignored preparation makes no tracked changes.

Suggested future immutable path: `artifacts/mkii_updated_2026-09-10/native_inspection_preparation_001/`.
- `STATUS.md`: canonical native import/cooking inspection prepared; actual execution pending. Physical admission false; all old C-model data remain historical.
- `docs/PLAN.md`: Phase A inspects exact canonical identity/native SDF representation before separately admitted actuator, suspended-motion, gravity/contact and learning work.
- `docs/NEXT_RUNS.md`: one root-owned, single-articulation, zero-gravity, eight-explicit-step native inspection, fixed geometry and no drives; 90s AppReady/600s total with exact ownership/cleanup. Preserve failure rather than auto-changing SDF.
- `site/project.json`: canonical geometry node points to selected asset; evidence node labels this preparation as CPU-tested/native-pending, not walking progress or physical admission.
- Append `site/updates/<UTC>_canonical_native_inspection_preparation.json` covering the bounded artifact directory and those edited documents/registry. Update actual result state only after a separate raw terminal audit.
- Run `python3 tools/project_site.py check --base <integration-base>` and `python3 tools/project_site.py build`; if integrated runtime paths are covered, use the versioned lineage procedure in `docs/PIPELINE_LINEAGES.md` and `tools/check_pipeline_lineages.py current`.
