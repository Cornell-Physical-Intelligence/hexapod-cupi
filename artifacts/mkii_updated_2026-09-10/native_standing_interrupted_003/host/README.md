# Canonical standing host003

Pin-only runtime successor of frozen host002. The sole launcher change binds optimized source003; original source002/host002/standing acceptance and operational stopped32 evidence remain immutable. Source003 requires fresh1 then32 admission even though its classifier passed exact CPU parity. Entry, CLI, all mounts, native result/identity validation,600s/90s limits and source009 owned-container functions are unchanged. This host does not select or chain allocations.

CLI: `python3 -B launch_standing_spark.py --source SOURCE003 --asset ASSET001 --admission ACTUAL_ACTUATION_DIRECTORY --num-envs 1 --supervisor-source SOURCE009 --output FRESH_OUTPUT`. `--preflight-only` checks actual inputs without creating the output. A later32 allocation explicitly adds `--num-envs 32 --standing-one SAME_SOURCE_ACCEPTED_STANDING_DIRECTORY`. Read-only asset/source/effort/standing1 input protections, exact post-exit seal and failure preservation remain unchanged.

Existing setup/test receipts copied from host002 are historical and retain their original names. tests_optimized_host.log and setup_source003.json are this successor's current tests/setup. Source003 preflight uses actual unchanged asset9/effort19. Root owns remote setup, dispatch and central project-site publication. No CPU test or operational stopped32 result admits walking/training/hardware/Stage2.
