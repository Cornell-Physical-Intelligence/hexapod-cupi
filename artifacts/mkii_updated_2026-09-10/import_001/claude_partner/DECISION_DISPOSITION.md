# Claude Fable 5.1 review and engineering disposition

The architecture consultation completed successfully with the requested **`claude-fable-5-1` and `--effort max`**. `LAUNCH.json` records the exact command and prompt hash; `EXIT.json` records return code 0. The provider response in `result.json` reports `is_error: false`, subtype `success`, canonical model `claude-fable-5-1`, and session **`18cbf0e7-827e-4404-bcdb-103220315959`**. The prompt SHA-256 is `61d213fb48521f71f6cef2c730296c8f3c768ec0d001c58f24cbe82ddbfbbe32`.

The partner had tools disabled and reviewed only the supplied facts. It did not inspect CAD files or run tests. Its exact prompt, response, launcher, launch/exit records and stderr are preserved unchanged. The review **predated the user's confirmation, “yup, no more four bar, makes it way easier.”** Some recommendations therefore inherited the obsolete mechanism described in the prompt. This disposition records what the root and geometry/mass workers actually adopted; a successful consultation does not make every recommendation correct.

| Recommendation or assumption | Disposition and evidence |
| --- | --- |
| Preserve exact source geometry, mass/COM/full tensors, filename decoding and immutable lineage. | Accepted. The pinned ZIP and extraction manifest, restricted pickle audit, per-instance ledger and mesh hashes implement this. |
| Distinguish arbitrary capture pose, inspection zero and physical encoder zero; prove the captured pose by all-part forward-kinematic reconstruction. | Accepted. The builder defines an inspection stance without moving CAD bearing axes; `cad_pose.json` restores the export. Independent validation checks every source vertex and serialized URDF transform. |
| Recover axis lines from instanced bearing/holder/flange/housing geometry and corroborate ownership across differently posed leg copies. | Accepted with explicit geometric uncertainty. Current-geometry cylinder fits and full relative-pose comparisons replace historical leg transforms. Rotational symmetry still limits race/rotor ownership inference. |
| Name 12 passive coordinates, six rod closures, test a parallelogram and preserve a pushlever controller. | Rejected for this revision. Those recommendations inherited the previous four-bar. The new export lacks its external pushlever/rod structure; the user confirmed direct drive. The candidate has 19 bodies and 18 revolute joints. Old physical four-bar and C-study lineages remain separate. |
| “Housing mass goes to the parent link” as a universal rule. | Rejected. In this CAD assembly the yaw housing follows the coxa while its output side follows the chassis. Shoulder and knee housing sides differ appropriately. Missing nominal motor mass is added to each specific housing's geometrically inferred owner, not generically to the joint parent. |
| Use one convex hull per rigid body's structure by default. | Rejected as a blanket rule. A hull can bridge real clearances and change the foot/support geometry. Collision preparation is an independent, explicit workflow with its own evidence; the detailed source geometry and original overlaps are preserved. |
| Assume a spherical foot from the tibia length or historical foot model. | Not adopted. The reviewer required re-evaluating the changed foot rather than certifying a sphere. The current tibia's +X surface extreme is not a sphere center; contact geometry must use the actual new shape. No spherical-foot assumption is accepted from any prior importer. |
| Treat any high-multiplicity part as belonging to a leg or assign unmatched parts to chassis by default. | Rejected. Chassis fasteners also repeat. The recovery explicitly explains 1,674 full-pose-consistent assignments and a reviewed, bounded 79-part chassis remainder; unknown remainder fails instead of silently falling back. |
| Infer actual material names from density bands, or infer a missing assignment solely from zero mass. | Not adopted as fact. Estimated mass/mesh-volume density remains separate from material identity. The user accepted mass properties without material names. Zero-mass motor internals are recorded without inventing why their CAD mass is zero. |
| Apply proposed numerical admission thresholds or automatically create a training task. | Not adopted. The suggestions are review proposals, not new accepted gates. Existing gates/task IDs are unchanged. Finite geometry checks do not constitute Isaac or hardware admission. |
| Keep raw CAD, sourced nominal corrections and measured hardware values separate. | Accepted. Raw CAD is 5.147603654203 kg. A separate 191 g-per-RS05 nominal variant totals 7.466088235226 kg; its missing-mass housing-cylinder allocation is explicitly approximate. Measured motor distribution, payload and calibration remain unqualified. |

The partner's opening explanation of why the exporter produced one link is a plausible diagnosis, not a verified Onshape mate-configuration finding. What is directly verified is that this ZIP contains one rigid link and no joint/closure/frame graph. A future mate-aware export would strengthen ownership and axis provenance, but absence of that export did not prevent the user-authorized, explicitly inferred inspection model.

The quantitative findings and engineering decisions rest on the stored source and independent tests, not the consultation's authority. The initial unsafe knee range and transient XML serialization failure remain preserved under `joint_validation/initial_unsafe_envelope/`. Existing source screw/tibia penetrations remain visible and prevent treating raw per-part triangle collisions as a qualified dynamics model.
