# Canonical inspection ownership guard

This is preparation for one canonical detailed direct-drive native inspection. Source and host pins are bound to the frozen canonical producer and host. The pending-value rejection remains covered by an explicit negative fixture before any pause, service operation or GPU allocation. The guard has no training/checkpoint argument or automatic successor.

All new paths are under `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`: `inspection_source_001`, `asset_001`, `inspection_host_001`, output `native_inspection_001` and pause `forecast_pause_001`. The owner is `hexapod-canonical-native-inspection-001-20260910.service`; the unique fallback unit is `hexapod-canonical-forecast-restore-001`, avoiding old namespace collisions. Root is the sole dispatcher.

The previous owner is the actually completed historical C-study preview invocation `3e88eb521861497cb3bd275b60c3c1b8`. Its seven exact campaign/job/state/video/launch/pause064/restoration receipt hashes are bound and copied unchanged as cleanup evidence. Before pause and before dispatch, the guard requires completed immutable recording status, inactive exit-zero owner, the exact invocation when still exposed, the exact recording name and recorded ID absent, and restoration of exactly the previously active timers. Historical journal/audit evidence supports the collected empty invocation case. This is ownership ancestry only, never reuse of old assets, checkpoints or physical admission.

The existing weather/GPU protection logic is retained. Its embedded restorer is byte-identical to the reviewed preview guard, and its CUDA ancestry loop is AST-identical. Identified weather services can be gracefully stopped under the user's explicit weather priority authorization; unknown non-weather CUDA workloads still reject. It uses both global GPU locks and coordination hash `35af5100e39980f55a3f5e19654e4550b3c3fb6da114b64fbe0d143817242cf3`. The owner bound remains 720 seconds with a 180-second stop allowance and 15-minute fallback; the actual inspection supervisor remains 600 seconds total and AppReady90. Only this exact owner/container may be stopped by recovery, and only originally active weather timers are restored.

Nineteen focused CPU guard tests currently pass: original embedded cleanup, actual main ordering with fake effects, known/unknown container absence, exact immutable previous receipts, pending pins, standard-library import and canonical command/namespace/deadline checks. The initial draft test expected the remote path after a temporary test patch was removed; only that mock-path assertion was corrected. No runtime behavior changed for that test correction, no remote calls occurred and no native physics was run.

Root must perform the actual canonical host preflight after source/host freeze and before dispatch. The reviewed root-only launch is `python3 -B launch_guarded_remote.py` from the transferred frozen guard; it verifies all final bindings and the actual prior owner before any pause. Do not install dependencies or change native inspection behavior to satisfy this wrapper.

Publication must obey docs/PROJECT_SITE.md with an accurate bounded central update. Inspection readiness is distinct from actual SDF cooking, native readback, suspended sweeps, contact/torque admission and PPO. No existing tracked files or frozen evidence were rewritten.

Final binding: native source `c59ebd53bb221fd17b46d9fbc31e3df7240271bfd13d6f7269d7307876c7bfaa`, host freeze `2d21500ae7273ecf70576b577772c38844915a05f0fe67b8e2c213137d9994a1`, host runtime `2ab3378ca6554baf292e25a7027c102017e6084b0440d8c77b62572bb73608be`. The final 19 tests passed and `check_bound_inputs.py` passed under local Python 3.12 with `-S`, verifying the actual frozen source, host, nine asset files and full original supervisor while prohibiting every external process call. This does not replace root’s actual Spark preflight or inspect native GPU behavior.
