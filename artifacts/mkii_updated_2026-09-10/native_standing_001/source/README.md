# Canonical provisional native standing preparation

This fresh source prepares a bounded one-robot standing screen, followed only after its accepted same-source result by32 robots. It uses the approved7.466088235kg detailed direct-drive CAD, exact153 SDF colliders per robot, full inertias,18 native named joints and six measured distal contact regions. No old task, motor adapter, actor or checkpoint is loaded. Source002 coordinate/effort evidence has completed36 pulses and36 coordinate checks; this source requires those exact29 raw files. Neither that diagnostic nor this CPU preparation admits standing or training.

The standalone allocation is1000 controls,8×2.5ms each:20s total with4s settling and a16s quiet window. Native initialization uses gravity−9.81m/s² and the imported reset height. Its uncontrolled SDK warmup state is saved. Exactly one explicit generalized state/target reset then restores zero joint coordinates, zero velocities and plate height0.08161109101311194m; the controlled trial has no further resets, root writes, position prescriptions or implicit drives. This reset does not claim to clear native solver warmstart caches.

## Actuation and observations

Every400Hz step reads actual native q and raw SDK qdot, computes `12*(target-q)-Kd*qdot`, and clamps to the accepted design002 provisional48V speed envelope and1.6Nm numerical software cap. Kd is the named full-inertia-derived candidate in servo_candidate.json (coxa≈.44198, femur≈.24567, tibia≈.10552). The held50Hz target is always exactly neutral for this screen. Native pre-step external input must equal the float32 software command exactly. Raw requested effort, software-applied effort, native input pre/post, projected reaction, q, SDK qdot, interval-angle rate, root/link poses and floor-contact patches are retained. Native input is a simulator input buffer, not measured motor output torque.

Battery48V, symmetric braking, friction1.0, restitution0, torque-speed interpolation and PD gains are explicitly provisional simulation choices. They are not measured motor/battery properties or guarantees of sustained1.6Nm stall. Extra rotor armature, motor friction, electrical/thermal behavior and delay are unmodeled; no5.5Nm burst is enabled. The numerical cap remains unchanged. Native material/offset readback is required. External forces every iteration is explicitly true, as in the passed effort source; self-collision, adjacent filters, collider geometry/resolution and joint limits are retained.

SDK velocity and angle-difference rate remain separate channels, with interval validity and dt. The quiet scorer retains raw SDK velocity. The initial packet has invalid contact/history until actual stepping occurs; every subsequent raw contact buffer is read immediately after the same explicit native step. No fabricated sensor timestamp or zero-age claim is used.

## Geometry and gates

The root metric is the bottom-plate frame, with the newly declared minimum0.055m; this is not a claim of equivalence to archived model frames. The neutral mesh touch height is0.07661109101311193m. The flat floor is an80m static triangle mesh at worldZ0 with explicit material. Robot copies occupy a2m grid; actual root paths and all19×N body mappings are checked.

For each native tibia-floor patch, the world point is transformed through measured body pose and the exact authored shape-to-link matrix into source mesh_056 coordinates. Distal membership is X≥0.115m, upperX≤actual0.129999995m+authored1mm contact offset, and Y/Z within actual cap vertex bounds±authored contact offset. A1e−12m computational epsilon only protects the X threshold against inverse-transform roundoff. This is an authored diagnostic foot-region classification, not hardware measurement or an analytic sphere approximation. Body/coxa/femur/shaft patches remain separate; all raw patches are saved before aggregation. Force norm>1N defines support. Buffer capacity is explicit and exhaustion or malformed patch indexing fails closed.

Minimum original-mesh plane clearance is computed using convex-hull **vertices as an exact extrema accelerator**, never as replacement collision geometry. Non-toe tibia surface includes all481 original triangle-edge intersections with X=.115, so clipping cannot omit the lower boundary. All other collider geometry remains in the non-toe cloud. Both all-mesh and non-toe minima are saved.

Unchanged quiet bounds: planar excursion≤10mm; heading≤2deg; raw SDK joint-rate RMS≤.03rad/s; joint range≤.02rad; target-step p95≤.002rad/20ms; requested saturation fraction≤.005; applied≤1.60001Nm; no terminations/truncations and≥10s quiet. Six-toe support is required after settling. Requested torque saturation is checked at every400Hz substep, with per-joint and mean limits. The stricter canonical rule requested by root rejects **any controlled-substep** nonfoot contact>1N or non-toe mesh penetration beyond−1mm, including startup. This is explicitly stricter than the archived post-settle nonfoot screen. Actual q must remain inside native limits with2e−6rad readback tolerance; SDK qdot must remain within native maximum velocity+2e−6rad/s. Physical failure saves the offending sample and aborts without reset.

`standing_score.py` replays sequence/counters, servo recurrence, command-to-native equality, all control endpoints and raw interval-angle channels before scoring. Acquisition completion and physical/quiet pass are distinct. The stdlib host validator rejects a completed acquisition with any failed replica, contradictory numeric metric, missing raw input, changed evidence or late PhysX error.

## Interfaces and evidence

`NativeStandingSession.reset_canonical()` establishes the only initial reset; `observe()` returns private named arrays; `step_control(target)` executes exactly8 native steps; `observe_control_substeps()` returns private copies of the last completed8 rows, surviving chunk flushes. A future separately versioned learner owns its action adapter and episode/reset policy. This source does not silently install one.

CLI: `run_standing.py --asset <exact9> --admission <actual actuation directory> --num-envs1 --output <fresh>` plus native `--device cuda:0 --headless`. For32 add `--num-envs32 --standing-one <accepted same-source1 directory>`. Arguments have spaces in actual invocation (`--num-envs 1`, `--num-envs 32`). `--preflight-only` is stdlib-safe with all late launcher flags. Host APIs: `standing_contract.verify_inputs(args)` and `validate_result(directory, identity)`; marker `REFERENCE_SCREEN_APP_READY` is emitted only after actual AppLauncher construction. Root owns hardened600s/90s host/guard and dispatch.

Raw substeps are saved in compressed800-row chunks. Contact patches are separate JSONL. A failing getter preserves all already-read fields and the actual advanced counter; `captured_steps` and `all_rows_recorded` distinguish complete rows from a partial last step. Session zero-force cleanup runs before the inherited pre-close seal. Post-exit native_errors and logs remain subject to host inventory/validation. Memory readback separates processRSS/HWM, hostfree/available/cache and CUDAfree/torchreserved; it does not attribute unified-memory cache to SDF. Actual1→32 scaling remains unmeasured until these runs execute.

Tests are CPU fixtures and source checks, not native stability evidence. The parent owns final freeze acceptance/publication and actual dispatch. See PUBLICATION_PROPOSAL.md for required central framework updates.
