# Canonical standing1 terminal auditor

This read-only auditor binds the actual standing1 invocation `f3cd14c1078e47c891d97718643b22aa`, unit `hexapod-canonical-native-standing-001-20260910.service`, output `native_standing_001` and pause `forecast_pause_006` under `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`. Corrected guard002 was actually dispatched after preserving guard001's invalid NVIDIA flag failure before allocation.

Exact input pins are native source00140 `f81f61d257cb92b65ad80f3b7d27c45497e66254092e244a32da02555b11e2a4`, host00118 `30235851f0dc95ce0c36ac3ec6912db144a7d3d6a6ab21c023d70d7cfefc2a33`, guard00273 `0860dfaaa515d6256dd72fdcd2b537c1f747f23c6427c6c50fcabae4da856b1b`, unchanged ownership supervisor926 and canonical asset9. Admission is the actual 19-file `native_actuation_001/actuation` directory, map `1ccd8f4ffaa2eadc1fd9c6202af8da965b0e9b95f39c585b68a3a64313ce2728`. Its original coordinate/effort evidence is historical input, not a new standing result.

Root runs `python3 -B -S audit_remote.py` only after terminality and redirects stdout/stderr outside original run/pause and frozen inputs. The script only reads files, `systemctl show`, `journalctl` and exact-name/ID `docker inspect`. It changes no timer, source, container, output or GPU state. Full source/host/guard/supervisor/asset/admission maps are checked in both passes. All raw run/pause files are hashed twice with byte sizes.

The full original host input validator runs with explicit `num_envs=1` and no standing-one input. A completed campaign requires the actual native standing validator to pass after process exit, the same receipt, successful unit/job status and complete immutable post-exit raw seal. `raw_acquisition_completed` is separate from `standing_completed`: acquisition can complete 8,000 steps while physical or quiet checks fail. The original state and optional standing report are retained verbatim in the audit result. An authentic rejected standing result remains an audited terminal failure, never retroactively a pass. The contract stays provisional and never sets broader physical admission, training permission or Stage2 completion.

Exact original timer restoration and full dispatched CLI including `--num-envs 1` are required. Both recorded container name and ID must be absent; an unavailable ID is explicitly not claimed absent, and unknown Docker errors reject. Eight focused final CPU tests pass, including completed acquisition with failed standing acceptance, exact cleanup/restoration/CLI, inventory mutation and stdlib/read-only operation. No actual terminal audit was executed by this preparation. Root owns native results and central project publication under `docs/PROJECT_SITE.md`.
