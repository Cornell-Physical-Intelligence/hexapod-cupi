# Independent canonical standing preparation review

This is a CPU and source review of the new canonical provisional standing screen. It does not certify native standing, hardware stability or Stage2 completion. The owner is bound by its final 40-payload frozen manifest `f81f61d257cb92b65ad80f3b7d27c45497e66254092e244a32da02555b11e2a4` in `review.json`; this review never edits the producer, native inputs or frozen predecessors.

The installed110 contact implementation supports the declared six-array `get_contact_data(0.0025)` interface, world contact points/normals and force scaling. Articulation material and contact/rest offset getters use the declared `(N,153,3)` and `(N,153)` layouts. Observed native name order controls joint gains, limits and link transforms; XYZW native pose conversion and source-shape contact coordinates are consistent. Floor and material assumptions are explicit, separate from canonical asset bytes.

The review found a concrete geometric omission in the initial non-toe clearance accelerator: dropping vertices with source X>=0.115 missed portions of 481 crossing triangle edges per tibia. A source -X plane could overstate clearance by 56.5 mm. The included reproducible CPU script preserves that counterexample; it is not a realized robot pose or native result. The producer now includes every edge intersection at X=0.115 before hull reduction, preserving exact minima over the clipped original triangle surface without changing colliders.

The producer also corrected inherited `ground=False` metadata, made each partial-step record fresh before pre-step getters, and separates actual controlled steps from successfully captured rows. Post-step getter failure preserves the actual native counter and fields already read; a prior complete sample cannot be relabelled as a new failure. Single initial reset and first-failure semantics remain explicit. Complete rows retain measured joint and speed bounds, full-run plate/nonfoot/clearance failures and the requested/applied servo channels separately. Quiet scoring uses the unchanged source009 function and raw SDK rates; angle-difference rate is separately exported.

Historical source009 standing gates evaluated support, nonfoot contact and requested saturation after settling, while termination, height and applied cap covered the full trial. Full-controlled-run nonfoot and exact mesh-clearance checks are a stricter new canonical requirement explicitly selected by root; this review does not falsely label them inherited requirements. The post-settle six-toe and 0.5% requested saturation requirements remain intact.

Final acquisition remains subject to actual native contact, cooking, execution-time and physical/quiet results. The 600-second host bound is unchanged. Same-source standing1 is required before a separately dispatched standing32, and neither automatically starts training. Root owns any central project publication under `docs/PROJECT_SITE.md`.

All 40 final owner hashes verified and all 12 final owner CPU tests independently passed. No remaining concrete blocker was found for the bounded native standing1 screen. Earlier draft hash/test receipts remain clearly labelled, separate from final binding.
