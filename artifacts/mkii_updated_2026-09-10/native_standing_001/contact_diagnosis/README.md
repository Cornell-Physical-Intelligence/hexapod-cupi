# Actual standing001 zero-normal contact rejection

The original native standing attempt failed after 14 actual physics steps, 13 complete captured rows and one complete control. Its SDK/scene/material/asset checks passed; this was not a completed standing screen. The frozen original classifier is independently replayed and reproduces `Invalid patch normal`.

All 32 original raw hashes and sizes matched the root terminal audit before analysis. The failed contact buffer contains 153 unique counted slots in disjoint ranges, below its capacity of 1,024. RF indices17–143 are 127 counted records with exactly zero force, zero normal and zero separation, one repeated finite point inside the existing RF toe cap. The seven nonzero-force records all have unit-normal error at most3.5763e-7, far inside the unchanged1e-3 test. Nineteen other zero-force records have valid normals. Thus neither a normal-tolerance boundary nor a nonzero-force invalid normal caused this failure.

The report retains all six native ranges and reconstructed force vectors. These observed vectors are not an accepted continuation or proof of stable standing. The installed110 Python source explicitly allocates and forwards separate force, point, normal, separation, count and start buffers, using the supplied2.5ms interval to convert impulse to force. No evidence here establishes the native internal reason for the repeated zero tuples; they are not relabelled as confirmed padding or an SDF gradient.

A minimal proposed correction recognizes only the exact finite tuple force=0, normal=(0,0,0), separation=0 as inactive zero-normal evidence. Every original index, body, point, separation and category must remain recorded. Its vector contribution is mathematically zero and cannot create support. Every nonzero-force invalid normal and every other malformed record must still reject at the same1e-3 unit-normal bound. Contact force thresholds, capacity/completeness checks, physics, geometry, gains and quiet gates remain unchanged. This is a proposed interpretation, not an adopted native success.

`analyze.py` reads the exact original source and raw inputs and produces `report.json`; `initial_rows.json` retains all153 counted records from the first inspection. No raw/source bytes were modified and no remote or GPU actions occurred. Root owns any successor selection and central project publication under `docs/PROJECT_SITE.md`.
