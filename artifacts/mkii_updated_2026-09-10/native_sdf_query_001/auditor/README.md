# Canonical SDF query terminal auditor

This read-only auditor is prepared for `hexapod-canonical-native-query-001-20260910.service`, output `native_query_001` and pause `forecast_pause_005` under `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`. The actual dispatch invocation is `8304b4418c734871a21cad8f2fe4184e`, independently supplied by root after dispatch.

It preserves the actuation001 auditor's logic and changes only the query contract, namespaces and exact source00250/host16/guard46 pins. The full ownership supervisor926, canonical asset9 and original inspection00313-file admission remain required. Both full input passes verify source `174a0c23eb351816abc9fea72acacaf8f40173baa40f73bbe69f70bc71e785f2`, host `b919fc90924da8765fb32510730392e42b94b7b5fa41d832272a6912f60509ae`, guard `36d56d88953a54bde60191fea7cfb74c49da8f95e4808b3e4f08f0d36f3b459f` and exact Phase A map `3905a714dce4055d5f85d2952584b2d42a236f65d144cd51ef9d32fda41fafe5`. The admission bytes are historical input provenance, never relabelled as new output.

Root runs `python3 -B -S audit_remote.py` only after the owner is terminal, capturing stdout/stderr outside frozen inputs and original run/pause directories. Operations are limited to file reads, `systemctl show`, `journalctl` and exact-name/ID `docker inspect`. No GPU, service, output, timer or source state is changed. All run/pause bytes are hashed twice with sizes; source/host/guard/supervisor/asset/admission maps are checked before and after.

The actual query contract reconstructs input identity and replays the completed native result after process exit. Completion requires the same native acquisition receipt, successful owner/job exit, exact input identity and full post-exit payload seal. This validates acquisition of eight import steps plus twelve read-only calls; it does not promote the distance or gradient result. In particular, false `declared_semantics_supported` and `geometry_accuracy_within_proposed_bounds` remain valid diagnostic outcomes when acquisition itself completed.

`audit_verified` is distinct from `query_completed`. An authentic failure remains `authentic_terminal_failure` with completion false; any audit inconsistency exits nonzero. Missing recorded IDs are expressly not claimed absent. Unknown Docker errors reject. Original timer restoration and complete dispatched command, including the read-only admission, are required. Current timer activity is not inferred from a restoration receipt.

Eight focused CPU tests cover stdlib import, terminal/invocation identity, failure versus completed acquisition including false diagnostic booleans, exact cleanup, restoration/dispatch bindings, inventory corruption and absence of mutation APIs. Root handles actual terminal acquisition and central progress/evidence records under `docs/PROJECT_SITE.md`.
