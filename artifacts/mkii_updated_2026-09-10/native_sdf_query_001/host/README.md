# Canonical native SDF query host

This prepares one bounded SDF acquisition using source002 freeze `174a0c23eb351816abc9fea72acacaf8f40173baa40f73bbe69f70bc71e785f2` (50 payloads), the unchanged canonical asset9 and exact completed inspection003 admission13. The embedded probe remains frozen at `e1109fc90dc3953bcb064fd0ab80158503ff0d48a3457453d504c5aa0e23e367`. Physical and training admission are false.

The native entry performs the same eight explicit zero-gravity import steps, then 12 read-only calls of 140 points each, across six exact tibia colliders with reordered repeats. The host independently binds schema `canonical_native_sdf_query_v1`, scope `canonical_native_sdf_query_only`, eight steps, no ground and exact query/fixture lineage. It supplies no policy, drives, effort pulses or new physics settings. The query addon itself requests no simulation steps.

Source `/query:ro`, canonical asset `/asset:ro` and exact Phase A `/admission:ro` mount alongside only a fresh `/output:rw`. The contract and its parent load by verified file-relative origins, without a global path workaround. Original source009 `run_owned` and `owned_container` code remains unchanged, with the same 600-second phase and actual AppReady90 bound; that supervisor supplies ownership infrastructure only. After exit, the original query contract verifies raw acquisition and late errors, then the host inventories/rechecks every payload and original input.

Completed acquisition deliberately preserves `declared_semantics_supported=false` or `geometry_accuracy_within_proposed_bounds=false` when those are the measured diagnostics. Those flags do not become contact, standing or training acceptance. Native failure, incomplete acquisition, changed data, missing callbacks or late errors remain failures. This distinction is covered by the host completion fixture.

Twelve focused CPU tests passed on the final source binding. The actual Python 3.12 `-S` setup imports the new contract/parent, compiles the entry and compares original supervisor code objects with every external process call forbidden. No native or remote execution occurred in preparation. The original full-input preflight is recorded separately.

Root-only preflight after transfer:

```sh
python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/query_host_001/launch_query_spark.py --preflight-only \
  --source /home/orionh/HEXAPOD_runs/canonical_direct_20260910/query_source_002 \
  --asset /home/orionh/HEXAPOD_runs/canonical_direct_20260910/asset_001 \
  --admission /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_inspection_003/inspection \
  --supervisor-source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_physics_source_009 \
  --output /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_query_001
```

Root alone dispatches through the separate query guard and handles any `docs/PROJECT_SITE.md` central update. Prior sources, owners and results remain immutable; no tracked file was edited here.
