# Canonical SDF query ownership guard

This prepares one bounded native SDF acquisition after completed canonical actuation001. Root alone transfers, checks actual Spark setup and dispatches. It binds source002 `174a0c23eb351816abc9fea72acacaf8f40173baa40f73bbe69f70bc71e785f2` (50 payloads), host001 `b919fc90924da8765fb32510730392e42b94b7b5fa41d832272a6912f60509ae` (16 payloads) and host runtime `9737b48897f97e09cfae0da1453a3eee727092853e93f8994918db0d70f3853f`.

All paths use `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`: source `query_source_002`, host `query_host_001`, unchanged `asset_001`, read-only admission `native_inspection_003/inspection`, fresh output `native_query_001` and pause `forecast_pause_005`. Owner is `hexapod-canonical-native-query-001-20260910.service`; fallback is uniquely `hexapod-canonical-forecast-restore-005`. Root's launch command is `python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/query_guard_001/launch_guarded_remote.py`.

Both before pausing and immediately before dispatch, the guard verifies the entire source and host, original ownership supervisor, canonical asset and admitted Phase A inputs via the frozen native contract. The schema is `canonical_native_sdf_query_v1`, scope `canonical_native_sdf_query_only`: eight explicit import steps, followed by twelve read-only query calls with 140 points each. The queries add no physics steps or state writes. Source, asset and the exact 13-file Phase A admission mount read-only. Only a fresh output is writable. Physical and training admission remain false. Completed acquisition may retain false distance-semantics or proposed-accuracy diagnostic booleans; these are never relabelled as quality acceptance.

Prior ownership binds actual actuation001 invocation `c6d287881452428d90e8e432ae739c86`, all 29 unchanged raw output/pause files and its independent terminal audit. Its completed 2,064-step state, source/host identities, post-exit validation and complete raw seal are required. Both actual recorded container name and ID must be absent. The previous owner must be inactive with exit0; a collected empty invocation is allowed only with unchanged audited historical completion. Exact pause004 restoration `1789074862.1635034` and its original timer set are checked. This previous coordinate/effort test supplies ownership lineage, not physical admission for the new query.

The ownership flow retains both GPU locks, unknown non-weather workload refusal, explicit all-weather priority and coordination `35af5100e39980f55a3f5e19654e4550b3c3fb6da114b64fbe0d143817242cf3`. Embedded restoration and CUDA ancestry code remain unchanged. Bounds remain 720-second owner, 180-second stop allowance and 15-minute fallback; the original `run_owned` and `owned_container` retain their 600-second phase and actual AppReady90 bounds. No automatic training follows.

All 21 focused final guard tests pass. They cover original restoration/ancestry behavior, before/after-pause order, admission CLI wiring, altered prior bytes, false completion, unknown container absence, fresh output requirements and dispatch uncertainty. The actual final local Python 3.12 `-S` guard-to-host preflight passes against frozen source, host, asset, admission and supervisor, with external calls forbidden. This preparation performed no remote or GPU action.

Verify with `python3 -B verify_bundle.py`. Root handles central publication under `docs/PROJECT_SITE.md`; native acquisition, physical admission and policy progress remain separate. All frozen predecessors are preserved.
