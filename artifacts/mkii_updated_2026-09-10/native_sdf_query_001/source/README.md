# Canonical native SDF query acquisition

Prepared, not dispatched. This new source composes the successful canonical inspector003 import prefix with the frozen read-only tibia distance addon. It requires the exact prior actual003 inspection inventory and state. It admits no robot support, contact, control or training.

Entry: `run_query.py --asset <canonical-nine-file-directory> --admission <actual003-inspection-directory> --output <fresh-directory> --device cuda:0 --headless`. `--preflight-only` uses the standard library and does not import Isaac or run a GPU. The host contract is `query_contract.verify_inputs(args)` and `query_contract.validate_result(output, identity)`; the phase is `query`. Root retains dispatch, deadlines, pause/restoration, owned-container cleanup and final log inventory. No new host or guard is included.

The actual003 configuration, imported geometry, SDK/provider checks, native mass/inertia/frame validation, zero gravity, no ground, no drives, self-collision setting, exact 153 SDF-path check and eight explicit observation steps are preserved. `NATIVE_DELTA.patch` records the bounded entrypoint changes. The addon runs once after that prefix's final kinematic refresh. It requests 12 calls of 140 points across six exact nonspherical tibia shapes and no additional physics step, target or state write. Whole source and canonical inputs are checked before initialization and before shutdown.

A completed acquisition can still report `declared_semantics_supported: false` or `geometry_accuracy_within_proposed_bounds: false`. Those are new proposed query diagnostics, not original robot qualification gates. Both are returned to the host and preserved; no automatic reinterpretation or controller promotion follows. The portable addon replay can independently recompute a future result from externally pinned raw bytes. See `probe/README.md` for exact frames, signs, unit assumptions, source geometry, limits and the completed Fable MAX review.

Every query result, including a partial first result or nonfinite tensor, remains in `query/`. The outer receipt seals its nested state and raw files. PhysX events remain subscribed throughout the run; pre-close events are copied into state, and `native_errors.json` plus Isaac lifecycle logs deliberately remain mutable until the host inventories them after process exit. Host validation rejects a late native error. It also requires exact query state, complete raw inventory and equality between the last import sample and both query boundaries. This source does not reuse unsupported cooking APIs or publish proprietary Tensor110 implementation files.

`SOURCE_ORIGIN.json` binds exact copied003 helpers; `PARENT_INSPECTION003_FREEZE_SHA256.json` preserves its source map. `PHASE_A_SHA256.json` binds 13 files inside the actual003 inspection directory; the separately published full campaign also contains host/cleanup receipts. `probe/` is the complete immutable 28-payload addon plus its original manifest. Only needed parent runtime/identity helpers are copied; this source is intentionally not a full copy of all003 preparation materials.

CPU validation: eight source tests pass, including actual003 admission, changed/missing inputs, unchanged physics configuration/loop/helper AST, required admission CLI, old-result refusal, nested raw-state sealing and late-error rejection. Root independently passed the addon's 17 tests. No native SDF distance output or actual query admission exists yet.

Source002 corrects only the host contract import seam: it loads adjacent inspection_contract.py with importlib by exact file path. The added isolated Python -S test removes the native directory from sys.path and injects an unrelated cached inspection_contract; the exact local helper still loads. Frozen source001 is preserved in PARENT_QUERY001_FREEZE_SHA256.json. Entry, addon, physics and measurement gates are byte-identical.

Later publication must follow `docs/PROJECT_SITE.md`: append a bounded central record, update relevant status/measurement documentation truthfully and validate the poster build. Suggested destination: `artifacts/mkii_updated_2026-09-10/native_sdf_query_preparation_001/`. No tracked file was modified by this preparation.
