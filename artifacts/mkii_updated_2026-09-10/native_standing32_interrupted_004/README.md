# Standing32 interrupted by competing CUDA work

The source00532/0 run reached the **900-control log milestone**, then the unchanged ownership monitor detected an unrelated CUDA process and yielded its owned job. AppReady had succeeded. This is an operational interruption, **not a physical standing rejection or an accepted32-robot run**.

The job records competitor PID3803709, `/home/orionh/ithaca-reconstruction/env/bin/python`, in `session-c11072.scope`, and the exact error `RuntimeError('Unrelated CUDA process appeared; yielding this owned job')`. The host owner ended failed/exit1 with MainPID0. Actual invocation: `b33d418438ea438ca0805eea68e70a43`.

The native state was never finalized: its original `running`, zero recorded explicit steps, empty checks and empty errors remain unchanged. The log and retained remote trace files show that physics ran; the stale state does not mean no steps occurred. No final exact step count, completed row-clock proof, final quiet score or whole-run physical verdict is inferred from the milestone. `standing_report.json`, `session.json` and `control_trace.npz` are absent from the original inventory.

Root's independent terminal audit hash-inventoried all **34 original raw files /4,666,042,464 bytes**. This compact bundle contains the verified **24 small raw files /3,317,848 bytes**. The original contacts stream and9 substep NPZ chunks remain on Spark: **10 files /4,662,724,616 bytes**. Every omitted original has its SHA-256, exact size and remote path in `REMOTE_ONLY_RAW.json`; no large raw download, decompression or numerical replay is claimed here. `terminal/RAW_SHA256.json` retains the full inventory; `terminal/RAW_ENCODING.json` describes only the24 acquired files.

Exact source005109, host00763, guard32_00478 and terminal auditor21 payloads retain their original freezes. Root's12 setup, transfer, dispatch and compact progress captures are separately hash-bound. The audit verifies immutable source/host/guard/supervisor926/asset9 inputs and the accepted native6 prerequisite. Both exact container name and ID were absent at the historical terminal check. Pause016 restoration completed for the per-job snapshot, while the existing persistent reservation remained; this is not a claim that the old reservation blocked every external producer.

Source, geometry, provisional servo,32/0 solver recipe,1,200-second phase/90-second startup bounds and all numeric acceptance gates were unchanged. The separate native6 one-robot pass remains valid. This interrupted run permits no PPO, Stage2, walking or hardware admission. A separately audited complete32 run is still required.

Run `python3 -B -S verify_bundle.py` from this directory or use its absolute path. The standard-library verifier checks every locally included byte, original component freezes, the34-file remote inventory against the historical audit, exact24/10 partition, logged milestones, cleanup/restoration receipts and the original source contract's `Incomplete standing acquisition` rejection. It does not contact Spark, verify remote-only bytes anew, launch a simulator or rescore missing raw arrays. Root owns the central project update under `docs/PROJECT_SITE.md`.
