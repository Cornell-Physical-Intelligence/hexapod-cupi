# Canonical native inspection host 002

This is a source-pin-only successor of frozen host001. The only runtime edit is the `SOURCE_FREEZE` constant: native002 has 28 payloads and freeze `a9a4dbe0e28c0ed85a5d5d6dd7788bdf8bc1b9154f676aa7262fc6eb4beac3f9`. Every host function is byte-identical, including original-input verification, read-only native source/asset mounts, post-exit result/seal checks and failure-preserving finalization. The actual source009 `run_owned` and `owned_container` code remains unmodified and is used for ownership infrastructure only, with the same 600-second total and 90-second actual AppReady bounds.

Native002 corrects the recorded setup failure against the actual installed public tensor API. It explicitly distinguishes completed initialization and 153 valid requested SDF paths from an unavailable legacy cooking counter; unknown counter evidence remains null. Its identity adds `sdf_inspection_contract=canonical_native_sdf_initialization_v2`. Host identity is derived from the frozen producer contract and remains exact. The nine canonical asset files, eight explicit zero-gravity 2.5 ms steps, no-drive/no-target intent and no physical/training admission remain unchanged.

This preparation does not claim that native002 runs successfully. Native001 reached AppReady and failed before loading the model because its private API-source import did not exist. Its source, host, guard and authentic terminal failure remain immutable. Guard002 requires that failed owner’s exact cleanup and restoration before allocating this successor.

Twelve focused host tests pass on the final binding. The actual local Python 3.12 standard-library setup check imports the frozen native contract and exact original supervisor, compiles the native entry and verifies unchanged supervisor code objects with all external process calls prohibited. Its injected runtime identity is explicitly synthetic; the separate full-input preflight verifies actual identities. These are CPU checks only.

Root-only preflight after transfer:

```sh
python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/inspection_host_002/launch_inspection_spark.py --preflight-only \
  --source /home/orionh/HEXAPOD_runs/canonical_direct_20260910/inspection_source_002 \
  --asset /home/orionh/HEXAPOD_runs/canonical_direct_20260910/asset_001 \
  --supervisor-source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_physics_source_009 \
  --output /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_inspection_002
```

Root dispatches through the separately frozen guard002. It owns remote state and eventual central publication under `docs/PROJECT_SITE.md`; this preparation changes no tracked files and runs no remote/GPU command.
