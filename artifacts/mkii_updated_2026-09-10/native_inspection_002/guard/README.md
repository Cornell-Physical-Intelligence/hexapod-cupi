# Canonical native inspection guard 002

This guard prepares one fresh retry of the canonical native asset inspection after inspection001’s authentic API-lookup setup failure. It binds native002 source freeze `a9a4dbe0e28c0ed85a5d5d6dd7788bdf8bc1b9154f676aa7262fc6eb4beac3f9` (28 payloads), host002 freeze `3b0d22016e653b53fade516b5d55402cada4624b3c60296f27e044bf1083835e` (16 payloads), and host runtime `fb3a2ac8c9eea0fbd4eb67a38464e5684ae22c3546d6b55563791e9636240be8`. Root alone reviews, transfers and dispatches; no native or remote execution occurred during this preparation.

Paths are under `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`: source `inspection_source_002`, host `inspection_host_002`, unchanged canonical `asset_001`, fresh output `native_inspection_002` and pause `forecast_pause_002`. The owner is `hexapod-canonical-native-inspection-002-20260910.service`; the separate unique fallback is `hexapod-canonical-forecast-restore-002`. Root launches the transferred script with `python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/inspection_guard_002/launch_guarded_remote.py` only after actual Spark input preflight.

The prior owner is exactly inspection001 invocation `87a75ef9d130496a9e040b86b14367e9`, with exit status 1 and an inactive/failed zero-MainPID state. Its exact 13 raw output/pause hashes and independent terminal-audit hash are pinned. The failed campaign/native state must remain failed with zero explicit steps and the recorded missing `omni.physics.tensors.impl` API-source error. This guard deliberately accepts that authentic terminal failure as cleanup ancestry; it does not require a false completed/exit-zero verdict. The exact generated name and recorded 64-character container ID must both be absent. A collected empty live invocation is accepted only against the unchanged audited historical failure; a different nonempty invocation rejects. Exact pause001 restoration time and originally active timers remain required.

The existing main flow and input verifier are AST-identical to guard001 apart from the unique fallback unit name. The embedded restorer is byte-identical. Both GPU locks, unknown non-weather workload rejection, explicit all-weather priority, coordination `35af5100e39980f55a3f5e19654e4550b3c3fb6da114b64fbe0d143817242cf3`, pre-pause/predispatch checks, 720-second owner, 180-second stop allowance and 15-minute fallback remain intact. The inherited native phase remains 600 seconds total with actual AppReady within 90 seconds. There is no automatic subsequent allocation.

Twenty-one focused guard tests pass on the final binding, including the exact copied prior failure bytes, both recorded absences, rejection of changed/false-success/live owners, unknown Docker failures, input checks before and after pause, restoration after uncertain dispatch, and unchanged main/restorer behavior. The final local Python 3.12 `-S` guard-to-host preflight checks the actual source28/host16/asset9/supervisor926 while forbidding external calls. Pending bindings are tested through an explicit negative fixture. These are CPU preparation checks, not a successful native retry.

The original host/guard/source001 and actual failure evidence remain immutable. Source002’s public API/provenance correction and explicit SDF initialization contract are owned by its separately frozen producer. The guard supplies no gains, targets, checkpoint, controller or physical admission. Publication follows `docs/PROJECT_SITE.md` through root’s central update, preserving native inspection, passive sweeps, physical admission and future PPO as separate stages.
