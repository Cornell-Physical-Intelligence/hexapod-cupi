# Canonical coordinate and effort diagnostic host

This host prepares one bounded canonical native actuation diagnostic. It binds producer source002 freeze `1f02009cb22c1efbe4a0d0c7f7988d9d63b658d78e509ea444b7afafee6178c9` (38 payloads), the unchanged nine-file corrected-mass canonical asset and exactly completed native inspection003 as read-only input. The completed Phase A state SHA is `987dd4d2a2314ec234e9629dc4c045f10d6230f3301b4d9594c0695052d58228`; its full 13-file map is `3905a714dce4055d5f85d2952584b2d42a236f65d144cd51ef9d32fda41fafe5`. That initialization evidence is not physical admission.

The explicit allocation is eight import-prefix steps plus 2,056 experiment steps at 2.5 ms, in zero gravity with no floor or position drives. There are 36 named signed effort cases. Each declared pulse is 0.005 Nm for eight steps, with baseline and coast intervals, and 36 bounded coordinate checks. The contract limits, reset/evidence protocol and validation belong to the frozen producer. The host independently rejects changed step counts, admission identity, case schedule, gravity/drive/force settings and declared software limits. No checkpoint, policy, training or automatic successor is provided.

The container mounts `/actuation:ro`, `/asset:ro` and `/admission:ro`, plus a fresh `/output:rw`. It runs `/actuation/run_actuation.py --asset /asset --admission /admission --output /output/actuation --device cuda:0 --headless`. The native contract uses a file-relative standard-library parent import. Its exact source and import origin are verified before use. The same full source/asset/admission/host readback happens after container exit, followed by original native raw/result validation and a complete post-exit payload seal including shutdown logs. Failed reset/getter/setter evidence and original failure statuses remain preserved.

Original source009 `run_owned` and `owned_container` functions are unchanged and used only for ownership, liveness, AppReady, contact-log completeness and exact cleanup. They retain a 600-second phase and 90-second actual AppReady requirement. The host changes only the native contract/phase, source binding, additional admission input and declared allocation checks; it does not substitute historical physics. Runtime scope is `canonical_native_coordinate_effort_only`, with physical/training admission false.

Twelve focused final host tests pass. The actual Python 3.12 `-S` setup test imports the native contract and its exact parent, compiles the entry and checks unchanged original ownership code objects with every external process call prohibited. That setup uses a declared synthetic runtime injection; a separate full-input preflight verifies the real identities. No native or remote execution occurred in this preparation.

Root-only full preflight after transfer:

```sh
python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/actuation_host_001/launch_actuation_spark.py --preflight-only \
  --source /home/orionh/HEXAPOD_runs/canonical_direct_20260910/actuation_source_002 \
  --asset /home/orionh/HEXAPOD_runs/canonical_direct_20260910/asset_001 \
  --admission /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_inspection_003/inspection \
  --supervisor-source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_physics_source_009 \
  --output /home/orionh/HEXAPOD_runs/canonical_direct_20260910/native_actuation_001
```

Root dispatches through the separate weather/ownership guard only after actual Spark setup checks. All earlier inspection sources, wrappers and evidence remain immutable. Eventual publication follows `docs/PROJECT_SITE.md` with a bounded central update; no tracked or remote files were changed here.
