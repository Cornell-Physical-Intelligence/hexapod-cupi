# Canonical coordinate and effort ownership guard

This prepares one bounded native coordinate/effort allocation after the independently audited, completed canonical inspection003. Root alone transfers, checks Spark setup and dispatches. Source is the frozen producer002: `1f02009cb22c1efbe4a0d0c7f7988d9d63b658d78e509ea444b7afafee6178c9` (38 payloads). Host001 is `0f9a54963ec3220d0625722e588bb17c3efd534b0c5ac04c1f482db69511163e` (19 payloads), runtime `ca7c95bb51faa841702cdd69e38e1558a0d13cfab5ca07538cc9c0140505d956`.

All paths use `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`: source `actuation_source_002`, host `actuation_host_001`, unchanged `asset_001`, read-only admission `native_inspection_003/inspection`, fresh output `native_actuation_001` and pause `forecast_pause_004`. The owner is `hexapod-canonical-native-actuation-001-20260910.service`; fallback is uniquely `hexapod-canonical-forecast-restore-004`. After actual preflight, root runs `python3 -B /home/orionh/HEXAPOD_runs/canonical_direct_20260910/actuation_guard_001/launch_guarded_remote.py`.

Both pre-pause and predispatch checks verify the complete new source and host, original ownership supervisor, canonical asset and completed Phase A admission via the frozen native contract. The schema must be `canonical_native_actuation_v1`, scope `canonical_native_coordinate_effort_only` and allocation 2,064 explicit steps. Source, asset and exact admission mount read-only; only a fresh output is writable. Physical and training admission remain false.

Prior-owner evidence binds inspection003 invocation `ab32f02fccd1400da1e25ff3b8e525d7`, all 23 raw output/pause files and its independent audit. Its completed eight-step native state, host post-exit validation/seal and exact source/host identities remain required. Both actual recorded container identifiers must be absent. The previous owner must be inactive with exit0, or expose a collected empty invocation backed by the unchanged audited completion. Exact pause003 restoration `1789072877.4510024` and its original timer set remain required. Phase A establishes only native initialization evidence for this diagnostic, never standing or locomotion admission.

The reviewed main ownership flow retains both GPU locks, unknown non-weather workload refusal, explicit all-weather priority and coordination `35af5100e39980f55a3f5e19654e4550b3c3fb6da114b64fbe0d143817242cf3`. The embedded restorer and CUDA ancestry loop are unchanged. The new guard adds only required actuation/admission arguments, source/scope bindings and completed003 ancestry. Bounds remain 720-second owner, 180-second stop allowance and 15-minute fallback; original `run_owned`/`owned_container` remain unchanged with 600-second phase and actual AppReady90. No automatic training follows.

Twenty-one focused final guard tests pass, including exact prior bytes/identifiers, false completion and unknown absence rejection, before/after-pause ordering, admission CLI wiring, original restorer/ancestry behavior and restoration after uncertain dispatch. The final local Python 3.12 `-S` guard-to-host preflight verifies all actual frozen inputs while prohibiting external calls. Pending bindings are tested through a negative fixture. No remote or GPU actions occurred in preparation.

Root owns any central publication under `docs/PROJECT_SITE.md`, keeping native inspection, coordinate/effort evidence, contact/torque admission and later policy training separate. Frozen inspection sources and actual results remain unchanged.
