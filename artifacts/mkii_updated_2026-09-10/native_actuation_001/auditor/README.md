# Canonical coordinate and effort terminal auditor

This prepares a read-only terminal audit of the actually dispatched `hexapod-canonical-native-actuation-001-20260910.service`, invocation `c6d287881452428d90e8e432ae739c86`, output `native_actuation_001` and pause `forecast_pause_004` under `/home/orionh/HEXAPOD_runs/canonical_direct_20260910`.

It adapts the frozen inspection003 auditor only for the actuation contract/phase, exact native source00238/host19/guard40 pins and read-only completed Phase A admission. The unchanged supervisor926 and canonical asset9 remain bound. Source is `1f02009cb22c1efbe4a0d0c7f7988d9d63b658d78e509ea444b7afafee6178c9`; host is `0f9a54963ec3220d0625722e588bb17c3efd534b0c5ac04c1f482db69511163e`; guard is `9aef608cf860f32101a4aaf494d8d211adc47e81123418b423fc082a28c3b9e4`. Admission is exactly `native_inspection_003/inspection`, with its original 13-file map `3905a714dce4055d5f85d2952584b2d42a236f65d144cd51ef9d32fda41fafe5`.

Root executes `python3 -B -S audit_remote.py` only after terminality and captures stdout/stderr outside frozen inputs, original run/pause and admission directories. Only file reads, `systemctl show`, `journalctl` and exact-name/ID `docker inspect` occur. No source, service, output, timer or GPU state is changed. Input source/host/guard/supervisor and asset maps are checked before and after output audit. The historical admission is fully hashed in both passes and compared independently; its bytes are recorded as admission provenance, not presented as new actuation output. All new run/pause files are likewise inventoried twice with SHA and byte sizes.

The exact original actuation contract reconstructs input identity, validates the unchanged Phase A admission, and replays completed actuation evidence after native process exit. A completed campaign needs the same exact native receipt, successful owner/job exit and complete post-exit host seal. Native validation includes the producer’s raw coordinate/effort/clock and reset checks; it never admits physical control or learning. Original failed states and error values remain in the report.

`audit_verified` is distinct from `actuation_completed`. An authentic failure remains `authentic_terminal_failure`, with completion false. Any audit inconsistency exits nonzero. Recorded name and ID absence are checked exactly; an unavailable ID is explicitly not claimed absent, and unknown Docker errors reject. Exact original timer restoration and the full dispatched command, including admission, are required. Current timer activity is not inferred from the restoration receipt.

Eight focused CPU tests pass against the final constants, covering stdlib import, terminal/invocation, authentic failure versus completed validation, cleanup ambiguity, exact restore/dispatch bindings, source-map corruption and absence of mutation APIs. No actual audit has been run by this preparation. Root owns terminal acquisition and central progress/evidence updates under `docs/PROJECT_SITE.md`.
