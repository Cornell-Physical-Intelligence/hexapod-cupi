# Standing004 guard under the persistent reservation

Source00387/e922… is unchanged; host00433/c72c… changes only its expected coordination hash. This guard binds actual coordination649ccda1bdef20bc967ae78f68a992cc8001f4ed7009f78cab5133cc67b29d8f and the root-installed persistent reservation at canonical_direct_20260910/exclusive_reservation_001. Native target/physics/gates and original source009 owned-container functions remain unchanged. The exact retry is native_standing_004, pause011, ownerhexapod-canonical-native-standing-004-20260910.service, fallback011; pause010 remains reserved separately.

Before pause and again before dispatch, the guard checks13 exact files: ACTIVE, initial_receipt, scheduler_before, and10 persistent stormscope dispatch/scout/monitor/publish/verify timer/service drop-ins. It also verifies each drop-in is in the loaded unit's DropInPaths, NeedDaemonReload=no and its covered scheduler inactive/failed. Missing, changed, unloaded or unexpectedly active reservation inputs refuse before mutation. The actual read-only13-file capture and root loaded-state receipt are included. Root subsequently covered idle ollama separately; this guard does not falsely claim its13-file inventory includes that later unit.

The per-job restoration control flow is unchanged. Since the persistent reservation already deferred the original timers, their per-job prior-active set is empty and this job starts none. Its result now explicitly says scope=per_job_snapshot_only and persistent_reservation_release_attempted=false, with reservation_path. A per-job restored.json is not a release of the persistent reservation. The guard never removes ACTIVE/drop-ins, and never signals SSH/system services or unrelated producers. Root owns eventual release and exact prior scheduler/resume identities. Actual CUDA ownership checks and both GPU locks stay mandatory; the scheduler reservation is not claimed to be a hardware partition or proof that arbitrary manual CUDA launches are prevented.

All31 interrupted standing003 raw hashes remain pinned externally and streamed in8MiB blocks. The exact host/job competing-workload failure, native unfinalized running/0/checks{}, prior owner/name/ID absence and pause009 restoration are preserved. Only small receipts are copied; no physical rejection, invented final counter or prior standing acceptance is inferred. The actual non-weather interruption remains immutable.

Bounds remain600s phase/90s AppReady,720s outer/180s stop/15min fallback. All30 focused CPU tests pass, including unchanged restorer control flow after removing only three metadata keys, actual NVIDIA argv, loaded reservation/drop-in rejection, streaming hashes and interrupted owner evidence. Actual frozen source/host/asset/effort standard-library preflight passes with external actions forbidden. That preflight hashes local exact reservation captures; live loaded-state checks are separately exercised with explicit fixtures and must run again on Spark before allocation. Original copied parent test receipts retain their old identities; tests_coordination_final.log and bound_coordination_preflight.json are this successor's actual checks.

Root alone transfers, performs actual setup and runs `<NewBASE>/standing_guard_006/launch_guarded_remote.py`, then audits and publishes central project-site updates under docs/PROJECT_SITE.md. No policy/checkpoint is loaded, and this preparation does not admit standing32, walking, hardware, training or Stage2.
