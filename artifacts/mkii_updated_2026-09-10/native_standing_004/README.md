# Optimized detailed robot: one-robot standing passes

The actual canonical detailed robot completed 1,000 controls / 8,000 physics steps with its 16-second quiet interval and every standing gate passing. Root's independent post-exit audit verifies all 38 raw files / 179,450,040 original bytes, unchanged source inputs, exact owned-container absence and per-job cleanup. Persistent Spark reservation controls remained active through that cleanup. This is provisional supported standing, not learned walking, new-model PPO, Stage 2 completion or hardware qualification.

This run uses source003's contact-processing optimization: cached per-call foot transforms and ordered force aggregation. The original 153 SDF colliders, every raw contact record, geometry, motor inertia, 400 Hz servo, provisional48V curve,1.6 N·m cap and all standing/quiet thresholds are unchanged. Exact parent-output replay and adversarial tests are included in source provenance. The previous throughput stop and unrelated-CUDA interruption remain separate immutable attempts.

`RESULT.json` contains exact measurements. The native report and raw trajectories are preserved without rescoring. The contact stream is stored losslessly with original size/hash in `terminal/RAW_ENCODING.json`. The offline verifier reconstructs about180MB temporarily and executes the original standing contract against those bytes. It performs no native simulation or remote actions.

The actual invocation is808028c883fc4362880f1f112b4b7360. Source003, host004, guard006 and terminal auditor004 are frozen here. The new reservation-aware guard checks exact loaded scheduler controls; its per-job restorer explicitly does not release the persistent reservation. Arbitrary manual CUDA launches are not blocked by hardware. Assets, prior effort admission and the original ownership supervisor remain separately published dependencies.

The next admitted step is matching-source32-robot standing, then fresh two-update PPO integration. Battery voltage, hardware calibration, thermal behavior and broader contact/terrain qualification remain open. Run `python3 -B -S verify_bundle.py` to check this bundle.
