# Quiet-priority PPO interpretation and next budget comparison

The stronger quiet objective reduced its conditional training loss but did not solve quiet standing: the actual50-update pilot and earlier CAPS50 both fail0/48 trials. Median scored-window drift is50.42→57.60mm, with requested saturation also worse in the new pilot. The1.870m maximum includes a reset inside the quiet window and must not be described as uninterrupted drift. The unchanged reset failures remain failures. See the [complete pilot and exact comparison](../../omni_diagnostics_2026-09-09/direct_omni_quiet_priority_pilot_001/README.md).

The gradient diagnostics distinguish a measured nonzero quiet objective from consistent quiet improvement. Its weighted norm is roughly15% of PPO at the first sampled pilot update and6% at the last. These are sparse pre-Adam measurements, not shares of actual parameter displacement. Three of eight bounded dot-product signs oppose quiet descent under plain gradient descent; Adam's actual movement is not inferred. Per-minibatch rate histories disprove treating the smoke's near-floor behavior as the whole pilot. Literal versus roundoff-tolerant floor counts are explicitly distinguished.

Claude Code Fable5.1 with `--effort max` reviewed supplied evidence in parallel. The frozen sub-bundles preserve final responses, actual invocation/provenance, independent calculations and corrected dispositions; no internal thinking stream is included. Unsupported inherited-Adam, causal KL, parameter-movement and sensor-floor explanations were rejected. Deterministic evaluation and actual angle changes confirm that current jitter cannot be attributed solely to velocity reporting.

The selected next experiment is a bounded500-update normal-CAPS budget baseline from the original checkpoint, with unchanged losses, physics and quality gates, through a new source version. Retain intermediate weights including250 for later diagnosis. More training is a hypothesis to test, not a claimed solution or performance extrapolation. A versioned same-source smoke and appropriate timeout/resource checks must precede dispatch; this review does not launch a job.

Current checkpoints contain model/normalizer and Adam state but omit rollout, simulator and global/private RNG state. Exact interrupted-trajectory resume is unavailable. A fresh-original longer comparison is smaller and clearer than an undocumented partial resume; the latter would additionally need explicit LR-scalar/progress restoration and reset/reseed semantics.

All three original review manifests and payloads are preserved byte-for-byte. Their scripts retain original temporary input locations as provenance, so these nested audits are not claimed to be standalone packages. The main [pilot verifier](../../omni_diagnostics_2026-09-09/direct_omni_quiet_priority_pilot_001/verify_bundle.py) provides a portable exact replay of the numerical report and CAPS comparison. Source/runtime snapshots and raw primary data remain in the linked preparation and run bundles. Current execution is recorded only in STATUS.md.
