# Historical500-update jitter diagnosis

This is historical C-study evidence only. The user subsequently designated the7.466088235kg detailed direct-drive model as ground truth for all future training. No new C-study GPU diagnostic or training is proposed or authorized here.

After excluding env20's entire reset-containing quiet window, all47 remaining rows have exactzero commands throughout22.02–32.00s. Raw policy action, requested target, executed target and actual joint angles share an aggregate4.4Hz peak. Raw action has75.89% of detrended, Hann-windowed spectral energy at2–5Hz; executed target64.19%; actual angles54.37%. The20–25Hz bands contain only1.60%,1.40%,0.29%, respectively. Consecutive nonzero target increments reverse sign35.53% of the time, and raw-action lag1 correlation has median+0.556. This is a coherent lower-frequency closed-loop oscillation; the data do not support a dominant one-control25Hz alternation explanation. Initialstand and moving windows peak4.5Hz at their available resolution, so the pattern persists across commands.

In those47 quiet rows,87.21% of adjacent joint targets reach the.04rad limiter;18.03% of raw actions exceed the normalized clipping bound. Actual adjacent-angle interval RMS spans0.724–1.478rad/s across joints/rows, median1.047. Requested saturation is11.13%. These values are independent of reported SDK joint-rate scoring. The actual angles move; SDK velocity bias is not an explanation that removes that motion.

Every valid recorded raw→clipped action equals clamp[-1,1] exactly, filtered and unfiltered requests are identical, and the unfiltered-request→executed-slew recurrence has zero maximum discrepancy. Logged targets remainwithin.040000081rad float32 roundoff. Target-to-actual-angle correlation peaks at a median0.852 with actual angle one50Hz sample later, versus0.840 at the same sample. This is descriptive closed-loop correlation and cannot identify a hidden controller delay or establish causation. The trace does not contain actual actor packets or sampled observation noise, so input-channel amplification cannot be reconstructed exactly from it.

The source-bound actor observes five63-value frames with raw SDK rates, positions, gravity, command and previous clipped normalized actions. It does not directly observe the executed target or accumulated slew-limiter state. Saturated rate limiting thus introduces controller state not explicitly represented in those inputs. This is an architectural lesson for a future model-bound state/action contract, not a proven single cause or checkpoint-compatible observation patch. SOURCE_PIPELINE_BINDING.json lists exact deployed hashes and physical noise units.

The existing control_probes002 already compared the original1971 checkpoint at the historical.03rad limiter: removing observation noise barely changed standing joint-rate RMS(.7501→.7492) or median saturation(5.74→5.80%). The80ms target filter worsened median saturation to8.08%; zero normalized action under active PD yielded.00158rad/s and0% saturation. Those are genuine earlier controls but use a different checkpoint/limiter and lack newly fetched spectral traces. They argue against blindly repeating a generic noise/filter/PD sweep; they do not prove what every channel does in checkpoint500 or the new robot.

All reset failures remain in report.json. Env20 terminates at30.24s and is excluded from the entirequiet-spectrum description, not relabeled successful. The original48-trial formal verdict remains0/48. Frequency resolution is.1Hz in the10s quiet/motion windows; analysis is limited to50Hz endpoint samples, not400Hz torque/contact events.

Reproduction: run `.venv/bin/python3 tmp/direct_omni_extended_jitter_diagnosis_001/analyze.py --output <fresh-directory>` from this checkout. It refuses an existing output. A fresh independent execution reproduced report.json byte-for-byte. Input trace and exact historical sources are referenced by hash, not duplicated.

Transferable lesson: new native admission should separately log commanded/executed targets and actual angles/rates at eachphysics substep before diagnosing learning or feedback, establish quiet active-actuator behavior, and expose dynamic actuator/limiter state in any eventual new policy contract. Do not reuse the C actor or four-bar motor adapter. Root owns any new-model experiment design and dispatch. Publication must follow docs/PROJECT_SITE.md with the historical label, central registry/update record and relevant STATUS/PLAN/NEXT_RUNS context.
