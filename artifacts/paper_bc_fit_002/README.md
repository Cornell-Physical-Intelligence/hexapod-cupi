# CPU BC dataset contrast 002

Fresh, unchanged learner/config: 32 environments as checkpoint metadata only, initial standard deviation 0.1, seed 20260914, 1,000 uniform BC updates with batch 512 and Adam 3e-4. Two CPU threads; actual fit 4.374 seconds. No simulator was created. The complete fresh initial checkpoint content exactly matches fit 001. The only experimental change is the BC dataset (including normalizer statistics induced by that dataset).

Checkpoint SHA: 708ab3c5be62e34e0dea41c0452e96ac5dabd19e27ec6224be4b4414645db373. BC dataset SHA: f016af1acd6278f37ce3c3c35343554fc59fc258ee9dd39913be4a185a70606a. AMP prior stays 22f7b04b0c7540f7a27cbe4d13e23c09dbd3a2363ee0792f292e6293b16b9515. Strict reload reproduces every recorded actor output, optimizer, and RNG exactly. Update/transitions/PPO optimizer/discriminator/episode counters remain zero; BC steps are 1,000. The 20 actor-parameter Adam states retain step 1,000; critic, discriminator and log standard deviation remain freshly initialized and unchanged. Actor normalizer sees 3,760 rows; critic and AMP normalizers remain fresh.

REPORT.json contains aggregate and per-command action/increment metrics; DATA_CONTRAST.json compares both checkpoints on identical original-steady, onset and zero subsets, including named joints. Combined MSE is 7.055e-5 versus copying the previous applied action 0.00102473. Original-steady MSE increases from 1.338e-5 to 3.283e-5; screened-onset MSE decreases from 0.0009905 to 0.0001329; added zero-prefix MSE decreases from 0.0004795 to 1.218e-5. Cycle-3 MSE is 4.744e-5, a related temporal holdout rather than independent generalization. Zero-target copy baseline is exactly zero, so the network cannot beat it on those rows.

The estimator is still an unsupervised action latent: combined velocity RMSE is 0.839 m/s; no calibrated velocity claim is made. There are no walking-to-stop demonstrations. Native deterministic forward/quiet/stop probes, with original reset and unchanged gates, remain necessary. Adam inheritance for a future PPO handoff is explicit and requires review; no such handoff is adopted here.

ATTEMPT_001 preserved an executable-path mistake that bypassed the virtualenv and failed importing NumPy before learner construction. The subsequent invocation used the original virtualenv path and succeeded. No maintained source or native physics changed.
