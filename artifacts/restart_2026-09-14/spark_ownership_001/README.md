# Spark compute ownership renewed, 14 September 2026

The user confirmed the mass-corrected robot and authorized restarting standing, walking/stopping, terrain and survey. The subsequent instructions “you can stop all other processes on the spark” and “take full ownership of the spark, you have my permissions” authorized deferring competing compute. This bundle records actual operations, not a standing or training result.

Root stopped the exact `dsv41-inference.service`. Its parent PID2521668 and llama-server child PID2521678 disappeared; the GPU process query became empty. Available system memory rose from approximately11GiB to118GiB. The original unit bytes, permissions and prior active state are preserved privately on Spark; the installed unit is a persistent `/dev/null` mask. See DEEPSEEK_DEFERRAL.json.

The earlier reservation marker had been released on11September, its queue-lock service was inactive, and reconstruction's launcher was again a regular file with different bytes. These live observations do not invalidate the historical receipts. Root saved the current launcher and helper-unit originals privately, installed a fresh directory entry blocker, and updated the enabled helper service to a new source and marker. Its live process owns the exact reconstruction queue lock. No queued request, reconstruction output or DeepSeek model was deleted or modified.

RESERVATION.json binds32 inactive user masks (the prior30forecast units, Ollama and DeepSeek), four inactive system forecast masks, the new marker, helper and policy. RESERVATION_BEFORE.json retains observed prior state. The helper preserves each acquisition as a new record; it does not overwrite the historical September10/11 acquisition receipt. Previous release and pause receipts remain unchanged.

Remote originals and current controls reside at `/home/orionh/HEXAPOD_runs/restart_20260914/spark_ownership_001`. Foreign unit/launcher contents remain private; public files contain hashes and selected state only. Restore only on explicit later user release, after verifying current control identities and backup hashes, then restore the recorded prior state. No automatic release is scheduled. SSH, networking, operating-system services and host health remain available. Privileged/manual starts are not prevented by a hardware partition; each allocation still requires both compute locks and live contention checks.

`stop_deepseek.py` and `establish_reservation.py` are exact one-time operator scripts retained as evidence. They refuse existing output identities; do not rerun them. Native dispatch uses a separately reviewed successor guard bound to this policy and the current coordination hash. No native run was started by the operations in this bundle.

The exact system `slurmd.service` stop attempt was refused because the current account lacks administrator authentication. It remained idle with no child workers and an empty GPU query. BATCH_SCHEDULER_CHECK.json records this limitation; no privilege bypass was attempted. Foreign-workload detection therefore remains necessary. RESTART_BLOCK_PROBES.json separately verifies three rejected starts/re-enable attempts for installed user masks.
