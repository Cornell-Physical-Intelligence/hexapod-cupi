Review one conditional next learning experiment, not implementation. Return at most 400 words. Warm-start native results are UNKNOWN; do not assume success or failure.

Robot: confirmed mass-corrected 19-body/18-joint hexapod, admitted native Isaac standing. Actor observes five proprioceptive histories (gyro, gravity, relative q, dq), command and previous actual held target; estimated velocity and learned memory feed the policy. No phase input. Targets use unchanged 0.35-rad action scale and 0.040-rad/20-ms limiter. All existing physical, direction, quiet, stopping, motor, contact and visual gates remain mandatory; Stage2 is unqualified.

BC dataset002 has 3,760 authentic native samples: accepted steady motion, onset after four seconds neutral settling, and actual zero-command reset/settling. Exact forward +0.05 m/s has 120 onset rows (raw controls200–259) and 120 steady rows across two replicas. BC uses normalized action MSE plus coefficient1 raw velocity MSE, detaching estimated velocity only at the BC policy input. Fit003/004 model/normalizer tensors are identical, zero PPO updates. The expert is a recorded trajectory; there is currently no validated feedback teacher for arbitrary off-trajectory states.

Independent frozen-normalizer CPU diagnosis of cold evaluation012: initial observation is close to forward onset (normalized RMS .217) and within all dataset coordinate ranges. Root height differs7.04mm, which is not directly observed. First teacher onset request is underfit even on its own training inputs: .0321rad RMS error versus .00173rad mean steady error; identical first labels across replicas. After the limiter, cold first held-target disagreement is only .00516rad RMS. Later900 cold controls have six joint-position channels outside the entire dataset ranges, and38.43% of held-target scalars outside their ranges. Actor target differs .1001rad RMS from nearest same-command teacher. Eight-neighbor label spread is .01056rad while actor-to-neighbor-mean error is .10086rad: local target averaging is not established. These distances are descriptive, not invented OOD gates or causal evidence.

Native checkpoint220 evaluation014: forward is effectively stationary; original quiet and post-command recovery windows pass. It did not stop from successful walking. After100 more PPO updates, checkpoint320 evaluation015 completes all three probes but fails forward tracking and both quiet/recovery six-toe support. PPO v3 rollback/frozen-normalizer repair worked: 1,288 accepted model steps, 2,000 discriminator batches, accepted mean actor KL below .02. Final discriminator policy/expert scores are -.99953/+.99924, style reward .001409; sampled raw-action clipping is38.58%, distinct from motor saturation. Stable KL and critic/reward trends are not gait evidence.

Compare (a) explicitly tighter/weighted BC on first-onset samples before a fresh PPO successor, (b) on-policy teacher data aggregation requiring new native collection and valid expert labels, (c) AMP training-balance changes. Choose the smallest next action conditional on the actual matched cold/warm outcome, and name the evidence needed before adoption. No hidden phase, scripted qualification, relaxed gates, compatibility bypasses, or claims that unobserved warm results are known. Root alone adopts experiments.
