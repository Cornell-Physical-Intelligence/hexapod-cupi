# Independent interpretation; root adoption remains pending

The actual requested Fable 5.1/max consultation succeeds in 116.05 seconds with
tools disabled. `REVIEW.md` preserves its final response verbatim. The CLI counts
407 whitespace-delimited words, seven above the requested 400-word ceiling; no
retry, truncation or replacement response is presented as the original. Actual
model/session/usage are recorded, including the CLI's reported Haiku usage
alongside the Fable review. Session metadata identifies the final model and
confirms no tool-use blocks; no hidden reasoning is copied into this artifact.

The narrow useful proposal is conditional: if matched warm and cold trials
both drift, first test a separately versioned onset-weighted BC refit without
PPO, holding saved normalization and the existing velocity objective fixed. If
warm tracks while cold drifts, establish the missing feedback-teacher/data
prerequisite before choosing an off-trajectory collection experiment. In either
case, defer changing AMP balance while the startup comparison is unresolved.
These are proposals for root review, not adopted configuration or native work.

Several statements require tighter interpretation:

- The observed 900-row interval means controls 100–999, not a transition that
  occurs near control 900. The recorded cold trajectory begins departing much
  earlier. The original CPU diagnostic preserves the per-control distances.
- Smaller onset regression error followed by persistent drift would weaken a
  claim that the first-step error alone explains failure; it would not logically
  falsify every causal role for onset underfit. The limiter, later state errors,
  and other unmeasured differences remain.
- Successful warm behavior would show sensitivity to the intervention's
  combined physical/observed context. It would not isolate root height or prove
  the particular unobserved-state hypothesis. The initial actor observations
  are close but not equal, and physical contact/pose histories can also differ.
- Discriminator scores near their least-squares targets and a low style reward
  do not measure the actor's gradient directly. PPO uses sampled rewards and
  advantages, not direct backpropagation of the discriminator score through
  native physics. AMP signal starvation is a hypothesis, not an established
  gradient diagnosis or reason to rebalance immediately.
- The proposed 38.43%,0.1001-rad and order-of-magnitude comparisons are descriptive
  baseline values, not new pass/fail rules. They should not become requirements
  for adopting a bounded experiment or substitute for the original physical
  gates. Command-specific nearest demonstration error is not a valid expert
  label for an arbitrary off-trajectory state.
- A continued PPO lineage already exists through checkpoint 320. The review's
  statement that a successor does not exist is useful only in the narrower
  sense that an AMP-adjusted successor experiment has not been defined/adopted.
- A small CPU refit can test regression behavior before any additional PPO,
  but better fitted requests do not prove improved actual held targets or
  walking. The actual first held-target mismatch is only 0.00516rad RMS after
  limiting. Preserve steady/zero performance and all failed native outcomes.

No warm result was supplied to this consultation or read in this task. Source,
physics, checkpoints, reward, acceptance gates and active native work remain
unchanged. A valid teacher for arbitrary off-trajectory states remains a real
prerequisite for data aggregation; reusing open-loop phase labels or nearest
demonstration labels would not establish that prerequisite.
