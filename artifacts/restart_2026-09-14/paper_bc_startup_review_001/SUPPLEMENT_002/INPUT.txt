Review only one bounded robotics diagnostic; respond in at most400 words. All tools are disabled. No implementation, fitting, native execution or adoption is requested. The prior longer actual Fable max request timed out after600s without a response and is preserved. This is a fresh concise consultation.

Goal: existing cold-start Stage2 walking/stopping qualification on the exact confirmed mass-corrected18-joint hexapod. Physics400Hz/control50Hz; existing actuator,0.040rad/20ms limiter, contact, torque and numerical gates remain unchanged. Deterministic BC checkpoint fit004 is unchanged. Its model/AMP bytes match fit003 actually evaluated cold; only future rollout batch configuration differs. Moving demonstration onsets start after4s actual neutral settling. Dataset includes120 screened +0.05m/s forward onset rows, but no immediate-reset moving or moving-to-stop demonstrations. Cold BC emits rhythm/bobbing but fails forward, quiet and stop. It did not become nonwalking only after PPO.

Later PPO200 developed widespread deterministic action saturation and no useful walking. A20-update repair now bounds accepted rollout KL and freezes actor statistics; its policy evaluation remains under root review. Root independently continues that lineage while this diagnostic is considered. There is no claim that warm BC works.

Proposed matched pair uses the same existing BCfit004 weights/statistics:
C: one original native reset, then1000 deterministic BC controls(20s) with command[.05,0,0].
W: one same original reset, then200 actual native controls(4s) issuing zero normalized actions and zero command, holding the existing neutral through the same actuator; then1000 BC controls at[.05,0,0]. No root/joint pose writes, reset, history replacement or extra physics at handoff. Actor observations remain five actual42-value proprio samples, command3 and actual previous-held-target18. Estimator/memory are feed-forward on that recent210-value history, not recurrent4s state. No phase or privileged policy input.

Record all1000/1200 controls and8000/9600 native substeps, contacts, video and failures. Explicit action-source labels distinguish scripted neutral from BC; record actor mean separately from issued action. Scripted neutral is not a BC quiet/stop result. Current evaluation asserts actor mean equals issued action, so a hidden zero-returning wrapper is invalid. One new diagnostic case family must preserve all existing96 required cases and13 probes. Score each20s BC portion with unchanged1000-row scorer and its100-row settling exclusion; preserve full24s prefix evidence separately, without hiding early failures. Existing cold qualification is never replaced. Use matching seed/model/config/camera/reset readback; advise whether separate fresh apps are necessary initially or same-app sequential resets with solver-cache limitation suffice. BCfit004 is schema v2/source b0a3cf; current learner is v3/source867e0859. Preserve a coherent original loader or explicit verified migration, never bypass strict identity.

Please answer: is this pair worthwhile; what positive/negative results support without claiming history-only causality; minimum comparability/action-source/evidence constraints; and one compact conditional next decision. Root decision remains pending. No broader architecture proposal or escape-update estimate.
