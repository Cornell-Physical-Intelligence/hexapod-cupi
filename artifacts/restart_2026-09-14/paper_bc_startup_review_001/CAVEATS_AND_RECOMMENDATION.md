# Independent interpretation of the completed short consultation

The initial actual Fable max consultation timed out after600.024s with empty stdout/stderr and no completed assistant response in its recorded session. It remains intact. SUPPLEMENT_002 is a fresh426-word request to the same requested Fable5.1/max model, bounded300s. It completed in152.151s; its actual response and usage are preserved. No experiment was adopted or executed by either consultation.

The response supports the targeted comparison and correctly preserves a separate nonqualification family, a single initial reset per arm, explicit issued-action versus actor-mean labels, intact1000-row policy windows, unchanged100-row scorer exclusion, coherent checkpoint loading and fresh matching app instances. Root owns whether/when to execute it after current policy evidence. Separate apps give a cleaner initial comparison than same-app sequential arms because a reset does not promise to clear native solver/contact cache. If same-app execution is chosen for operational reasons, that limitation must stay explicit.

Four claims need narrowing:

1. The previous-held-target feature does not change between the intended C and W policy handoffs. C resets the held target to neutral and that observation feature to zero; W issues zero normalized actions from that same neutral, so it keeps the held target neutral and its feature zero. The intervention does change realized q/dq/gravity, contacts, solver state and the recent proprio history. Both estimator and memory are feed-forward on five samples; there is no four-second recurrent memory. Readbacks should verify this rather than assume it.
2. A different new cold result does not itself prove the harness invalid. Verify exact checkpoint/source/config/reset/camera bindings and acquisition integrity, then report the measured discrepancy. Do not force the result to match prior failure or silently discard it.
3. Failure of one W run only weakens the specific four-second neutral-prefix explanation. It does not prove that startup state is unimportant, that the attained state matches the accepted moving demonstrations, or that a particular fit/data/conditioning defect is established. Conversely W improvement supports a contribution from the combined startup distribution, not history-only causality or cold qualification.
4. Absence of moving-to-stop demonstrations does not logically guarantee stop failure. Stop failure is already measured for this BC checkpoint, and this forward-only diagnostic cannot qualify stop/quiet behavior. The scripted zero-action prefix is not BC quiet behavior.

The phrase that the W prefix is never scored should mean it is not assigned a formal quiet-standing or BC locomotion score. Preserve and summarize all1600 prefix substeps, contacts, torque, terminal flags and failures. Neither slicing nor a later policy-segment pass may hide an early native failure. The policy tail retains its own8000-substep motor/contact evidence; combining benign prefix samples must not dilute a failing tail fraction. New diagnostic result fields should keep acquisition completeness, policy-only numeric score, complete-run physical evidence and Stage2=false distinct.

Do not add a post-hoc hard 'inside the120-row distribution' acceptance gate. Native handoff feature/contact comparisons are descriptive support, with any distance definition stated explicitly. The proposed second seed is advisory only; if reset/control are otherwise deterministic and contain no randomization, changing a seed need not yield an independent physical condition.

Minimal conditional recommendation, still not adopted: if current PPO evidence motivates revisiting BC startup, collect matched same-checkpoint C20s and W4s scripted neutral+20s BC using one original reset per arm, preserving all2200 controls/17600 substeps and action-source semantics. Compare the exact policy-only1000-row windows and separately expose the first100 BC controls. A useful positive W result merits a reproducibility check and a discussion of actual cold-onset coverage, without prepending a hidden startup controller to qualification or deployment. A similarly poor W result is a reason not to spend a larger branch based solely on this warm-up hypothesis. It identifies no unique training fix by itself.

BCfit004 remains schema v2/source b0a3cf. Current source018 learner is v3/source867e0859. Any diagnostic source bundle must preserve a coherent original loader or use a separately verified explicit migration; no source/config/schema check is bypassed. No new fit, source edit, native run or physical gate change was performed in this review.
