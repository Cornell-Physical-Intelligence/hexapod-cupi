{
  "tests": 7,
  "failures": 2,
  "errors": 1,
  "scope": "Initial CPU fixture/config integration run; no native action.",
  "failures_preserved": [
    "Synthetic capture command used float64 requested vector while actual native capture uses float32 command; fixture corrected to read actual synthetic env command.",
    "Inherited CLI default command was.04; startup now explicitly defaults to fixed.05 while ordinary modes preserve.04, and explicit incompatible startup commands are rejected."
  ],
  "numeric_gates_changed": false
}
