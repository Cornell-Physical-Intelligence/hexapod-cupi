# Paired BC startup diagnostic

Both attempts completed their full declared records and failed only the unchanged forward planar-tracking screen. The four-second neutral prefix brought the first BC observation very close to a native demonstration onset, but that closeness did not produce sustained forward tracking in this one paired comparison. Stage 2 remains unqualified. No further experiment was started; this evidence is sealed for the user-requested pause.

The identical source019 learner and migrated fit004 checkpoint (`dfb6d3ecc6ff5fae30c77bbac9056875eec6f19c9cb129917f97542d3b68d52b`, zero PPO updates) were used in both arms. Every model and normalizer tensor matches the original fit004 checkpoint. The two arms share the exact recorded physics binding and mass-corrected model. All 38 cold and 40 warm result files were independently verified against their complete inventories; 91 analysis input hashes were checked again after calculation.

Cold contains 1,000 BC controls and 8,000 native physics steps. Warm contains 200 explicitly labeled scripted-neutral controls (1,600 steps), followed by 1,000 BC controls (8,000 steps). The prefix issued exactly zero actions and held the configured neutral targets. It is not a BC quiet-policy test. Global and policy indices, native counters, commands, actual issued actions, and the handoff observation/critic/AMP/previous-target records match exactly. There was one initial reset, no reset during either trial, and no extra handoff step. Only the BC suffix is reconstructed as actor-issued behavior: recorded native actor means equal issued actions exactly; CPU actor reconstruction differs by at most 4.17e-7 cold and 5.36e-7 warm.

| Policy-only measurement | Cold | Neutral4 |
|---|---:|---:|
| All 1,000 controls: mean forward COM velocity (m/s) | -0.000576008 | -0.000280371 |
| First 100 controls: mean forward velocity (m/s) | -0.005335315 | -0.006033846 |
| Later 900 controls: mean forward velocity (m/s) | -0.000047196 | +0.000358904 |
| All 1,000 controls: planar RMSE (m/s) | 0.057868758 | 0.076026256 |
| First 100 controls: planar RMSE (m/s) | 0.100708666 | 0.110272826 |
| Later 900 controls: planar RMSE (m/s) | 0.050930995 | 0.071211593 |
| Original fixed-window planar error, limit 0.025 m/s | 0.050607534 (fail) | 0.059982400 (fail) |

All rows request +0.05 m/s forward. The existing gate uses the later 900 policy controls (global [100,1000) cold and [300,1200) warm); the first 100 and complete 1,000-control summaries are additional descriptive views, not replacement gates. The original planar error and planar RMSE are different statistics and are labeled separately above.

At the first BC control, both arms' nearest same-command onset is dataset002 row1884: original native control200, replica30. Saved actor normalization gives RMS feature distance 0.217287 cold and 0.009918 warm. Current-joint-position RMS difference from that teacher row is 22.180 mrad cold and 0.0853 mrad warm. Root heights are 0.102802306 m and 0.095763326 m, respectively, against the teacher's 0.095761582 m. Height is not an explicit actor feature, and this intervention changes several physical/observed quantities together.

Despite the close warm onset, the first requested-target error against the nearest actual teacher label is 31.864 mrad warm versus 30.758 mrad cold. After the existing target limiter, the actual held-target difference is 6.671 mrad warm versus 5.161 mrad cold. These distinguish requested action from physically held target; they are not motor-torque saturation measurements. Nearest teacher actions are observed labels at their recorded states, not validated expert labels for a policy-generated state.

The warm policy subsequently leaves the demonstrated feature range as well. Mean nearest-onset distances for the first100/later900 controls are 1.95224/1.92155 warm versus 2.12786/1.77451 cold. Later900 joint-history scalar fractions outside the complete dataset's coordinate ranges are 36.6605% warm and 33.3333% cold; held-target fractions are 37.2346% and 38.4259%. Per-group q/dq/gyro/gravity/held-target/command distances, all 1,000 rows, both onset and steady pools, and nearest raw row identifiers are in `RESULT.json` and `nearest_rows.npz`. Empirical distances/ranges are descriptive evidence, not acceptance thresholds or proof of a causal mechanism.

The independent `native_audit/RESULT_004.json` reconstructs all 17,600 native steps and reclassifies 394,457 actual contact patches. Native servo demand, cap, applied input, control target, continuity and contact-category checks reproduce exactly, as do all original gate checks and verdicts. Both arms have zero joint-bound, speed-bound, nonfoot-contact, termination and reset events under the recorded checks. The neutral prefix has no requested saturation and 12 initial zero-toe-support steps. Across the separate BC suffixes, requested saturation fractions are 0.23403% cold and 0.47847% warm. Missing six-toe samples in the fixed 7,200-step scoring windows are 100 cold and 1,063 warm; this forward profile does not turn that descriptive support count into a quiet gate. Native versus local descriptive float32 percentile rounding is explicitly reconstructed in the audit and does not affect any gate.

The maintained generic analyzer's rejection of the new Unicode `action_source` channel is preserved at `../analysis_startup_cold_001/FAILED_GENERIC_ANALYZER_001.json`. Artifact-only typed loaders accept exactly `bc`/`scripted_neutral` labels and retain numeric finiteness, lengths, hashes and continuity checks. Maintained code and raw evidence were not changed. Three independent raw-audit loader/percentile assumptions and their failures are also retained alongside its successful final result.

Three plots per arm in `cold/` and `neutral4/` use the unchanged maintained numerical plotting functions. The warm prefix is explicitly titled and shaded blue on time axes; spectra and torque bars use the complete record, including that prefix. Policy-only numbers remain separate above. All six generated images were visually inspected for readable labels and correct interval marking; they show measured timelines/spectra, not substituted robot imagery. Native RGB review belongs to the root's separate visual receipt.

This pair supports the narrower conclusion that improving the recorded initial-context match alone was insufficient for this checkpoint. It does not isolate root-height causality, demonstrate local target averaging, or establish that a new fit will solve walking. The earlier conditional Fable review and the independent onset-error concentration audit remain relevant evidence for a later root decision; no learning change is adopted or executed by this report.
