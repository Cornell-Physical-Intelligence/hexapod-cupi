# Reviewed candidate for one and 128 standing replicas

This candidate binds the unchanged successful `diagnostic_one_002` and the new
`diagnostic_batch128_001`. Root adoption is a separate operation. The two draft
files contain the exact proposed host/container admission bytes; no admission002
was created by this preparation. Admission001 and the earlier pending candidate001
remain unchanged. This standing result does not qualify walking or complete Stage2.

The remote CPU audit recomputed the frozen source016 standing scorer for every
replica over all 1000 controls and 8000 physics steps. Its report equals the native
report exactly. An additional streaming audit independently reclassified 24,479,073
contact patches from all 8000 packets and checked every recorded root pair and the
straight relative-root segments between samples. All128 passed the original gates.
The minimum sphere gap was 1.0581628607200673m and the minimum floor-envelope margin
was 17.529077615654735m. Worst quiet joint RMS was 0.012585166841745377rad/s against
the unchanged0.03 limit; maximum applied torque was 1.098325490951538Nm.

`prepare.py` verifies the audit digest, auditor digest, exact report, successful
native lifecycle and all14 compact files against the audit's remote input hashes.
It verifies unchanged physics files and reused one002 bytes. The actual maintained
`require_admission` accepts the proposed local-path copy for one and128 replicas
and rejects a32-replica mismatch. `verification.json` records those results and
`ROOT_HANDOFF.json` gives the final path/binding changes. The script completed under
Python3.12 with `-B -S`; no Isaac, Torch, GPU or remote access was involved.

The local `results_diagnostic_batch128_001` collection is partial: it contains only
the14 compact metadata/log files. The complete11.5GB raw recording remains at
`/home/orionh/HEXAPOD_runs/restart_20260914/paper_walk_001/diagnostic_batch128_001`.
The copied audit receipt records every remote raw input digest. This compact review
does not claim that the raw arrays were downloaded or reprocessed on the Mac.

The audit uses the recorded mesh-clearance channels; it does not independently
re-evaluate every mesh vertex. Sampled roots and linear sweeps do not prove
continuous collision detection or solver independence. Native USD/SDK attributes
are verified, but backend iteration counts are not exposed. No contact insulation
or passing-subset fallback was introduced.
