# Completed continuation008

100 additional PPO updates (221–320),307,200 native transitions,1,288 accepted model steps and2,000 discriminator steps completed. All accepted rollout KL values stayed at or below0.02; actor statistics are bitwise unchanged. No update had zero accepted model steps.

Mixed-command signed movement averaged0.000678m/s, with74 joint-limit terminations and no height/tilt terminations. Last20updates averaged0.001305m/s and38.54% raw action clipping. These measurements do not demonstrate walking. Evaluate015 uses the same three probes and actual forward video.

RESULT.json binds raw metrics and native log inputs and uses the immutable analysis_train_007 aggregate/guard routines for the new100-update window. Command-weighted means, complete accepted/rejected proposal logs and separate first/last20windows are retained. CHECKPOINT_CHECK.json verifies source/config identity and frozen statistics independently. Fresh simulation resets prevent claiming exact physics continuation.
