# Root-owned integration proposal

Publish the frozen standing005 preparation under a new immutable native standing preparation/evidence path. Preserve source004 and its authentic native5 rejection. This package currently records CPU preparation only, not native execution, a pass, training admission or a hardware-qualified actuator.

Required central framework updates on adoption, per `docs/PROJECT_SITE.md`:

- `STATUS.md`: source004 single-robot 32/4 rejected on the unchanged SDK quiet bound; source005 32/0 is only prepared until an actual dispatch/result exists.
- `docs/PLAN.md`: preserve canonical model/servo/geometry/gates and distinguish reported SDK velocity from interval angle rate; qualify same-source standing1 before32.
- `docs/NEXT_RUNS.md`: the next selected comparison changes only velocity iterations4→0 and records legacy friction, link COM velocities and the ground-filter aggregate. Nonzero discovered friction is not automatically cleared. No unconditional sweep or PPO allocation.
- `site/project.json`: point to the exact new immutable preparation and authentic predecessor rejection, keeping Stage2 in progress and no false32 admission.
- Add a new `site/updates/<UTC>_standing32_0_preparation.json` covering the bounded artifact and these exact living-document changes; evidence paths must exist when the record is integrated. Run project-site change checks/build and applicable pipeline-lineage checks.

Root chooses the final public artifact path and record timestamp. This proposal is not a pre-created update record with guessed paths or execution status. No living documents or tracked runtime files were changed by this preparation.
