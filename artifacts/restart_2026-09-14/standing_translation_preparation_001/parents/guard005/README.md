# Standing32 retry005 after an unrelated CUDA interruption

This fresh guard preserves standing source005 (`c87f2d340c935cd947c7aed3f3dcd7c9a9965b6e19d6726546abb560b27ab131`), host007 (`7085776db928bd600ee3e8aff48af433eee1f35e758ce6221b34e4f2cfe50577`) and the genuine native6 one-robot admission. It allocates only32 replicas for1000 controls/8000 substeps under the unchanged original physical and quiet gates,32/0 solver recipe, geometry, servo and full raw evidence. No PPO or Stage2 admission follows automatically.

The immediate prior native_standing32_004 invocation `b33d418438ea438ca0805eea68e70a43` was interrupted by the original supervisor after an unrelated reconstruction CUDA worker appeared. The original job records that exact competitor and cleanup; the native state remains running/explicit_steps_completed0/checks{} because the finalizer did not execute. Progress reported900 controls separately. Neither zero acquired physics nor a completed physical verdict is inferred from the unfinalized state. All34 raw hashes (4,666,042,464 original bytes) are checked remotely in bounded chunks; only six small original cleanup/identity files are copied here. The earlier startup003 failure and frozen guard004 remain preserved separately and in historical inputs.

Fresh names: output `native_standing32_005`, owner `hexapod-canonical-native-standing32-005-20260910.service`, pause018, fallback `hexapod-canonical-forecast-restore-018`. Root alone dispatches. Host phase1200s/AppReady90s and guard1320s/180s stop/25-minute fallback remain unchanged. Source009 exact ownership, both GPU locks, unknown competitor checks and the complete per-job restorer are preserved. The guard never signals unrelated processes or releases the persistent reservation.

Root's stronger operational reservation is bound by `inputs/reservation_policy.json`. It rechecks the unchanged13 original reservation files,31 exact user /dev/null unit masks and loaded inactive state, four existing system forecast masks, archived unit definitions, blocked reconstruction entry directory, exact __main__.py/README/recovery bytes and exact lock-holder helper/service/receipts. Service MainPID must be0 when masked; timers may omit that property. Eleven actual masked units retain historical Condition drop-ins and report NeedDaemonReload=yes after reload; this flag is retained rather than misreported, while exact file/loaded masks and inactive processes remain mandatory. The queue-lock service must be enabled and active with the exact helper command, matching acquired-record PID and actual /proc/locks inode ownership. Its record binds the full ACTIVE file path; per-job restoration continues to record the reservation directory.

`tests_candidate_002.log` records40 passing CPU tests; `tests_final.log` additionally binds the actual installed receipt. The earlier39-test file-gate candidate remains preserved. They include true native6 and interrupted004 compact fixtures, explicit external raw-hash substitutes for files intentionally not copied, initial-state/failure preservation, wrong owner/unknown cleanup rejection, masks/queue source/policy/backup/lock failures, literal NVIDIA CLI and protected main/restorer parity. `DELTA_REVIEW.json` limits changed function ASTs to final bindings, actual prior interruption, reservation validation and the fallback name in main. No full32 raw physics replay or GPU execution is claimed. The first actual reservation probe correctly refused an external uploader replacing the original file gate; exact policy6e808d89, runtime and stderr are preserved under inputs/first_file_gate_attempt. Root subsequently installed a non-symlink directory at queue-worker.py with an unconditional no-GPU __main__.py, preserving both copies of the uploader-restored source. The new policy rechecks that directory and exact files, while preserving the original queue lock. This blocks the observed ordinary-file upload/Python-entry pattern; no arbitrary privileged isolation claim is made. Root's second actual installed -B -S reservation-only probe passed: `inputs/reservation_probe002.json` binds current runtime e80490c0 and policy dcb62320, all31 masks and exact lock owner3814169, with native_calls0/output_createdfalse/pause_createdfalse. The first rejected probe remains untouched. `inspect_reservation.py` provides the same bounded read-only check for later use.

The copied earlier CPU admission identity and earlier preparation logs are historical same-source native6/host007 evidence, not a new actual32 pass. `inputs/PRE_REVIEW_reservation_function.txt` retains the pre-review draft; the executable fixed function is in launch_guarded_remote.py. Source/host frozen bytes are external bound inputs and are unchanged.

Eventual publication follows docs/PROJECT_SITE.md through root's central STATUS/registry/update record. This preparation changes no shared documentation, Git state or remote service. Native32 result and PPO readiness remain pending genuine acquisition and original-gate acceptance.
