# Standing004: controlled solver diagnostic

This source tests one numerical hypothesis after source003's actual32-robot acquisition:21 replicas missed at least one post-settle six-toe support substep, seven also exceeded the SDK joint-rate quiet bound, and11 passed. The separate host600-second timeout does not explain or erase those physical/quiet failures. No PPO admission exists.

Before any solver mutation, this candidate reads every articulation's installed USD fallback and requires un-authored32position/1velocity iterations. The NVIDIA schema documentation declares those defaults, but installed behavior has not yet been observed. An unexpected value fails with the complete before-readback in the exception. Only then does it explicitly author32position/4velocity iterations and verify the exact attributes after authoring, after reset and after all8000 controlled steps. This preserves the position count and tests additional velocity convergence. These are USD attribute readbacks, not independent introspection of the solver backend. No repair is assumed.

The detailed asset, all153 SDF shapes, masses/inertias,400Hz servo,1.6N·m cap, provisional48V curve, neutral targets, raw contact recording, session/reset/counter logic and every physical/quiet threshold are unchanged. The frozen native acquisition/source identity now includes the exact solver recipe, and completed-result validation requires its sealed per-root before/after evidence. The original numeric rejection tests remain intact. Twenty-one CPU tests pass. The initial synthetic accepted-result fixture lacked the new required solver evidence; that failure and its narrow fixture correction are preserved. Intermittent local NumPy warning flags remain visible and do not establish native failure.

Run one robot, then the same source with32 only after authentic one-robot admission. The actual32-runtime measurement motivates a separately versioned1200-second host budget with90-second AppReady, exact ownership and bounded cleanup; this changes no quality gate. Prior standing results and failures retain their own immutable source/host identities.

`SOLVER_CHANGE.json` and `solver_delta.patch` describe the bounded differences. The original87-payload parent map and README are retained. Root is the sole Spark dispatcher; persistent reservation controls must survive cleanup. Read the central project-site contract before publishing any change. This bundle is preparation until native receipts exist.
