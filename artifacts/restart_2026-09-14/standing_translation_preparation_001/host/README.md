# Single-robot placement diagnostic host preparation

Successor of host007, restricted to one explicitly named diagnostic placement.
The original command, source/asset admission checks and ownership executor remain
available. The new external binding pins this exact host/source, current compute
coordination, selected placement and fresh output. Disabled templates cannot
allocate. Root supplies the reviewed external binding; never edit a frozen tree.

The preserved callable interfaces are verify_inputs(args), command(args,name,
"standing"), and CONTRACT.validate_result(output/"standing",identity). A dedicated
guard may own the container lifecycle and invoke these interfaces without nesting
the old supervisor's GPU locks. Root must use one owner, not both paths.

Native standing quality failure remains failure. A completed8000-step acquisition
with rejected gates retains all raw bytes and can supply event analysis for this
non-admitting pilot. It does not invalidate the independently prepared second arm.
No32, PPO, checkpoint, automatic continuation or broader standing admission exists.
