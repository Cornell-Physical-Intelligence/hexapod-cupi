# Scripted showcase draft — publication checkpoint

The user requested publication of all hexapod changes on 8 September 2026. This preserves the unfinished viewer/showcase work on `codex/isaaclab-training-artifacts`, with its matching packer and shared viewer dependencies. It has not passed the requested independent visual review and is not a final product film or a learned-policy result. The dated [showcase handoff](../../HANDOFF_SHOWCASE.md) records the earlier unfinished work and review history; its references to uncommitted files describe the September 4 state.

The source adds studio lighting/material controls, camera field-of-view controls, a continuous 92-second showcase and deterministic frame/contact-sheet export. The tripod motion is scripted through the viewer's gait function. Before publication, captions claiming training in Isaac Lab were corrected to identify kinematic/scripted motion, and an unverified part count was removed. No animation was re-rendered, visually approved or represented as physical simulation evidence during this publication step. Existing local movie takes retain their historical captions and are not the current corrected source or a training video.

`original_worktree.patch` preserves the exact pre-publication changes to `hexapod_core.js` and `showcase_template.html` against commit `d85698c`. The original `HANDOFF_SHOWCASE.md` bytes are preserved as the top-level file. `publication.json` records the original and corrected source hashes and the four caption replacements. Apply the archival patch only to its recorded base in a separate checkout to reconstruct the original draft; it is not a patch for the physical training branch.

The [physical-project status](https://github.com/Cornell-Physical-Intelligence/hexapod-cupi/blob/codex/mkii-fourbar-training/STATUS.md) remains authoritative: the corrected physical-model PPO has not completed, and simulation/training are paused pending the user's revised single-leg URDF and meshes. Publishing this viewer draft does not resume GPU work.
