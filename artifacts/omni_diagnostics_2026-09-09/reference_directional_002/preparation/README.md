# Directional002 immutable preparation

This compact bundle contains the frozen13-payload owner, five changed source paths and complete930-file map. It does not duplicate the930-file directional001 parent. Copy that exact parent, overwrite the paths in RECONSTRUCTION.json, replace campaign_source_hashes.json and verify all930hashes with no extra or missing files. Parent and reverse001 evidence remain immutable.

Only left strafe, left turn and a forward-right arc are declared. Fresh32standing/quiet is required; reverse001 is already measured and remains rejected on its original5mm50Hz consistency gate. The successor fixes strict JSON scalar/failure finalization without changing wave005, physical control, motion, contact, torque or quiet thresholds. The owner README documents16CPU regressions, source identity, commands and bounded dispatch.

Keep this bundle immutable. Append independent review, bound guard, actual results and cleanup/restoration to a separate terminal wrapper. No GPU run, PPO/full-omni admission or velocity-fidelity fix is claimed here.
