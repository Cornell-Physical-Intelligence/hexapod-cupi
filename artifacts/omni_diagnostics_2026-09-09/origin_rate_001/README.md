# Matched-origin diagnostic001: setup check failed before measured motion

Fresh standing passed all 32 physical and unchanged quiet checks. The first matched-origin case then stopped before control 0 because its scene-origin Python object-identity check failed. No translation comparison or rate-discrepancy trajectory was acquired. This is an instrumentation/setup failure, not evidence that an origin or controller passed or failed walking.

[The complete initial readback](evidence/run/origin_a/initial_readback.json) preserves exact matches for all joint positions and velocities, root pose and COM velocity, position/velocity targets, commands, named ordering, origin values, and controller state. The sole failed check was `scene_origin_alias`; the separate [ground readback](evidence/run/origin_a/ground_readback.json) confirms one enabled infinite collision plane with world normal +Z at Z=0. Both recorded reset injections occurred before the first physics step.

Python object identity cannot establish shared storage for tensor views. The pinned environment also creates its terrain object separately from the scene registry, so matching numeric values at the origin do not prove the metadata would remain synchronized at the translated cases. A separately versioned correction must inspect the installed view/storage behavior, synchronize both origin stores during the cold reset, and verify their values and storage semantics. The failed check is not simply removed, and original001 bytes remain unchanged.

All 29 raw result/restoration payloads, 933 source files and 550 admitted assets matched the independent remote audit. Both exact owned containers are absent and pause037 restored previously active forecasting timers. The fresh standing control trace, 400 Hz substeps and control integrals are byte-identical source009's standing evidence; its existing independent quiet and rate audits apply to those exact arrays. No new origin trajectory exists.

The immutable 40-payload preparation includes exact parent-plus-overlay reconstruction, the 24-file owner freeze, independent 15-test review, all seven new tool/data files, source metadata and full source map. Root independently passed the 15 tests, exact source preflight and eight outer-restorer tests before dispatch. BUNDLE_SHA256.json covers this terminal publication, while nested preparation/review freezes retain their original identities. No wave, PPO, terrain, hardware or velocity-fidelity admission is implied.
