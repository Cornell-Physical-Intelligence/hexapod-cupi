# Independent matched-origin review

The declared diagnostic is ready for a bounded runtime check. The reviewer independently passed all 15 CPU tests and read the cold-reset setters, installed SDK origins/target APIs, per-case loop, immutable input checks and host cleanup.

Only the audited initial XY translation changes between five cold single-environment cases. Exact generalized state, articulation target buffers, six-axis root velocity convention, terrain-origin alias and an infinite horizontal collision plane are read back before control zero. No body-pose write occurs during stepping. Original C2 startup, all 400 Hz samples, physical gates, raw SDK rates and interval-position evidence remain.

Fresh 32-replica standing requires the unchanged physical and quiet gates. A later origin case may complete acquisition while failing its quiet test: both verdicts remain separate, and such a result grants no walking/PPO admission or velocity-fidelity qualification. Each case still requires its unchanged physical gate and exact initial/ground readbacks.

The host uses five separate cold containers, both job locks and the previously reviewed exact-identity cleanup. Unknown Docker inspection fails closed. Source and read-only admitted assets are checked between cases and at terminal exit. Actual SDK/ground behavior remains a runtime check, not a CPU-test claim.
