# Matched-origin rate-fidelity diagnostic001

Prepared for root-owned bounded dispatch after recorder pause036 restoration. No GPU run or new physics admission is claimed here. This is a separate diagnostic following source009's declared slow reference-screen pass and material400Hz velocity/position disagreement.

The exact source is `source_origin_001`, copied from immutable source009 manifest `04942c62de1f557d8f117f648f990b6dac69443d61c546529d05a4e2a7f0e01e`. Seven new tool/data files and `source_origin.json` metadata are the only changes. All original009 environment, controller, waveform, observer, metrics, runtime16 and550admitted asset bytes remain unchanged. `SOURCE_BUILD.json` and the source manifest define the exact reconstruction; the earlier `derive_entrypoint.py` is development history, not the final reconstruction command.

A new32×1000 standing run must pass the original physical and all32 quiet gates under this exact new source. Its receipt cannot be replaced by009 or a synthetic CPU receipt. Then five cold single-replica1000-control cases run in fixed order: origin_a(0,0), near(3,−5), far(30,−50), near_opposite(−3,5), origin_repeat(0,0), in metres. Every case retains2s bounded canonical-target startup plus2s settling and16s scored standing, with the original16/1TGS, external-forces-every-iteration enabled, stabilization disabled, .0025×8 timing, PD/RS05 parameters and full C asset.

`origin_initial_state.json` selects009env6 by the predeclared largest absolute settled joint angle/rate-integral disagreement, with replica/name tie-break. It contains exact audited initial joint positions, zero joint velocities, uprightXYZW root pose, zero COM/world angular velocities, and the actual randomized position target. The original32replica grid origin is removed; each case adds only its declaredXY translation. This is an explicit common cold-reset state, not a claim that the initial state arose from each case's ordinary random draw. The adapter installs it inside `_reset_idx` while the physics counter is zero, including controller/previous-target/actual actuator target buffers. After the first physics substep, it never injects another pose. The inherited terminal reset is retained and any terminal event rejects acquisition before continuation. No native warm-start/contact cache is copied.

Every case must read back the exact named generalized state, target, zero commanded/reference/residual velocities, shared terrain/scene origins and counter0 before control1. All19derived body positions/quaternions are saved; their float32 world-coordinate differences are evidence, not asserted bit-exact invariants. The inspected installed scene property returns the terrain's origin tensor, and both object identity and values are checked at runtime. The ground readback requires a single enabled collision `Plane`, world normal+Z and height0, with no other external collision body. An infiniteZ0 plane is invariant under the declaredXY translations; the visible grid mesh is not support geometry. Unknown geometry or reset readback fails before control0. This remains a first-run runtime check until actually observed.

The frozen original400Hz observer records every real update, with initial sample, exact counters/timestamps and control-endpoint q/qdot parity. Each case saves all50Hz contact/torque/pose/command/target rows, actual400Hz q/qdot, link/COM pose/velocities and requested/applied torque, original physical/quiet verdicts, source-bound reset/ground/solver readbacks and a new separate rate-discrepancy report. Offline float64 coordinate subtraction does not recover precision lost by float32 simulation. Quaternion increments explicitly use SDKXYZW and world angular velocity. The diagnostic does not replace SDK rates in observations or scoring and introduces no new rate-fidelity pass threshold.

Acquisition completion is distinct from admission. A case must complete1000 finite controls with the unchanged physical standing gate, contact-log completeness and no reset. A quiet miss is preserved as a failed original quiet verdict and does not hide the remaining matched cases; no quiet miss becomes a pass. The final campaign records every case and origin-repeat differences, and reports whether all existing bounds were met. It never grants wave/PPO, hardware, terrain, production-default or velocity-fidelity admission. Comparisons are within these five common-state cold runs. A contrast to the earlier32replica009 experiment is not an isolated translation comparison.

Root launch after exact remote-source verification and prior owned cleanup/restoration:

```sh
python3 /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_origin_source_001/tools/launch_origin_physics_spark.py \
  --source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_origin_source_001 \
  --output /home/orionh/HEXAPOD_runs/mock_length_study_20260909/origin_rate_001
```

The outer guard is root-owned: unit `hexapod-origin-rate-001-20260910.service`, reserved pause037, RuntimeMax3900s plus180s stop allowance and75minute restoration fallback. Each of six possible containers has the unchanged600s deadline,90sAppReady requirement and45s traceback. Both GPU locks, coordination identity, unrelated-workload checks, read-only source/study mounts, contact-overflow rejection, exact Docker identity cleanup and terminal source/asset verification are preserved. The host contains no automatic retry, training or wave dispatch. Inherited run-owned error messages may say “standing”; the report's exact phase identifies the case.

Expected output: `campaign.json`, `standing/admission.json`, five case directories (`state.json`, `initial_readback.json`, `ground_readback.json`, `solver_comparison.json`, `startup_reference.json`, `trace.npz`, `physics_substeps.npz`, `physics_control_integrals.npz`, `physics_substep_review.json`, `rate_discrepancy.json`), per-phase job/contact audits/logs, `matched_origin_comparison.json`, and terminal source/asset maps. Failure keeps partial pre-reset and substep evidence; ownership uncertainty is not reported as absence. Forecast restoration evidence is produced by the root's outer guard.

CPU verification:15 focused tests pass, including real009 angle/rate replay, constant SDKXYZW world yaw, exact initial-state variation, target synchronization, reset/terminal behavior, ground rejection, source/admission failures, host modes and unchanged cleanup AST. Exact933-file source preflight verifies pinned16runtime, rejects actual009 admission and rejects failed-quiet synthetic admission. Synthetic receipts exercised CPU logic only and were not retained as physical admissions.

```sh
PYTHONDONTWRITEBYTECODE=1 uv run python -B -m unittest discover -s tmp/reference_origin_adapter_001 -p 'test_origin_adapter.py' -v
```

Installed SDK readbacks are retained separately in `sdk_*_api.json` with source hashes: root pose expectsXYZW; root velocity writer expects COM linear plus angular world velocity; indexed target setters populate buffers; scene origins return terrain origins; ground spawning binds material to the collision Plane and scales only the visible grid. No undocumented native API or cache path is assumed.
