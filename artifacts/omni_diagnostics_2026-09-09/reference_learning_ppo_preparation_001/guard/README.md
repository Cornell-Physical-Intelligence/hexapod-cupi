# Learning admission guard preparation

Prepared for root to bind and dispatch only `--phase-group admission`: fresh standing, calibration and `learning_recovery_32`, then exit with immutable admission evidence. This guard cannot allocate a ten-update continuation. The source remains reference009; the new consumer is source003.

- Owner: `hexapod-learning-ppo-admission-001-20260910.service`
- Output: `BASE/reference_learning_ppo_001`
- Host: `BASE/reference_learning_ppo_host_001/launch_learning_ppo_spark.py`
- Consumer: `BASE/reference_moving_ppo_source_003`
- Pause: `BASE/forecast_pause_051`
- Outer bound: 1,920 seconds, 180-second stop; 40-minute independent restoration fallback.

All 16 CPU tests pass: eight original embedded-restorer tests and eight admission/identity/immutability tests. The embedded restorer is byte-identical to the frozen pair-motion guard. Its active-timer restoration, owned-ID cleanup, uncertain-Docker failure behavior, and ambiguous-launch cleanup are preserved. Both GPU locks, live workload/coordination preflight and authorized forecasting-only pause remain in place.

Read-only Spark verification matched the prior pair-motion campaign, two job receipts and pause050 restoration. The exact previous invocation is `aeb33fdfce794cfca50e4227ebfada1d`; its unit is terminal failed and both immutable container IDs and names are absent. These are historical observations. The guard repeats those checks at execution; no current or future lock ownership is assumed. Exact small prior receipts are included under `previous_owner/` for portable tests.

## Final binding required

`HOST_SHA256`, `HOST_FREEZE_SHA256` and `CONSUMER_FREEZE_SHA256` deliberately remain explicit placeholders. Execution rejects them before creating the pause directory or making any subprocess call. Root must bind the final immutable host and consumer, verify the constant-only change, freeze this guard and perform its normal remote preflight. The reviewed draft hashes are recorded in `CPU_READINESS.json`; this preparation is not a launch receipt.

```sh
PYTHONDONTWRITEBYTECODE=1 uv run python -B -m unittest discover -s tmp/reference_learning_ppo_guard_001 -p 'test_*.py'
```

No GPU job was launched or paused during preparation. No main or tracked documentation was edited.

## Final root binding

Root substituted the exact frozen consumer003 and host identities after independent review. The earlier CPU_READINESS.json is retained as the preparation snapshot with placeholders. The final host and guard suites pass 28 and 16 tests respectively, and the separate root final-binding receipt proves only the three expected guard constants changed. The committed guard now contains concrete hashes. Actual remote preflight remains required before dispatch.
