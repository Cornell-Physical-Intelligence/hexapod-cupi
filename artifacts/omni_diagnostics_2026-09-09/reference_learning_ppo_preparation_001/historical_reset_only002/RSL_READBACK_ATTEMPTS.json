{
  "first_attempt": {
    "receipt": "RSL_STORAGE_SPARK_READBACK.json",
    "receipt_empty": true,
    "failure": "The post-removal absence check used case-sensitive Docker error matching. Docker reported lowercase no such object, so the check raised after removal and before emitting its report.",
    "followup": "A read-only docker ps -a label-filter listing returned no matching readback containers. The first exact container ID was not retained; no exact-ID audit is claimed for that attempt."
  },
  "second_attempt": {
    "receipt": "RSL_STORAGE_SPARK_READBACK_002.json",
    "readback_passed": true,
    "exact_container_absent": true,
    "started": false,
    "GPU_requested": false
  }
}
