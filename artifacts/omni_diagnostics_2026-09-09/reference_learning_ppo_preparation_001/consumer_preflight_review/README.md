# Frozen consumer003 preflight

The exact 51-file consumer freeze passed the standard-library-only input
preflight under `python3 -S`, with the complete late AppLauncher arguments,
including `--device cuda:0`, `--headless`, `--info` and `--kit_args`.
The result verifies the bound source009, observation005, device bridge/proof
and 550-file robot package. It does not construct Isaac, admit standing,
recover a physical robot or allocate PPO updates. The null standing hash in
`preflight.json` is intentional; the actual campaign must perform fresh standing.

The consumer's final aggregate contains 30 passing CPU tests. The independent
reviewer separately passed five learning-ledger and two inference-reset tests
and found no concrete blocker for the bounded infrastructure screen. The
independent byte-bound review is preserved separately by its owner.

Root owns dispatch. Admission is standing, calibration, then 200 startup plus
512 zero-residual recovery controls. It exits before optimization. Ten-update
allocation requires the shared `validate_learning_decision` helper to verify
an external root review of the exact three phase states and compact ledger.
All formal motor, quiet and evaluation gates remain unchanged.

`verification.json` records the exact identities and host API. Both predecessor
consumer inventories remain exact. No main, frozen-source or GPU mutation was
performed for this receipt.
