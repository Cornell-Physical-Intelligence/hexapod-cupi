# Prepare exact CAPS500 review and recording

The frozen recording adapter and host accept only a completed six-phase source004/native005/host005 CAPS500 campaign, with its explicitly selected actual final checkpoint and complete input identities. Thirty adapter tests and eleven host tests pass. The full-robot38-second sequence retains1900 controls/950 frames, real command arrows and trace, strict loaded-weight checks, a pre-close output seal and true post-exit source readback. Rendering and shutdown files preserve the previously repaired implementation. No future checkpoint hash, video or quality result is invented; the separately guarded recording still needs actual terminal admission and root selection.

The [CPU analysis preparation](cpu_analysis/README.md) uses the existing verified Spark Python/NumPy runtime, with GPU access disabled and one CPU thread. It binds the unchanged analyzer004, terminal campaign and external root audit, inventories every raw input before/after, and retains exact output reports and execution provenance. Its eight CPU tests pass. The actual analyzer/utility transfer is verified separately; analysis has not been executed by this preparation.

Large raw training arrays and all ordinary autosaves remain intact on Spark. The published selection must explicitly distinguish omitted required training inputs from ordinary autosaves. Keeping full evaluation traces and selected checkpoints locally does not constitute full local replay of omitted raw. An actual final inventory replaces all size estimates. Root must verify actual terminal ownership/source/restoration and analyzer evidence rather than treating a successful CPU formatter as a policy gate.

The user already authorizes terrain/perception work in parallel; no renewed blanket permission is needed. Stage2, source/terrain admission and calibrated-hardware gates still apply. Current execution is only in STATUS.md. These are CPU preparations, not a new physical experiment or Stage2 completion.
