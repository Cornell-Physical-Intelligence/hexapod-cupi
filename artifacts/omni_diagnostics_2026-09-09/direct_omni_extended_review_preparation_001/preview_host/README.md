# Guarded native005 CAPS500 progress preview

This prepared host runs one 38-second full-robot recording from an explicitly selected completed source004 CAPS500 run under training host005. It is a pin-only successor of shutdown-repaired preview host002: every function AST is unchanged. It imports source009's exact owned-container supervisor, preserving cleanup, locks, contact-overflow checks and liveness; it never substitutes source009 physics for the native direct315 source.

The new bindings are source004/native005 and preview adapter004. The adapter requires all six completed extended/caps500 phases, the selected host005 freeze, fresh matching standing, complete phase trees and exact final checkpoint. Smoke, 50-update and failed/partial campaigns are ineligible. No future checkpoint or campaign SHA is embedded. A full video remains unqualified progress evidence, not Stage2 admission.

All source, native contract, adapter, selected run, standing and checkpoint mounts are read-only. The output must be fresh and separate. The native recording writes its pre-shutdown integrity seal before process-exiting SimulationApp.close. After the inherited owned process returns successfully, this host rechecks actual original source/input bytes, verifies the seal and all output hashes, then marks recording completion. Exit zero alone or a completed native state with no seal never passes. Failures and partial frames remain preserved.

```sh
python3 launch_preview_spark.py --preflight-only \
  --source /absolute/direct_omni_train_source_004 \
  --contract /absolute/direct_omni_train_preparation_004 \
  --supervisor-source /absolute/reference_physics_source_009 \
  --adapter /absolute/direct_omni_preview_adapter_004 \
  --pilot /absolute/completed_extended_caps_retry \
  --campaign-sha256 ACTUAL_COMPLETED_CAMPAIGN_SHA256 \
  --checkpoint /absolute/completed_extended_caps_retry/train/policy/final.pt \
  --checkpoint-sha256 ACTUAL_FINAL_CHECKPOINT_SHA256 \
  --output /absolute/fresh_preview_output
```

The legacy `--pilot` argument names the selected extended run. Root supplies exact terminal hashes, performs the real preflight and wraps execution in the separately reviewed GPU/forecast guard. This preparation authorizes no dispatch. The 38-second sequence, 1900 controls, 950 frames, 25 fps, 1× playback, command labels/arrows, actual trail and fail-on-first-terminal behavior are unchanged.

The focused host tests retain RO mount/CLI checks, source009 cleanup-function identity, exact selected-campaign rejection, complete/partial-result handling, mandatory pre-close seal and actual post-exit readback. Tests use temporary synthetic files, not a simulator. Source and frozen predecessors remain unchanged.

Any future repository adoption must obey `docs/PROJECT_SITE.md`, including a bounded append-only central update and accurate actual-media provenance. Root owns tracked changes, execution and publication. No existing docs, remote files or GPU state were changed by this preparation.
