# Matched-origin diagnostic002 immutable preparation

This compact bundle contains the exact owner22-payload freeze, independent19-test review, two changed runtime files, changed source_origin metadata and complete933-file map. Reconstruct from the exact origin001 parent in RECONSTRUCTION.json, overwrite the three declared paths and replace campaign_source_hashes.json. Verify every933hash and reject extra/missing files. Do not duplicate or modify the parent source.

The successor corrects only cold-reset origin metadata: synchronize terrain and actual public scene origin XY stores, verify stable underlying Torch views/storage after reread, and preserve Z and default-pose quaternion. Python object identity was an invalid check for a public slicing view. The first failed attempt remains unchanged. Common reset payload, five cases, physical/quiet gates, solver settings, observer and acquisition-only semantics remain unchanged. The owner README gives exact bounds, invocation and output contracts.

This preparation contains CPU/source verification only. Root dispatch/guard, actual measurements, cleanup and restoration belong in a separate append-only terminal wrapper. No rate-fidelity, gait, PPO, terrain or production admission follows from preparation or diagnostic completion.
