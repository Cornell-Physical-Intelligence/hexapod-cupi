# Directional003: only the two unmeasured cases

Fresh exact-source32×1,000 physical-plus-quiet standing precedes **left_turn** `[0,0,0.015]` and **forward_right_arc** `[0.0035355339059327372,-0.0035355339059327372,-0.01]`. Each cold seed0 case retains 2,400 controls/48s: 4s startup,24s motion,20s requested-zero stopping. The campaign stops at first rejection. There is no PPO or full-omnidirectional admission.

Reverse001 remains rejected on its original5mm position/rate consistency bound. Left-strafe002 remains rejected at505controls when RR load fell below the unchanged1N support threshold during LM swing. Neither case is repeated. Their immutable review identities are recorded in the new contract; they confer no admission.

Parent source002 map is `7c64602cae91478aebd4b03adcc79f56084db7529fd1c54c0439d061075a1c58`. New930-file map is `e36fb225c1b1235c7a1389a37f491f71430dee9fefac52605e6302c8c34af650`. Exactly three paths change: the case contract/new identity, host descriptions (“two cases”) and lineage. The other927 payloads, including the complete control entrypoint, metrics, strict JSON/failure finalization, wave005, contact/torque/quiet gates, physical solver/PD/reset, geometry, pinned16 runtime and400Hz observer, are byte-identical to002. No added/removed runtime files or threshold changes.

Pure turn still requires actual signed rotation and body gyro, with no nonzero translation requirement and the same proposed10mm planar excursion bound. The arc retains signed measured motion plus independent position/heading progress. Both retain the original5mm full-interval50Hz world position/velocity consistency criterion, at least five admitted supports, six qualified measured flights/landings, original requested torque and residual/target limits, no forbidden contact/reset, and measured final quiet≥10s. Raw400Hz evidence remains a separate required diagnostic; favorable diagnostics cannot replace a rejected original gate. Known rate-fidelity bias is unresolved.

`tests_001.log` records16 passing inherited CPU tests adapted to the two declared cases, including explicit rejection of both old case names, strict complete result serialization and failure-finalization regressions. `INTEGRATION_PREFLIGHT.json` verifies930 hashes/pinned16 runtime and rejects actual002 standing admission or failed-quiet synthetic admission. A fresh standing receipt is mandatory for this new source. Synthetic tests are not physical admission.

```sh
PYTHONDONTWRITEBYTECODE=1 uv run python -B -m unittest discover -s tmp/reference_directional_adapter_003 -p 'test_*.py' -v
```

Root owns transfer, full map verification, prior exact-container cleanup/restoration, both locks, coordination, the new forecast pause and all dispatch. Suggested source/output are `reference_directional_source_003` and `reference_directional_003`:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_directional_source_003/tools/launch_directional_physics_spark.py --source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_directional_source_003 --output /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_directional_003 --isaaclab /home/orionh/IsaacLab
```

At most three phases, each bounded to600s, retain90s AppReady/45s traceback, unbuffered output, readonly source/study mounts, exact owned-container cleanup and terminal source/asset/log checks. Outer guard/restoration are root-owned. No GPU launch is claimed here. Preserve this preparation, parent002 and all rejected raw evidence; actual003 results belong in a separate wrapper.
