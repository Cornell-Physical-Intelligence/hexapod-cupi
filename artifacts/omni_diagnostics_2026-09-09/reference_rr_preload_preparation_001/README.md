This candidate was rejected by the Spark host preflight before GPU dispatch. The directional contract imported a NumPy-dependent helper merely to read its proposal metadata; the host's standard-library Python has no NumPy. [The captured failure](host_failure/failure.json) preserves the exact preflight and error and confirms that output001, its preflight receipt and pause048 were absent. The capture explicitly repeats the original read-only failure; it is not a second GPU attempt.

The [frozen preparation](preparation/README.md) remains unchanged: sixteen root CPU tests pass, including actual-input prefix, zero residual, one-shot/C2, contact interruptions and finite stops. The narrow correction arms at the measured8.40 s landing completion, changes its first8.42 s target and ends8.70 s. The earlier CPU target proposal remains separate and immutable. These CPU checks did not establish host-launch compatibility or physical feasibility.

[Root review](ROOT_REVIEW.json) verifies931 source files, the complete parent reconstruction and eight exact restorer tests. Physical loops, unchanged support/torque/quiet gates and original geometry remain preserved. The candidate is a single left-strafe diagnostic, with no old actor/schema adoption and no claim to repair the arc's RM load loss.

The successor must separate standard-library proposal metadata from the numerical controller in a new source and prove the actual host import without site packages. No host dependency installation or edit to this frozen candidate is part of that repair. `preparation/` retains its earlier CPU-only readiness wording as historical evidence; the terminal preflight failure here supersedes it for dispatch.

Run `python3 -B verify_payload.py` for portable read-only verification. The nested preparation verifier can additionally check an exact directional002 parent and reconstruct the931 source files into a fresh output directory. There is no simulation/checkpoint/quiet result from this failed preflight.
