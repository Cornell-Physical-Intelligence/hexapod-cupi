# Exact reference001 → reference002 startup delta

This compact bundle preserves three runtime files and new source identity; it does not duplicate the 923-file parent source. It prepares source002 and makes **no runtime success claim**. Root owns result collection, interpretation and publication after the bounded job.

Parent source manifest: `a13f1534f9f802871a2cacbc0be603eae2d80738905381da40691f7cd4beff7c` (923 files), published with reference001. Target source manifest: `34390c6162f462baa4c531f1d5aff346ffbc98d4d0266a68ea797f5e5b992ee0` (924 files). The only runtime changes are the new `canonical_stance_startup.py`, its use in `run_reference_physics.py`, and the explicit startup identity in `screen_contract.py`. `source_origin.json` adds lineage. All host, environment, asset, residual, wave, measurement and acceptance code remains byte-identical.

The physical reset remains randomized. A two-second C2 **joint-target** transition starts at its exact emitted target, reaches the exact named canonical target, then holds for two seconds before unchanged scoring. Neither physical pose nor joint state is overwritten. The full rationale, independent trace audit, exact patch, 24-test output, four new original regression tests and owner source preflight are preserved in `review/`. The original test module retains its development paths as evidence; it ran in the owner workspace recorded by that review. Existing 20 tests are published under the reference001 preparation.

Verify the parent and virtual merged source without copying it:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 reconstruct_source.py --parent-source /path/to/reference_source001
```

Add `--output /fresh/reference_source002` only when reconstruction is needed. The recipe verifies every parent byte and forbids extra files, validates the exact four overlay identities against the frozen target manifest, then copies into a new output. It does not launch anything. To recreate the parent first, use the already-published reference001 `preparation/reconstruct_source.py` and its documented velocity003 parent. Do not reconstruct from a dirty/current source checkout or rerun a standing admission from another source identity.

`COPY_VERIFICATION.json` records exact copied preparation hashes. `reconstruction_verification.json` records the no-copy reconstruction audit. `BUNDLE_SHA256.json` hashes this compact bundle. None of these replaces the final run's source/assets/post-run hashes, contact completeness, owned-container cleanup or forecasting restoration evidence.
