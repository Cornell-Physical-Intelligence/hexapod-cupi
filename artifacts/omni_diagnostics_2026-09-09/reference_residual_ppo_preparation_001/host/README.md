# Guarded bounded residual-PPO campaign

The exact source009 supervisor supplies fresh32×1000standing, followed only by the frozen consumer’s smoke, evaluate_initial and evaluate_final phases. Every phase has600s runtime and90sAppReady limits; the outer dispatcher must stop only exact owned containers and restore the authorized forecasting pause. This host itself does not queue a future run or authorize longer training.

All immutable inputs are bound before allocation and rechecked between/after phases. The original owned_container function is unchanged. run_owned has only two intentional edits: report policy presence accurately and call the external command builder. Every phase retains both job locks, competitor/coordination checks, raw logs and contact-completeness checks.

The consumer is19-payload freeze258c304f409373e49667bc7fb653026fcb0754c2c92d6dfd70eb04769c3e754b. Its standard-library validator checks scored outcomes and checkpoint contracts; it does not pretend to re-run raw numeric physics on the host. Root/independent raw review remains separate.

The complete smoke directory becomes immutable before retention and is mounted read-only inside each retention container. All of its hashes must still match before subsequent allocation and at campaign end. The returned phase name and exact initial/final checkpoint hash must match the allocation. Any standing, calibration, runtime, reload, quiet or initial-retention failure prevents later phases. Actual reported PPO updates remain recorded even if a later quiet check rejects the smoke.

Ten focused host tests cover readonly mounts, original standing command, exact-supervisor reuse, missing helper regression, serial short-circuit behavior, source-evidence mutation, wrong result phase and wrong retention checkpoint. These tests and CPU RSL results do not establish a physical policy pass.
