# Independent bounded residual PPO consumer review

The frozen 19-file consumer `258c304f409373e49667bc7fb653026fcb0754c2c92d6dfd70eb04769c3e754b` has no remaining concrete blocker in this focused CPU review. This is permission to assess a bounded physical attempt under its existing gates, not evidence that exploration or PPO walking works.

The review independently verified every consumer payload and compared every retained fixture array with the original source009 raw standing and wave traces. All values, timestamps and joint names match. Four owner score/admission regressions pass: the original 32-replica quiet and full forward/stop verdicts reproduce, while a noisy replica, nonfinite bound, wrong mode, source or campaign rejects.

Five independent fault-injection tests execute the actual frozen entrypoint's finalization AST. Export, logger, integrity and environment/application close failures remain failures, preserve diagnostic state where writable, and attempt remaining cleanup. These tests use mocks; they do not start Isaac, allocate a GPU, or prove simulator shutdown behavior.

The host's ten orchestration tests pass independently. A failed smoke cannot allocate either retention phase; a failed initial retention cannot allocate final retention. The complete smoke tree is checked before each subsequent allocation and again at campaign completion. Evaluations mount that tree read-only. The returned phase must match the allocated phase, and its loaded checkpoint hash must match the corresponding exact initial or final checkpoint. The copied source009 ownership/supervision functions retain only their two declared edits. Host code and test hashes are recorded in `report.json`; root owns its final freeze, preflight and outer guard.

The physical sequence remains fresh 32 × 1000 standing, two 20-second zero-mean/sampled quiet calibrations, only two stand-only PPO updates, 20-second deterministic quiet, then separate full forward/stop retention of initial and final checkpoints. Failed post-update quiet remains a failed campaign even if two updates already occurred; the host separately records that observed training history. Existing source009/bridge/observation identities and same-campaign standing/calibration bindings remain mandatory. No old actor is loaded.

The prior independent command-timing and actual RSL two-update/reload receipt remains frozen separately at `../reference_residual_ppo_independent_review_001`. Session, runner and checkpoint helper bytes are unchanged from it. The owner's final RSL rerun binds the expanded metadata-only plan; it is not represented as another independent rerun here.

Raw SDK joint rates remain distinct from valid interval angle differences. Native velocity fidelity, useful walking PPO, omnidirectional qualification and Stage 2 completion remain unproved.

To repeat the bounded checks from the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python tmp/reference_residual_physical_independent_review_001/verify_review.py
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tmp/reference_residual_physical_independent_review_001 -p test_finalization.py -v
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tmp/reference_residual_ppo_001/_deps .venv/bin/python -m unittest discover -s tmp/reference_residual_ppo_source_001 -p test_physical_contract.py -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tmp/reference_residual_ppo_launch_001 -p test_host.py -v
```
