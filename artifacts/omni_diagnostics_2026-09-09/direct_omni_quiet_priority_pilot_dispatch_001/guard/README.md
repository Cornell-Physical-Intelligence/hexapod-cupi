# Bounded quiet-priority pilot guard001

Final preparation is ready for root review and dispatch. All seven actual smoke003 terminal files are copied and hash-verified, and the exact frozen native004 stdlib validator independently accepts the four actual phase receipts, strict checkpoint reload,40 minibatches and2 sparse gradient rows. Root’s full terminal audit proves all eight names/IDs absent and pause059 restored. All34 CPU tests pass. Root alone dispatches.

One independently initialized pilot: original315 checkpoint `1971b782327408f8446b3271cc665dad8c9c515523cf8c87303141dc487d06e8`, 1024 replicas ×24 controls/update ×50 updates. Native004's exact-zero valid consecutive-pair temporal weight is1.0, moving temporal and spatial weights remain0.1, with the same all-valid denominator. Physics, actions,315/318 observations, adaptive learning rate, cold evaluations and motor/contact/quiet gates are unchanged. No checkpoint resumes from another pilot.

Bindings: source003599 map `ef962070bd770d03553eb824fb6caccada7d6a06ac26d2f180bad068f0a9cc62`; native00443 freeze `1ab678beeba6bf74978c210c4e38f13655e25728fe66e3007e3b1db5e19b6b20`; host00320 freeze `df815208f340a9c8c0c2ce8c9fcfad7fd28791f745961ab714f96c0508454efc`, runtime `d2b5298aa444f49b46611e377e63ad388dbaa11404bfb9c5b5024ed80cb6f672`. Source009 is the unchanged ownership supervisor, not substituted physical configuration.

Remote BASE is `/home/orionh/HEXAPOD_runs/mock_length_study_20260909`. The exact new output is `direct_omni_train_pilot_quiet_priority_001`, unit `hexapod-direct-omni-train-pilot-quiet-priority-001-20260910.service`, pause `forecast_pause_060`. Six phases: standing, original constant, original moving→stop, train50, final constant, final moving→stop. Each phase retains the inherited600-second bound; outer runtime3720 seconds, stop180 seconds, fallback70 minutes. No automatic continuation.

Admission requires successful same-source smoke003, invocation `0882531a12724963a64901721d87dc96`, all four phases, exact accepted diagnostic/checkpoint/optimizer receipts and unchanged inputs. The existing native `verify_smoke_campaign` validates each accepted result; host `verify_inputs` checks the full source/native/supervisor trees and original checkpoint. These execute before pausing and again before pilot dispatch. Previous-owner checks bind seven campaign/job/pause/restoration files, inspect each of four exact container names and IDs, require the previous owner terminal, and verify restoration of previously active timers. Any missing phase, failed smoke, changed input, unknown Docker result, active/conflicting owner or failed native diagnostic validation blocks admission. A physically failing quiet diagnostic remains unqualified; acquisition completion is not a new quiet or Stage2 pass.

The whole guard main function is AST-identical to the proven parent after the declared branch/pause/reason literal substitutions. Both GPU locks, competitor checks, exact owned cleanup, pre-pause validation, fallback-before-timer-stop ordering and embedded restorer bytes remain unchanged. The runtime pauses only the existing known forecast units; unrelated CUDA work still blocks. The user's broader weather override authorization remains available to root for separately identified weather work, preserving outputs. This guard does not signal unidentified services or mutate the separately archived halo workload.

Coordination is now pinned to root’s between-allocation update `35af5100e39980f55a3f5e19654e4550b3c3fb6da114b64fbe0d143817242cf3`, preserving history and recording the user’s all-weather priority instruction. `COORDINATION_BINDING.json` binds the exact remote-after bytes and root receipt; the historically different local doc was not used to derive this hash.

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover \
  -s tmp/direct_omni_train_pilot_quiet_priority_guard_001 -p 'test_*.py'
```

34 CPU tests pass in `tests_final.log`; the earlier34-test pending run remains `tests_draft_003.log`. Earlier draft001 failures were test fixture substitutions (negative branch accidentally matched the new allowed branch, and one old CLI expectation remained); draft002/003 correct those test-only expectations. `GUARD_DELTA_DRAFT.patch` and `CPU_READINESS_DRAFT.json` preserve the draft state. All external effects in main/restorer tests are mocked. Native smoke aggregator tests execute the actual frozen stdlib function, with phase validators mocked only for orchestration testing; actual native result validators and remote preflight remain required. No test creates a GPU job or weather signal.

`fetch_previous_owner.py` is a read-only terminal snapshot helper. It emits seven exact file bytes/hashes and eight name/ID absence results only after completed smoke/terminal status; it must not substitute guessed terminal values. Actual final readiness is `CPU_READINESS_FINAL.json`, `ACTUAL_NATIVE_SMOKE_VALIDATION.json`, seven copied `previous_owner/` files and `ROOT_SMOKE_TERMINAL_AUDIT.json`. The actual smoke final checkpoint is `ffdb347eacea5d87172fdcd122eaa6836fc43ed1bd139f20493fdf3d9672b000`. All48 smoke quiet trials remain failed and unqualified; that integration result does not claim the new objective converged.

Every eventual tracked publication must follow `docs/PROJECT_SITE.md`: update relevant `STATUS.md`, `docs/PLAN.md`, `docs/NEXT_RUNS.md` and `site/project.json` evidence/next-action entries, append a coverage record in `site/updates/`, and run the site check/build. This preparation only changes ignored tmp; root owns publication and final execution status.
