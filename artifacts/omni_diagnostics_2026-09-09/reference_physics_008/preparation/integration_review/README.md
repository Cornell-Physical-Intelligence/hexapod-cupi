# Reference008: fresh standing, then bounded zero-residual wave

This is a new full-C reference trial, not production adoption, PPO training or Stage2/terrain qualification. It retains source007's successful standing physics configuration: TGS16 position/1 velocity iteration, external forces applied each internal position iteration, stabilization disabled. It adds the separately reviewed wave004 timing: the same endpoint and7mm vertical arc, with horizontal travel complete at80% of the2s swing.

The unchanged robot assets, plan/stance, reset noise,2s C2 startup plus2s hold, PD/torque limits, contact sensors, residual core and400Hz observer are inherited byte for byte. Original wave acceptance, including measured2mm flight, six distinct legs, five-foot support,1.6Nm requested torque, original50Hz progress/integral5mm difference and measured quiet stop, is unchanged. The new source/protocol and serialized horizontal swing fields are bound explicitly; there is no old checkpoint compatibility claim.

The host performs fresh32×1000 standing before the1×2400 wave. Standing must pass the original physical gates **and every one of32 replicas must pass the existing unchanged quiet scorer** over the post-settle interval. This strengthens fresh admission using the same quiet gates used to compare005 and007; it does not loosen a threshold. Exact current-source identity, motor/plan/asset equality, scene settings and authored articulation-root16/1 readback are required. Source007's standing-only receipt cannot authorize this trial. No wave follows a failed standing or quiet result.

Relative to the last actual wave004, both the solver settings and horizontal timing have changed. A new wave outcome cannot attribute causality to just one intervention. Source004/005/006/007 and their evidence remain immutable. The actual007 stationary trial proves only bounded undisturbed holding; dynamic progress, landing and stopping remain to be tested.

Source007 still has a reported joint-velocity/position discrepancy below the existing quiet RMS limit: env6 `revolute_2_1` integrates to approximately+0.40970rad while its measured position changes−0.000697rad over16s. This limitation remains explicit; passing standing does not establish accurate joint velocities or safe large-replica PPO scaling. No favorable finite-difference metric replaces the original reported-velocity gate.

The reviewed wave generator retains the bounded contact-triggered C2 landing correction, including previously documented approximately1.7mm target overshoot/return in the real002-trigger CPU reconstruction. No instantaneous anchor replacement or relaxed landing region is introduced. Its shortened horizontal path changes serialized state: `horizontal_duration_fraction` and `horizontal_coefficients` must be preserved by future observation/replay consumers.

Twelve CPU tests cover exact plan/source admission, stale/missing-quiet rejection, reviewed wave hash binding, both bounded host phases and immutable mounts, exact-container cleanup and unknown-inspection errors, the single-wave-replica root readback, byte-equivalent old wave gate AST, and replay of actual005(30/32quiet) versus007(32/32quiet) with the unchanged scorer. They are preparation evidence, not a physics result.

Root owns the guarded launch, both GPU locks, identified forecasting pause/restoration and terminal source/550-asset audit. The inherited launcher retains startup deadlines, unbuffered logs, exact-container cleanup, no automatic retry and failure-time source integrity. This bundle does not itself launch a job.
