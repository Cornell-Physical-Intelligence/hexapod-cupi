# Exact reference007 → reference008 fresh standing and wave trial

This is immutable preparation only. It contains six runtime overlays, exact source lineage/map, integration tests and the separately frozen wave004 owner evidence, without duplicating the926-file parent. Future terminal evidence and independent receipts belong in a separate wrapper.

Parent926-file map: `666571ed6e37a73b857179323573ae20cdef0eee53aa25908108bd4e29465465`. Target926-file map: `a73075ebdf5d6c986728b96117495b8de85adc8db62de1c41ffbe49eb6176d66`.

The trial retains source007's TGS16/1, external-force timing enabled and stabilization disabled. It binds reviewed wave004 horizontal completion at80% of the2s swing, same original endpoint and7mm vertical arc. Fresh32×1000 standing must pass original physical gates plus all32 replicas under the existing unchanged quiet scorer before1×2400 wave. Prior007 standing-only admission cannot authorize008. The new protocol/hash also binds serialized horizontal state; no actor/checkpoint compatibility or PPO path exists.

The original wave gate AST is unchanged, including all measured flight/contact/torque/progress5mm/quiet limits. Environment configuration, startup, residual core, actuators and400Hz observer are byte-identical to007. Compared with actualwave004, both solver settings and horizontal timing differ; no later outcome may isolate one intervention's effect.007's residual joint-velocity bias remains documented and does not become a production/scaling qualification.

The integration owner and independent root/PPO reviewers passed12 focused CPU tests. The final module-docstring-only correction occurred in a fresh owner directory; its preliminary frozen source remains untouched and executable AST equality is recorded. This preparation claims no new physics outcome.

Verify without writing a source tree:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B reconstruct_source.py --parent-source /path/to/reference_source007
```

Add `--output /fresh/reference_source008` only when materialization is required. Reconstruction checks all parent/overlay/target hashes and rejects extras or overwrite. It launches no simulator or job. Root owns guarded dispatch, exact-container cleanup and forecast restoration; those terminal artifacts remain separate.
