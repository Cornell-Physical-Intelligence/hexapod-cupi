# Directional discriminator001 immutable preparation

This preparation contains the exact12-payload owner, independent root11-test/source review, four new runtime tools, lineage metadata and complete930-file source map. It does not duplicate the926-file reference009 parent. Copy that exact parent, apply the five paths in RECONSTRUCTION.json, replace campaign_source_hashes.json and verify every930hash with no extra/missing files.

The owner README and directional contract describe the root-approved reverse, left-strafe, left-turn and forward-right/right-turn arc. One fresh32standing/quiet admission precedes independent cold48s cases, with unchanged wave005/physical/contact/torque/quiet behavior. New limited direction metrics retain signed measured and position-based progress plus the original5mm world consistency gate. Pure turning requires actual rotation, not nonzero translation. No full-omni, PPO, packed-device-observation or rate-fidelity qualification is implied.

Keep this preparation unchanged. Bound launch/guard, actual per-case evidence, raw400Hz audit and restoration belong in a later terminal wrapper. A rejected case stops this bounded campaign; later cases remain untested.
