# Bounded moving PPO host001

This host runs exactly seven serial phases on the frozen C-study source009: fresh32-replica standing;32-replica zero-mean/sampled exploration calibration; zero-residual32-replica timing; ten moving PPO updates at32 replicas and256 controls per update; cold initial and decision010 forward-stop screens; and32-replica decision010 quiet. CLI mode `calibrate` writes the `calibration` folder. It does not allocate128 replicas or25 updates, change gates, reuse the old stand-only optimizer, or claim Stage2 completion.

Every phase must pass its source-bound consumer receipt before a successor starts. For consumer phases, source, device proof, bridge, observation, consumer and550 study assets are read-only mounts; a nested read-only `/outputs/inputs` mount also closes the package alias under the writable output mount. The frozen parent standing command retains its existing mount behavior, with exact asset hashes checked before and after the phase and at exit. All previously completed phase folders are mounted read-only into later phases, with complete file-map verification before and after every job and at campaign exit. Initial and decision checkpoints are bound separately to each consuming phase. The host uses only standard-library Python before Isaac launch.

Each actual job keeps the previous identity-checked Docker ownership/cleanup,90-second AppReady,16GiB free-memory, competing-CUDA, two-lock and shared-coordination guards. The sole supervision change is the declared phase deadline:1800 seconds for ten updates,600 seconds for each of the six other phases. These are maximum bounds, not measured ETAs. An outer5520-second unit plus180-second stop allowance and100-minute independent restoration fallback bounds the whole campaign. The guard restores only previously active forecasting timers and stops only its exact owner.

Run `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s . -p 'test_*.py'` from this directory. Sixteen focused tests cover stdlib import, all command modes/read-only mounts, rejected calibration/profile/training/screens, raw/checkpoint/source mutation, and exact ownership/supervision AST parity except declared deadlines. CPU tests do not establish actual device throughput, physical recovery or improved walking.

The ownership AST comparison locates the exact prior host under the repository temporary preparation directories; for standalone reconstruction set `MOVING_HOST_PARENT_SOURCE` to the published/reconstructed residual-PPO002 `launch_residual_ppo_spark.py`. No test rewrites its input.
