# Moving PPO001 preparation and bounded dispatch

This is the new recoverable **moving** PPO consumer, following the separate two-update stand-only integration. The initial allocation is32 replicas and ten256-control updates. It preserves the exact source009 C-study physics,846/849 observation contract,18 finite position residual actions and1.6 N m motor cap. The command envelope remains0.005 m/s forward and zero; this is an integration/improvement decision point, not useful project speed or Stage2 completion.

The seven serial phases are fresh32 standing, two exploration calibration trials,32-replica zero-residual timing, ten moving updates, cold initial and decision010 forward-stop screens, and32-replica decision010 quiet. The profile must succeed before optimization. No128-replica or25-update continuation is allocated by this host. Any later continuation requires exact measured improvement and retained behavior, beyond merely passing a gate.

The35-payload [consumer](consumer/README.md) has23 passing CPU tests, including actual RSL5.0.1 optimizer/checkpoint reload, selected-row reset/history isolation, recovery exclusion, timeout bootstrap, requested-goal reward masks, callback diagnostic parity and torque spikes between controls. Six installed RSL sources match; stopped-container file readback started no workload and its exact ID is absent. CPU physical measurements are synthetic. The consumer's copied old calibration fixture is a host-score regression, not this campaign's fresh standing/calibration admission.

The [host](host/README.md) passed16 focused tests and the [guard](guard/) passed eight actual embedded-restorer tests. Independent review found and corrected a writable alias to study inputs: consumer phases now mount `/outputs/inputs` read-only as well as `/study`. The frozen parent standing command retains its old behavior, with explicit before/after asset identity checks. Every completed phase folder is read-only to later containers and hash checked before/after each job and at final exit. The old ownership/cleanup loop is unchanged except declared phase deadlines.

[ROOT_BINDINGS.json](ROOT_BINDINGS.json) proves the reviewed host/guard bytes changed only by substituting exact final hashes. The remote [operator preflight](operator/reference_moving_ppo_preflight_001.json) passed with host standard-library Python, all source/dependency maps, previous RR002 campaign/job/restoration hashes, both exact previous names/IDs absent, unchanged coordination and fresh output/pause names. The source/host/guard were checksum synchronized before dispatch.

Unit `hexapod-moving-ppo-001-20260910.service`, invocation `53fa4c8afc7940e0931a77c18d310557`, output `reference_moving_ppo_001`, pause049 records dispatch only. Bounds are600 seconds per nontraining phase,1800 for ten updates,5520 overall plus180 seconds to stop and a100-minute restoration fallback. Those are deadlines, not ETAs. Root alone dispatches GPU work and only the previously active authorized forecasting timers are restored.

## Dependencies and reproduction

[Source009](../reference_physics_009/README.md), the [actual device bridge](../reference_device_smoke_001/README.md), observation005 and550 exact study assets remain separately frozen dependencies; this bundle does not duplicate them. Their full manifest identities are enforced by `consumer/campaign_contract.py` and the operator receipt. The previous host for ownership comparison is published in [PPO001 correction002](../reference_residual_ppo_001/correction002/host/launch_residual_ppo_spark.py).

Run `python3 -S verify_bundle.py` for portable byte and binding verification. Consumer tests require the pinned local RSL/NumPy/Torch dependencies and reconstructed exact input fixtures described in its README. Host tests can use `MOVING_HOST_PARENT_SOURCE` to point at the published prior host; guard tests require only standard Python. Do not run evidence generators into these frozen directories.

Actual training raw data records measurements, commands, full reference state, per-row episode/recovery flags, rewards, sensor clocks and400 Hz physics. It does not serialize the full846/849 actor packets or encoder-history validity counters: actual packet-history content cannot be claimed directly replayed merely from these logs. CPU history-isolation tests and physical reset/clock evidence remain distinct.

No new checkpoint, successful walking result, terrain or production-model qualification is asserted by preparation or dispatch. Final results belong in a new immutable terminal bundle. Stage2 still requires every direction, turn, arc and path to pass unchanged numeric gates and the user's forward-benchmark visual standard; Stage3 remains the subsequent terrain qualification.
