# Independent moving-PPO host and guard review

All **16 host tests** and **8 actual embedded-restorer tests** pass. No further concrete orchestration blocker was found in the reviewed bytes. Consumer/host binding constants were deliberately pending at review time and therefore not launchable; the final hash-only binding must be checked separately. This receipt grants no physical or GPU launch admission.

The host runs seven serial phases with exact `calibrate` → `calibration` folder mapping. Calibration and profile rejection prevent learning; failed training or retention prevents later screens. Initial-checkpoint consumers bind `calibration/initial.pt`; decision screens bind `train_10/decision_010.pt`. Result mode, phase, full input identity, checkpoint SHA and complete raw-file receipt must match. Prior phase trees are checked before and after every subsequent job, mounted read-only, and verified at finalization. Failed phases preserve their state/logs and the campaign separately records any training updates completed before rejection.

The review found one concrete mount gap: the admitted package's `/study:ro` alias was also reachable through writable `/outputs/inputs/study`. Root added nested `/outputs/inputs:ro` after the writable output mount and a focused regression. The final independent 16-test run includes that fix. The frozen parent standing command remains unchanged; its historical alias and before/after integrity checks are explicitly documented, rather than described as a newly sealed mount.

Ownership and cleanup functions match the prior reviewed host AST, apart from the declared phase deadlines: 1,800 seconds for training and 600 seconds for each other phase. The 90-second AppReady, GPU competition/memory checks, identity-based Docker cleanup, shared coordination and locks remain intact. Guard restoration code is AST-identical to RR002. Its four exact prior campaign/job/restoration pins match actual fetched RR002 bytes; it checks both prior names and both recorded IDs absent before pausing.

The outer bound is 5,520 seconds with 180 seconds for stop, plus an independently armed 100-minute restoration fallback. These are bounds, not measured training estimates. Restoration stops only the exact owner and resumes only previously active forecasting timers; unknown ownership or failed cleanup does not silently resume them. No services or containers were signalled during this review.

[REVIEW.json](REVIEW.json) binds the exact reviewed host/test/README/guard bytes and phase mapping. [guard_binding_review.json](guard_binding_review.json) preserves the independent old-restorer and actual-prior-receipt comparison. Logs retain the initial and corrected host test runs and all eight guard tests. No frozen owner source, robot geometry, PPO learner or observation contract was modified by this reviewer.
