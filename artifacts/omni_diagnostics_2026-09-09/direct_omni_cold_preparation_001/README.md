# Preserved omnidirectional PPO: cold comparison preparation

This bundle binds the two-phase original-policy comparison: fresh standing with32 replicas/1,000 controls, then48 replicas across twelve constant-command cases for12 seconds. It loads the exact preserved315/318 checkpoint, not the slow reference-residual actor. Completing this measurement does not qualify Stage2 or admit training automatically.

The new589-file source changes the historical target-step limit from0.03 to the common0.04 rad/20 ms and replaces the old USD authoring import with a read-only asset audit. The physical solver16/4, disabled external-force default, PD30/0.6, C geometry, mass/inertia, action/noise/reward behavior and applied1.6 N·m cap remain explicit. Source009 supplies supervision only. The legacy16 runtime bytes equal the frozen C runtime under the same canonical hash convention.

[Contract and builder](contract/README.md), [host and metadata adapter](host/README.md), [pause/restore guard](guard/README.md), and [independent execution review](independent_host_review/README.md) are copied verbatim with their original manifests. The external metadata adapter adds readiness after real AppLauncher construction and exact runtime/import evidence; original actor/control functions remain unchanged. The host preserves the hardened owned-container loop and checks complete read-only inputs before/after each phase.

Root passed5 contract,12 host and20 guard tests; the independent actual-supervisor fixture passed5 cases. The [source build and read-only preflights](operator/) bind source, original checkpoint, prior cleanup, current coordination and fresh output. The recorded dispatch used invocation`ae3b95c756db4aa4b639ee4230e8c958` and pause053. Actual results are a separately frozen terminal bundle; this preparation cannot stand in for motor/contact, stop or full-direction qualification.

Run `python3 verify_payload.py` from any location to verify all payloads and nested freezes. Files inside frozen reviews retain their original workstation references; PREPARATION.json records portable identities.
