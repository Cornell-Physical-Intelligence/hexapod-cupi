# Native005 host004: explicit extended campaign and bounded deadlines

This is a frozen CPU-tested successor bound to native005's exact28-file freeze and source004's599-file map `aa70659d33243f1e28e9c71b1a7f9d33341642db770fd7356192573a26fe904e`. The root-executed source build and inventory audit are preserved under source_build; FINAL_BINDINGS.json records plan/origin/code hashes. Root owns actual remote preflight, GPU dispatch and publication. This preparation does not establish physical or learning quality.

The explicit allocations remain smoke32×24×2 and pilot1024×24×50; `extended` adds1024×24×500. Smoke accepts only `quiet_priority`. Pilot and extended accept only explicitly selected `caps`, `curriculum` or `quiet_priority` branches and require completed same-source quiet-priority smoke through the unchanged native API. No allocation continues automatically. Initial evaluation and training start from the same original checkpoint. Extended uses the same six folders: standing, initial_constant, initial_stop, train, final_constant, final_stop. The prior smoke and completed phase directories are mounted read-only and fully rehashed before/after each phase.

The host records successful training updates from three agreeing pieces of evidence: the validated selection, native accepted result and complete training receipt. It cannot turn an accepted50-update result into a500-update success. A later phase failure preserves the verified completed count; failures before training acceptance retain the separate producer-observed receipt count without claiming acceptance.

## Versioned deadline adaptation

Source009's immutable supervisor `launch_reference_physics_spark.py`, SHA `9ebabaf254cc77fabad7d4759cc80fc26d7efd3912ea84bd55304594c88ed561`, contains fixed600-second receipt and monotonic deadline expressions plus a misleading standing-only timeout message. `deadline_adapter.py` is installed **only for extended**. It verifies the entire original source hash, exact loaded function bytecode and its original module globals, then substitutes exactly:

1. Receipt `deadline_seconds=600` → explicit phase deadline.
2. `monotonic()+600` → the same phase deadline.
3. The timeout message → actual phase and declared seconds.

Extended training gets1800 seconds; all five other phases stay600 seconds. AppReady stays90 seconds. The adapted function is compiled into its own verified supervisor module globals. Every other original statement—including both locks, foreign-workload checks, cancellation, exact container identity/cleanup, uncertainty receipts and final lock release—remains unchanged. Reversing the three source substitutions restores the exact original function text; the entire finally AST is separately checked. No frozen source file is written. This is explicitly a versioned runtime adaptation, **not unchanged run_owned**. Each extended job receipt records the adapter schema/source/code hashes and deadline map. Smoke and pilot do not install it.

The sibling adapter is loaded lazily by exact file path after own-bundle verification, so existing guard importlib loading works from outside the host directory without adding a new search path. The adapter refuses wrong source, ambiguous/missing seams, replaced loaded code, duplicate installation and unknown phases. It remains standard-library-only; no Isaac/Torch imports or new GPU steps are introduced.

## CPU evidence

47 tests pass in `tests_final.log`; `tests_draft_003.log` preserves the prior45-test result. They execute the real inherited/adapted run_owned bodies under fake process/container/resource clocks, proving700-second extended training succeeds,1800-second training timeout,600-second evaluation timeouts, unchanged90-second startup deadline, old smoke/pilot deadlines, exact owned stop/cleanup, uncertainty on unknown Docker inspection, all six phases,500 accepted updates, changed smoke rejection, wrong count rejection, and guard-style `python -S` import from an unrelated working directory. Existing entry/legacy map/unchanged-function tests remain included. Tests use the source-bound actual native005 contract, not a new simulator harness.

`tests_draft_001.log` preserves an initial fixture error: its smoke fixture incorrectly supplied a prior-smoke path and was rejected by the existing selector; only that fixture setup was corrected. A separately detected real sibling-import integration issue was corrected before the final draft test run and has its own import regression.

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tmp/direct_omni_train_host_004 -p 'test_*.py' -v
```

Planned CLI remains the existing host flags plus `--allocation extended --branch caps --smoke <exact-completed-same-source-smoke>`. Source/checkpoint/contract/supervisor-source/output/isaaclab paths stay explicit. Native005 independently verifies500 ordinary autosave names, seven decisions and declared control/transition/optimizer counts; this host does not duplicate that native contract. Extended target dynamics and all physical/quiet gates remain unchanged, and500-update acquisition is not Stage2 acceptance.

Old receipts under historical_host003 and parent_evidence remain labeled archival; current readiness is CPU_READINESS_FINAL.json; DRAFT_READINESS.json is preserved historical preparation. Eventual tracked integration must follow docs/PROJECT_SITE.md, with a bounded central site/update record, STATUS/relevant Markdown and site check/build. No tracked files, frozen originals, remote state or GPU jobs were changed here.
