# Reference007: correct articulation-link iteration readback

This is an introspection-only retry of source006. The physical configuration remains TGS16 position /1 velocity iteration with external forces applied every internal position iteration, and stabilization disabled. Standing-only32×1000, randomized physical reset, C2 startup, robot assets, actuator parameters, observer and every physical/quiet gate remain unchanged. No wave, policy or production adoption is permitted.

Source006 stopped before control0 because the readback incorrectly required independent solver iteration attributes on all608 articulation links. Its actual snapshot proves all32 articulation roots are explicitly16/1. The root links have resolved16/1 rigid-body fallback metadata, while the other576 links have absent, unauthored attributes. This is an instrumentation failure, not a result of the16/1 physics or quiet-hold experiment.

NVIDIA documents that solver iteration counts belong to the entire articulation, not individual links. Its reduced-coordinate API sets/gets the counts on the articulation object. Therefore absent unauthored per-link metadata must not be treated as missing articulation configuration. [PxArticulationLink](https://nvidia-omniverse.github.io/PhysX/physx/5.4.1/_api_build/classPxArticulationLink.html), [PxArticulationReducedCoordinate](https://nvidia-omniverse.github.io/PhysX/physx/5.3.0/_api_build/class_px_articulation_reduced_coordinate.html)

The correction still requires exactly32 roots at16/1 and608 bodies, each under exactly one proven articulation-root path. It records absent per-link attributes explicitly and never interprets absence as zero. Present malformed or conflicting per-link metadata still rejects conservatively; it is not claimed to be an effective native override. Missing/wrong root counts and unowned or missing bodies still reject. The retained before/after config and scene attributes remain mandatory. Reported counts are resolved USD configuration, not native per-island iteration counters.

Four focused tests replay the byte-preserved actual006 failed snapshot and test wrong/missing roots, missing/unowned bodies and authored-invalid/conflicting metadata. Source006 and its failed-readback payload remain immutable. The new owner fixture contains the exact actual006 readback JSON for reproducibility. Root owns guarded dispatch, new source identity and cleanup/restoration.

The actual low-level tensor iteration getter remains unverified/unavailable. The installed host source contains no getter, and bounded no-GPU reads of the immutable base image did not find the runtime tensor extension source. No undocumented getter is called. Deprecated high-level Isaac Sim API documentation is not treated as proof of this backend's callable interface. The complete authored articulation-root settings are mandatory; this run reports USD configuration rather than native internal iteration counters.
