# Paired-motion001: preparation and bounded physics dispatch

This new contact-aware controller proposes moving two opposing feet together while retaining four required supports. It preserves the C geometry, mass/inertia, source009 physics/actuator settings and target limits. The diagnostic requests0.01 m/s forward. It is a proposed faster reference, not PPO, omnidirectional control, useful mission speed or Stage2 completion.

The [121-payload preparation](preparation/README.md) preserves the complete43-file CPU owner,40-file actual adapter, independent tests/bindings and exact21-file delta reconstruction. All925 parent source009 files other than origin metadata remain unchanged;20 files are added, producing946 payloads. The new1014-value controller state fragment is explicitly incompatible with the scalar846/849 actor packet. No checkpoint is loaded.

The run first requires fresh32-replica/1000-control standing and unchanged quiet gates. One separate robot then executes2400 controls:4 s canonical startup,2 s reference hold,24 s requested forward motion and18 s stop. Each moving foot must independently qualify lift, descent and contact-confirmed landing. Required support is checked against actual articulated COM and measured contacts; the original scalar five-support gate is preserved as a separate contract. The paired test declares four required supports and a50 mm support-margin floor, with each confirmed foot also required while its partner finishes.

Every ordinary sensor clock and all eight physics updates per control are recorded. Requested/applied torque is checked at400 Hz before the next target, and original progress/rate-consistency/quiet metrics remain enforced. Full reference state, each foot's qualification and every poststep support/torque result are independently recomputed from raw data in the scorer. CPU synthetic contacts or scheduled endpoints cannot admit actual walking.

Root repeated13 adapter tests in20.605 s and eight embedded-restorer tests. Tests include a complete2400-control synthetic sequence, exact state replay, hidden intermediate torque spikes, tampered support reports, missing/stale support, failed quiet/progress and strict finalization. The portable preparation/source verifier passed all40 owner/946 source payloads and original cleanup AST. The actual remote Python-S preflight confirms source, guard, unchanged coordination, five exact previous moving-PPO campaign/job/restoration receipts and all three previous names/IDs absent.

Root dispatched `hexapod-pair-motion-001-20260910.service`, invocation `aeb33fdfce794cfca50e4227ebfada1d`, output `reference_pair_motion_001`, pause050. Bounds are600 s per phase,90 s AppReady,1320 s outer plus180 s to stop and30-minute restoration fallback. These are deadlines, not ETAs. Only exact owned containers are cleaned up and previously active authorized forecasting timers restored.

The preceding moving PPO001 failed before any optimization: a retained RR support loss on replica6 triggered a deferred reset and exposed an inference-tensor mutation error. Its rejected no-event profile is preserved separately. This new paired test neither repairs that software API nor promotes the failed scalar profile to a pass.

Run `python3 -S verify_bundle.py` for portable byte checks. The [inner reconstruction instructions](preparation/README.md) reproduce the exact946-file source from source009 without modifying the parent. Root review and actual preflight are in [ROOT_REVIEW.json](ROOT_REVIEW.json) and [operator](operator/). Historical CPU/failed fixture evidence remains intact. Actual results belong in a separate immutable terminal bundle.
