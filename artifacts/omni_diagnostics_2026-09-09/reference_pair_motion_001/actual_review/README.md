# Actual paired-motion 001: complete stepping, unchanged consistency rejection

The actual trial is **rejected**. It completed all 2,400 controls and all 11 paired steps, but its original 50 Hz position/velocity-integral difference is **5.579826 mm**, above the unchanged 5 mm bound. The same 6–30 s interval at 400 Hz differs by **6.979341 mm** for the root link and **6.970354 mm** for COM. These separate diagnostics do not replace the original gate or establish the native cause of the bias.

The physical contact/target/torque/stop evidence is useful: all six legs independently achieved qualified flight and landing, for 22 confirmed foot landings. Measured lifts span 4.591–5.491 mm. All four required supports were retained, with a minimum measured articulated-COM margin of 101.899 mm. There were no resets, nonfoot contacts or poststartup torque exceedances. Every one of the 19,201 initial-plus-substep records has verified indices, counters and control endpoint parity. Poststartup requested and applied torque peaks are **1.397031784 N·m**, at 6.4375 s on `revolute_2_3`.

Actual forward displacement over the 24 s request is **234.943479 mm**, equivalent to **0.009789312 m/s** from positions; mean reported body-forward velocity is **0.009583095 m/s** for the 0.01 m/s request. Integrated link-forward displacement is 229.668915 mm at 50 Hz. The consistency gate uses the full three-dimensional displacement difference, not just its forward projection. After the zero request, measured additional forward displacement is 11.240319 mm. The reference reaches its exact quiet hold after 5.90 s; the final 10.08 s measured quiet window passes the original bounds, with worst joint RMS 0.005605481 rad/s.

Fresh standing independently passed for all 32 replicas, including all original quiet gates. Its settled 400 Hz requested peak was 1.483903289 N·m. The paired run retains the initial reset's 63.659622 N·m computed peak and approximately 1.6 N·m applied clamp in raw data; the existing startup exclusion is not hardware startup qualification.

The frozen source scorer was independently replayed locally. All 2,200 complete paired controller states and raw poststep support/torque checks reproduce with maximum floating difference 1.6653e−11, inside the predeclared 1e−8 replay tolerance. Local/remote gate differences are limited to that replay difference and 4.66e−10 m in the original consistency norm. Three standing heading rows differ by about 6.83e−6 degrees across hosts. Both full local and original remote reports are retained; all verdicts agree, and no exact-row equality is claimed.

The inherited generic observer's `moving_window_or_prefix` covers controls 200–1400. The paired request instead covers controls 300–1499, with physical endpoints at 6 and 30 s. `report.json` explicitly evaluates the correct paired interval at both rates; the original observer report remains unchanged. `actual_paired_motion.png` uses actual raw pose, named force and 400 Hz torque data. The green region is the scored quiet window, blue is the 24 s forward request, and gray is canonical startup. It is a trace figure, not a rendered walking video.

`remote_audit.json` in the parent result directory verifies all 946 frozen source files and 550 admitted asset files unchanged, both exact owned container IDs and names absent, and the expected terminal unit invocation. All 33 raw run/pause payloads match their remote SHA-256 hashes locally. Pause050 restoration is recorded. This proves the historical job's cleanup; it makes no claim about current shared locks or another job's ownership.

No final walking, Stage 2, PPO, production or terrain admission follows from this rejected reference trial. Deeper rate/pose-gap analysis is a separate append-only review; no gate or source change is made here.
