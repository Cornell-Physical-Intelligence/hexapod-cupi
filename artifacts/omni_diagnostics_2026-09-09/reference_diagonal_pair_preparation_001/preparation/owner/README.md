# Two diagonal static-load diagnostics

Ready for root's bounded dispatch after independent review; this preparation contains no new physical result. The two cases are LF/RR unloading while LM/LR/RF/RM support, followed by LR/RF unloading while LF/LM/RM/RR support. Each uses a separate cold environment. The previously measured LM/RM case is not repeated. These are proposed static four-support diagnostics, not original five-support walking gates or PPO qualification.

The exact parent is the admitted middlepair001 source (`69a1a23304448d53f7681dfa87ab4fa652ad682ba0e45406e82187a66c4224f0`). The child retains 934 payloads: 928 are byte-identical, and `SOURCE_BUILD.json` lists exactly six changed paths. Explicit named pair selection is added to the generator/scorer and bound into the CLI, receipt and host case identity. All reference rows must name the same pair and retained supports as the scorer. Existing fields named `corner_*` now apply to that case's four retained feet; their numerical bounds are unchanged. The old middle-pair LP force prediction is deliberately absent for diagonal cases.

The five source overlays are `load_transfer.py`, `score_transfer.py`, `pair_screen_contract.py`, `run_pair_physics.py` and `launch_pair_physics_spark.py`; the sixth path is lineage metadata. The historical middle-pair owner/hash records remain provenance of the parent. The child map and new overlay hashes identify the modified generator/scorer. No parent source, production model, wave005, actor schema or original walking gate is changed.

## Fixed runtime and gates

One fresh 32×1000 standing test must pass the original physical and all32 unchanged quiet gates. Its exact source/plan/stance/asset/solver identity and admission SHA authorize both following cold cases. Each diagonal case executes 200 startup controls and 1100 diagnostic controls (1×1300 total). A failure stops the campaign before the next case. Prior middlepair001 or directional standing admissions are rejected.

Cold startup is exactly the inherited randomized physical reset, followed by the same 2 s bounded C2 target transition to the canonical named C40/120 stance and 2 s hold. No additional joint or body state write is introduced. The 22 s diagnostic then has 2 s baseline, 3 s raise to 7 mm, 2 s unload hold, 3 s return, and 12 s quiet including 2 s settling plus the complete original 10 s measured quiet window. Targets for the four retained legs stay fixed. First and final knots, named soft limits, preload continuity and exact zero-residual emission remain enforced.

All numeric rules from middlepair001 are retained: four valid distal supports, at least 50 mm actual articulated-COM support margin, at most 15 mm body displacement / 0.10 rad rotation / 10 mm retained-foot drift / 0.020 m/s reported retained-foot slip, and at least 1 s contiguous measured two-foot unload with both feet at least 2 mm clear. Actual force balance must remain within 5% of weight. Return requires all six supports within 0.6 s with three confirmations and then the unchanged quiet scorer. Every diagnostic 400 Hz requested torque sample, including its initial sample, must be ≤1.6 Nm; applied torque must be ≤1.60001 Nm. No reset, nonfoot contact or missing/overflowed contact evidence is allowed.

The accurate full-C body and masses, per-run writable USD handling, 18 named joints / 19 bodies, unchanged PD/actuator snapshot, contact buffers, distal-versus-shaft classification, SDK XYZW telemetry and pinned16 runtime remain exact. Physics is TGS16/1 with external forces every iteration, stabilization false, dt 0.0025×8. Target limits remain reference 1.75 rad/s and 6 rad/s² inside the total formal 2 rad/s and 8 rad/s² budget. Existing authored-root solver readback gates remain mandatory; no new native solver API assumption is introduced.

The physics emit loop, both startup/diagnostic observer contexts, pre-reset capture and host `run_owned` / exact-container cleanup code are byte/AST unchanged. Diagnostic time zero is physical time 4 s; all 8801 diagnostic substep rows are scored without a settling exclusion. Contact forces/classifications are 50 Hz; only joints, poses, rates and torques have 400 Hz coverage. Known reported-rate versus position discrepancies are unresolved and are not waived. Initial reset torque transients are preserved in the separate startup trace; this is not hardware-startup qualification.

## CPU evidence

`tests.log` records 12 passing focused tests in 4.98 s. Both diagonals complete the whole synthetic 1100-control driver/core/scorer path with 8801 paired substep rows and a full 10 s quiet window. A reversed runtime joint order selects the same named pair. Missing retained support rejects before the next target. The middle-pair generator P/V/A is exactly unchanged over 1100 knots and the actual middlepair001 trace produces an exactly equal scorer result. Tests also verify immutable physics loops/observer dependencies, original cleanup, readonly mounts, allowed phases and fresh standing/quiet identity. Synthetic forces/contacts are test fixtures, not diagonal physical evidence.

`INTEGRATION_PREFLIGHT.json` records all934 hashes, pinned16 runtime, old middlepair001 admission rejection, failed-quiet rejection, both named case identities and explicit middle-repeat rejection. Run from repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 uv run python -B -m unittest discover -s tmp/reference_diagonal_pair_adapter_001 -p 'test_diagonal*.py'
```

`build_source.py` and `check_integration.py` are one-shot scripts. Preserve their existing outputs and all frozen bytes.

## Root-owned launch

Remote source: `/home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_diagonal_pair_source_001`

Output: `/home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_diagonal_pair_001`

Root's declared unit is `hexapod-diagonal-pairs-001-20260910.service`, with pause047, 1920 s outer deadline and 40 min restoration fallback. Each of the three phases independently retains a 600 s bound, 90 s AppReady deadline, 45 s traceback and unbuffered output. No phase retry, automatic training or gate relaxation is allowed. Root must verify live workloads, both locks, previous owned-container absence and forecasting restoration before dispatch.

Under root's guarded environment and source/tools PYTHONPATH:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_diagonal_pair_source_001/tools/launch_pair_physics_spark.py --source /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_diagonal_pair_source_001 --output /home/orionh/HEXAPOD_runs/mock_length_study_20260909/reference_diagonal_pair_001 --isaaclab /home/orionh/IsaacLab
```

Expected phase directories and job/log labels are `standing`, `lf_rr`, `lr_rf`. The standing receipt is unique to this exact child source. Each diagonal directory preserves startup and diagnostic traces/400 Hz rows, full reference state, actual pair reset, post-step checks, solver/asset readback and terminal state. Campaign records per-case state hashes after each success. Original study/source maps, failures and exact owned IDs are retained on all exits. Unknown Docker inspection remains cleanup uncertainty and requires root review before another workload starts.
