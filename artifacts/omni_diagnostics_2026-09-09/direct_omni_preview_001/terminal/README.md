# CAPS50 actual progress video: complete media, failed final receipt

The actual full-robot video is complete: **38 seconds at 1× playback, 950 independently decoded 1280 × 720 H.264 frames at25fps**, from the exact CAPS50 PPO checkpoint `ca0545760f81b8b8fb0cafde4b1ef89bfa5d8765a8b43b578e451f648a954415`. It shows quiet, forward, stop, left strafe, stop, forward-left arc and stop, with actual simulated motion and no ideal-pose controller.

[Watch the actual recording](run/recording/rollout.mp4). [Representative actual frame](review/frame_07.png). Video SHA-256: `b2ccdd62821e58c3d8bfd62150b9272d4d3c47c7d8020dea0df2e8571550547d` (38,307,255 bytes). The full robot and top telemetry are visible. Ground arrows/trail are visible; the 3D ground text exhibits overlapping/ghosted rendering in the inspected frame, so the top caption remains the legible command reference.

**The host campaign failed.** After the native container completed and exited0, the host required `recording/final_integrity.json`; that post-App-close receipt was absent. The native state and video receipt are complete, all1,900 controls were captured, there were no returned terminal events, and before/after actor, critic and normalization tensors match the selected checkpoint. The recorder's own pre-close source check and the external terminal source/input check both passed. Those facts do not manufacture the missing post-close receipt or change the failed host verdict. The actual cause of the missing outer continuation is not established by these exports. No frozen source or result was patched or rerun.

The guard owner was `hexapod-direct-omni-preview-001-20260910.service`, invocation `7121f177c458460e81de0ef7f4a967f3`. The read-only remote audit verified all598 native source files,926 supervisor files,26 native contract files,20 adapter files,7 host files,9 guard files,550 asset files,16 legacy runtime Python files and the full selected completed pilot. Both exact recording container name and ID are absent. Pause058 restored the two previously active forecasting timers at Unix1789058753.2000067. No agent launched or signalled a job during this audit.

All28 raw payloads (42,606,350 bytes) match two stable remote hash passes and the complete local transfer. `remote_audit.json` retains the exact terminal journal/traceback, source/input identities, cleanup/restoration evidence and raw maps. `ffprobe.json` independently counts950 decoded frames. `review/report.json` retains segment telemetry, checkpoint/provenance checks and the failed-finalization interpretation. The first audit script had a Python bracket syntax error before remote execution; its exact rejected source and stderr are preserved separately, and the corrected read-only script then passed.

This is a progress preview, not policy acceptance. Across each complete six-second commanded segment including command ramps, reported mean forward speed was0.0851m/s for a0.10m/s request, mean left speed0.0900m/s for a0.10m/s request, and the arc averaged0.0782m/s forward plus0.1246rad/s yaw for0.10/0.20 requests. Those are descriptive recording averages, not matched cold-evaluation scores. Requested torque reached6.2926N·m and the per-segment requested saturation averages remained about9–11%; applied torque clipping is a separate channel. Short stopping clips cannot replace the actual completed pilot's failed0/48 sustained quiet result. Stage2 remains incomplete.

Run `python3 -B verify_payload.py` for portable hash and terminal-boundary checks; it imports no simulator or checkpoint. The selected checkpoint itself is referenced by exact hash and completed pilot identity, not copied into this media bundle. No raw forecasting dataset is included.

This bundle is prepared in ignored `tmp/` only. `SITE_UPDATE_PROPOSAL.json` proposes the central framework evidence/media update for root to review when publishing it. Latest saved checkpoint, this actual recording and immutable Benchmark1 remain separate. Frozen failure evidence stays intact.
