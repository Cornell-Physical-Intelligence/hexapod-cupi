# Proposed tracked documentation updates for root

## docs/PLAN.md

Add one bounded quiet-priority direct315 ablation following the completed matched curriculum/CAPS pilots. The existing C-study physical setup, 315/318 observation contract, original checkpoint, adaptive PPO, reset curriculum and cold gates remain fixed. Only exact-zero temporal pairs receive total coefficient 1.0 instead of .1; moving temporal and spatial coefficients remain .1 with the same all-valid denominator. It tests objective priority, not a missing quiet mask or proven convergence limit. Actual prior traces show ~87.5% of quiet target increments at the .04 rad bound; both prior pilots fail 48/48 quiet.

Record every minibatch KL/rate and mode counts. Sparse first/last minibatches at updates 1/10/25/50 record actor-gradient decomposition without changing optimization. CPU diagnostics off/on parity is required and passed for the prepared implementation. Native004 remains unexecuted until the guarded run's receipt exists.

## docs/NEXT_RUNS.md

Root dispatch sequence: build/pin the native004 source and host successor; run quiet_priority smoke32×24×2 with fresh matching standing plus final constant/stop acquisition and strict reload. Only after complete same-source smoke/input/cleanup review, separately dispatch one quiet_priority1024×24×50 pilot from original1971 with initial/final cold constant+stop screens and immutable decisions10/25/50. No automatic further updates. Preserve all failure/evaluation rows; loss reduction alone cannot admit quiet or Stage 2. Compare every direction and all48 stop replicas against the published matched pilots. Forecast/GPU ownership remains exclusively root's guarded dispatch.

## Central project site

Keep latest saved checkpoint at the actual CAPS50 result until new actual weights/receipts exist. Add native004 as prepared next experiment, not learned progress. Existing benchmark and latest actual video remain separate. The append-only record must cover the new preparation artifact plus actual `docs/PLAN.md`, `docs/NEXT_RUNS.md`, `site/project.json` and STATUS edits if made. Root should use the actual publication timestamp, not a guessed launch time.
