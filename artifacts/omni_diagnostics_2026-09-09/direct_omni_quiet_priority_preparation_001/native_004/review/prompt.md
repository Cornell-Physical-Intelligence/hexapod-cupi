Review this single objective decision. Reply in at most 500 words, prioritizing up to three concrete errors or useful cautions. Do not use tools, request files, delegate, or propose a broad sweep. Work only from these stated facts. The user explicitly requested Fable 5.1 effort max consultation; implementation proceeds in parallel. Every eventual repo change must update docs/PROJECT_SITE.md's central progress/evidence registry, but you must not edit anything.

Robot simulation: 18-joint hexapod, prior 315 actor /318 critic direct joint-position PPO. Raw deterministic actor mean is multiplied by .5 rad, joint-clipped then slew-limited to .04 rad per20ms; PD and applied1.6Nm cap fixed. Five actor history frames: IMU, command, q/qdot, raw previous actions. Actors/critics have separate MLPs/normalizers. New data show both matched50-update runs (1024x24x50=1,228,800 transitions each, original same warm-start checkpoint, std reset.1, adaptive5e-5 LR) fail all48 measured quiet trials. LR was not stuck: floor14/50 and13/50 update endpoints, peaks5.0625e-5; per-minibatch LR/KL not recorded. Resampling is proven working, all256 dedicated quiet rows exactlyzero;68.34% of valid temporal pairs quiet. Actualfinalquiet deterministic raw-action adjacent-step RMS median .5594 baseline/.5517 CAPS; ~87.5% of executed joint/control increments hit.04 slew. Existingquiet targetp95bound.002 rad,rawSDKqdotRMS.03rad/s,drift.01m,requestedsaturation.005; no gates change. These actual metrics are distinct from aggregate minibatch temporalMSE.

OldCAPS objective: .1 * sum(valid_i*d_i)/max(sum(valid_i),1) + .1*spatialMSE, where d_i=mean over18 of (mu(obs_i)-mu(previous_obs_i))². Both actor passes differentiable; synthetic neighbors never update normalizers. valid excludes done/reset crossings and changed latest command slices; no teacherstopgradient. Meanforward draws no actions. Aggregate temporalfirst10=.316994,last10=.312438(-1.44%). Scalar losses don't establish gradient balance, and50updates doesn'tproveinfeasibility.

Proposedoneablation: same initialcheckpoint,50updates, observations/physics/actions/curriculum/optimizer/coldinitial-finalconstant+moving-to-stop screens unchanged. Change only temporal pair coefficient from .1 to1.0 when BOTH latest command triples exactlyzero, otherwise.1; SAME all-valid denominator. Spatial stays.1. Explicit objective priority, notfixmissingquietmask. Freshnew source/contract/guard, immutable decisions10/25/50; no automaticcontinuation.

Instrumentation: every minibatch stores KL and LR before/after existing adaptive rule. First minibatch of updates1,10,25,50 records actor-gradient norms for PPO(actor surrogate-entropy), quiet temporal, moving temporal, spatial and cosine PPO-vs-quiet; autograd.grad(retain_graph=True,allow_unused=True), without assigning.grad, new RNG draws, optimizer/normalizer updates. Then originalzero_grad/backward/clipping/step; capture combined actor.grad norm before/after clipping. Tests must prove enabled-vs-disabled diagnostics exact actor/critic/optimizer/RNG/normalizer parity through realRSLCPU updates; .1 quietcoefficient parity and quiet-only1.0 scaling/mask tests. No GPU inyourreview.

Is this a defensible fastest bounded learning intervention versus merely extending oldbudget? Identify specific gradient/instrumentation or loss-normalization pitfalls that would materially change code or conclusion. Distinguish facts from unproven feedback/noise causal claims. No promiseofconvergence.
