Smoke002 is a fresh four-phase direct315 CAPS smoke under pause055, prepared from frozen guard002. Root is the sole dispatcher. Final successor source/contract/host hashes are bound and verified; the actual launch remains root-owned.

The allocation stays standing32×1000, train32×24×2, final constant and final stop diagnostics. The owner retains 2520 s runtime plus 180 s stop, with a 45 minute restoration fallback. Physical settings, checkpoint initialization, torque/contact/quiet gates, acquisition semantics and automatic-continuation prohibition stay unchanged. Root's separate source002 fixes only checkpoint-finalizer inference-tensor handling and provenance; this guard binds that reviewed correction without implementing or changing it.

Prior smoke001 is preserved as failed: standing was accepted, two PPO updates were observed, checkpoint finalization failed, and final evaluations did not run. Its completed standing job and failed train job both require exact cleanup and four name/ID absence checks. All five terminal campaign/job/pause/restoration bytes are copied under previous_owner; they were read and hash-verified directly from Spark. The original smoke invocation is 1de80a43928f4d7497846dc762048d86. The prior source001/host001 identities remain separately fixed and cannot be replaced by successor hashes. This is ownership cleanup evidence, not a completed-smoke admission for a pilot.

Source002, preparation002 and host002 are the exact bound successor remote names. The checkpoint and source009 supervisor remain unchanged. The guard runs full host/source preflight before pausing and immediately before dispatch. The embedded restorer is byte-identical to guard002; both GPU locks, unrelated-workload rejection, exact owner cleanup, active-timer restoration, failure fallback and current coordination hash are retained. The independent halo replay stays under the separate frozen deferral helper.

Focused tests:

    PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tmp/direct_omni_train_smoke_guard_003 -p 'test_*.py'

29 CPU tests pass, including live-path fake-effect ordering, exact prior failure semantics, source/selection rejection, cleanup failures and the unchanged embedded restorer. Actual Spark reads confirmed the previous unit failed and all four exact container identities absent. No GPU execution, pause, remote mutation or tracked/main edits were performed by this preparation.
