# Directional002 reviewed source and guarded dispatch

This preparation contains the exact 930-file source reconstruction, independent
review, root review and bounded guard for left strafe, left turn and a
forward-right arc. Fresh standing precedes those three cases. Reverse001 remains
rejected and is not repeated; original wave/contact/torque/quiet gates stay fixed.

All 16 source tests and eight guard-restoration tests passed independently and
under root replay. The source and guard inventories also matched on the Spark.
The previous PPO002 owner had completed all four phases; its four recorded
container IDs and names were absent and pause043 restoration was recorded before
this dispatch. These operational checks do not substitute for the independent
PPO002 numeric audit.

`owner_preparation/` preserves its original 22-payload bundle; reconstruct the
new source from the exact directional001 parent using its five-file overlay and
verify every source hash. `independent_review/` preserves the separate review.
`guard/` permits only this new source and fresh output, with a 2520-second owner,
90-second per-phase AppReady check, 600-second phases and a 50-minute forecasting
restoration fallback. It preserves the shared locks and unrelated workloads.

The dispatch receipt records unit `hexapod-reference-directional-002-20260910`,
output `reference_directional_002` and pause044. This immutable preparation has
no actual directional002 results; terminal results belong in a separate bundle.
No directional, PPO, velocity-fidelity or Stage 2 admission follows from dispatch.

Run `python3 verify_payload.py` from this folder or by absolute path. It reads
only published payloads and does not launch or inspect a GPU job.
