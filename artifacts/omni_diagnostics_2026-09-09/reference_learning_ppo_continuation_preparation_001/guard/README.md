# Reviewed ten-update continuation guard

The guard prepares a separate `--phase-group learn` allocation against the existing `reference_learning_ppo_001` admission output. It calls the exact bound host's `admit_existing_campaign` before any forecasting pause. That host validates the original completed campaign, accepted phase maps, assets, checkpoints and the external reviewed decision. It writes a distinct `learning_campaign.json`; original admission evidence remains immutable.

- Unit: `hexapod-learning-ppo-train-001-20260910.service`
- Pause: `forecast_pause_052`
- External decision: `BASE/reference_learning_ppo_decision_001.json`
- Host and consumer: exact final consumer003 hashes in `CPU_READINESS.json`
- Outer runtime: 3,720 seconds; stop: 180 seconds; fallback restoration: 70 minutes.

The prior admission invocation is bound to `d161f77036f346d2b1abe244d95b2dbe`. The actual admission campaign, three job receipts and pause051 restoration hashes, plus the external reviewed decision hash, remain pending. Execution rejects incomplete bindings before any subprocess or pause. No actual admission or learning pass is implied by this preparation.

All 20 CPU tests pass: the eight unchanged embedded-restorer tests and twelve continuation tests. They exercise same-host admission calls, decision substitution/rejection, exact six old container tokens, restarted owners, absent restoration, the existing-output/fresh-learning contract and time bounds. The final post-pause integrity check belongs to the known-not-launched restoration branch; failure restores timers without requiring a nonexistent owner unit. The embedded restorer is byte-identical to the reviewed admission guard.

Root will supply the final actual receipts after independent review, bind/freeze this guard and perform remote preflight. No GPU job or forecasting pause occurred during preparation.

A successful transient unit may be garbage-collected by systemd. An empty live InvocationID is accepted only with an inactive/failed unit state, all five exact admission receipts and six exact container-absence proofs. Any conflicting nonempty invocation or active state still rejects; the expected historical invocation remains a mandatory bound value. The new regression covers the empty and active-empty cases.

## Final root allocation

Root independently replayed all actual recovery numerical evidence and verified all74 raw hashes. The exact external decision allocates32 replicas and10 PPO updates followed by unchanged cold initial/final and quiet screens. All six pending prior/decision hash values are now concrete; the historical CPU preparation receipt is preserved. Twenty final root guard/restorer tests pass. No larger continuation is implied.
