# Reviewed ten-update PPO continuation

The complete actual recovery admission passed independent and root numerical replay. Root verified all74 raw files and authorized the existing32-replica ten-update PPO pilot, followed by unchanged cold initial/final forward-stop and32-replica quiet evaluations. The [admission terminal bundle](../reference_learning_ppo_admission_001/README.md) retains the original zero-update evidence, and [source preparation](../reference_learning_ppo_preparation_001/README.md) pins consumer003 and the two-stage host.

The external decision binds exact actual source/standing/calibration/recovery states and learning-integrity evidence. Of16,384 poststartup row-controls,15,443 are active and941 excluded; six rows fail, two complete recovery, four remain within recovery at the bounded end. Active requested torque peaks1.5907357N·m. Higher requested startup/recovery transients remain explicit; all applied samples remain bounded at float32(1.6N·m). These facts admit learning infrastructure only. No GPU timeout occurred in the admission and memory peak coverage is limited; neither is overstated.

Root repeated the complete numerical replay and all20 continuation guard/restorer tests. Remote Python-S preflight verifies the exact completed initial campaign, all prior phase trees,550 assets, source/host/consumer, external review and six owned-name/ID absences before any forecast pause. A collected transient unit may have an empty live invocation; its exact historical invocation and five immutable campaign/job/restoration receipts remain bound, and conflicting IDs or active units reject.

Dispatch created unit `hexapod-learning-ppo-train-001-20260910.service`, invocation `804a727558374268b185325305148b1a`, pause052, writing a new `learning_campaign.json` in `reference_learning_ppo_001`. The original admission report, phase evidence, root receipts and prior jobs/logs are read-only and rechecked throughout. Training is bounded to1800s, each of three screens600s, outer3720s plus180s stop and70min fallback restoration. There is no automatic25-update or128-replica allocation.

This is dispatch evidence, not a completed learner or Stage2 qualification. Yesterday’s omni capability remains the baseline; full directions, paths, smoothness and quiet standing remain required. A breadth-first primary-paper review runs in parallel to challenge the next architecture and training choices.

Run `python3 verify_payload.py` for portable integrity and allocation checks.
