# Native005 smoke guard preparation

This is CPU preparation for a fresh two-update quiet-priority integration smoke. It is now bound to the reviewed native005, source004, host004 runtime and host004 freeze. Root must still run the real read-only preflight before dispatch. Root alone dispatches. This guard cannot launch the 500-update allocation.

The smoke runs fresh standing, 32 replicas × 24 controls × 2 updates, then final constant-command and stop diagnostics. It starts from the exact original 1971 checkpoint. The schema is `direct315_extended_native_v4`; the smoke selection remains quiet_priority with temporal/spatial weights 0.1 and quiet temporal weight 1.0. The later normal-CAPS 500-update allocation requires its own explicit dispatch and same-source smoke admission.

## Ownership and scope

Reserved output: `direct_omni_train_smoke_004`; source: `direct_omni_train_source_004`; contract: `direct_omni_train_preparation_004`; host: `direct_omni_train_host_004`; pause: `forecast_pause_061`; unit: `hexapod-direct-omni-train-smoke-004-20260910.service`.

The prior owner is the completed quiet-priority 50-update pilot, invocation `721f1886478a4e518c7e0b741256413f`. Nine original campaign/job/pause receipts are copied byte for byte. Its six accepted phases and all twelve exact container names/IDs must match and be absent. The old source003, native004 and host003 identities remain distinct from the bound successor. The prior pilot's 0/48 quiet result does not block an integration experiment and does not become a quality pass.

The prior transient unit may have an empty collected InvocationID. The guard allows this only alongside the pinned completed receipts, zero exit/MainPID and inactive state; a conflicting nonempty invocation fails. The original remote terminal audit is preserved separately. That earlier audit is historical evidence, not a fresh probe.

## Preserved execution protection

The reviewed guard004 embedded cleanup/restoration program is byte-identical. Main control flow is AST-identical after only the coordination hash, scope description and fallback timer name change. Keep source009 supervision, two GPU locks, 90-second inherited AppReady deadline, four 600-second phases, 2520-second owner limit, 180-second stop timeout and 45-minute fallback. Arm fallback before stopping the original weather timers. Revalidate source, contract, checkpoint and host before pause and immediately before dispatch. Restore exactly the original active timers after owned cleanup.

Unknown or non-weather CUDA processes reject before any pause. The implementation does not broaden the user's weather override into authority over unrelated processes. No GPU work, service action or remote write occurred during this preparation.

## Checks and handoff

Run `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m unittest discover -s tmp/direct_omni_train_smoke_guard_005 -p 'test_*.py' -v`. The final bound guard has 37 passing tests, including actual mocked guard order, pending-pin rejection, all twelve cleanup checks, partial campaign/phase rejection, exact restoration, unknown Docker errors, foreign CUDA rejection and the unchanged embedded restorer. `DRAFT_READINESS.json` and `GUARD_DELTA_DRAFT.patch` record the checked scope. `FINAL_BINDINGS.json`, `CPU_READINESS_FINAL.json`, `test_output_final.txt` and `GUARD_DELTA_FINAL.patch` preserve the final binding and independent rerun separately from the draft. Guard and actual frozen host both import under Python `-S`; no remote input preflight is claimed here.

The copied native004/host003 freeze manifests establish prior input identities; they do not replace root's complete prior source audit. The host's new input verifier checks complete new native/source/supervisor trees at dispatch.

## Central project site handoff

All files here are temporary preparation. Eventual publication must add a bounded `site/updates/` record and update the relevant registry/status under `docs/PROJECT_SITE.md`. Suggested message: “Prepared a two-update compatibility smoke for the explicit 500-update baseline.” Keep Stage 2 incomplete, preserve the failed quiet result, and do not describe preparation as an executed smoke or trained improvement. Root owns tracked changes and deployment.
