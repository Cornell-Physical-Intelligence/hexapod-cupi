# Learning admission001 terminal audit successor002

Read-only terminal identity, byte-integrity, cleanup and verified-fetch tools for the exact consumer003 admission allocation. This utility does not launch a GPU workload, stop or signal a process, mutate source, score the numerical recovery ledger, or allocate PPO. The PPO owner performs the numerical ledger review after transfer; root owns any later learning decision.

Pinned inputs are source009 (926), consumer003 (51), host001 (3), guard001 (11), observation005 (160), bridge001 (18), and the exact 550-file admitted study package. The target is `reference_learning_ppo_001`, unit `hexapod-learning-ppo-admission-001-20260910.service`, invocation `d161f77036f346d2b1abe244d95b2dbe`, pause051. The script refuses active or mismatched units and any later learning allocation in the shared output. A collected empty systemd InvocationID is accepted only when the complete exact-unit journal contains the unique expected historical invocation, its Started record and terminal Consumed accounting record. Unknown or newer invocations still reject.

On termination it checks every frozen manifest and complete tree, exact asset bytes, reviewed host input receipts, all allocated job names and recorded IDs, and the pause's exact prior timer restoration receipt. A failed early phase is preserved with later phases explicitly unallocated; it does not invent three completed jobs. A completed admission must contain all three job IDs. Contact-audit JSON files are not counted as jobs. All raw output files, ledger NPZ files, clocks, 400 Hz data, logs, reset history and pause/preflight evidence are inventoried; the unchanged asset files are verified and referenced rather than copied again.

`fetch_verified.py` transfers to temporary files, verifies size and SHA256, and only then installs each immutable final payload. Existing differing files or interrupted transfers require review. It prioritizes the recovery phase so the numerical reviewer can begin promptly. No original file is compressed, truncated or rewritten. A terminal numerical or infrastructure failure remains a failure.

Seven standard-library tests passed, including active/wrong invocation, unknown container absence, changed/unlisted input, symlink, unsafe fetch path and incorrect restoration rejection. The first syntax-check failure is retained in `tests_initial_syntax_error.log`; the bracket was corrected before the passing test run and before any terminal audit execution.

Execution after the exact unit is terminal:

```sh
mkdir /path/to/reference_learning_ppo_results_001
ssh spark python3 - < audit_remote.py > /path/to/reference_learning_ppo_results_001/remote_audit.json
python fetch_verified.py --output /path/to/reference_learning_ppo_results_001
```

The audit writes only JSON to stdout. Store failures separately; do not treat a partial/empty output as a receipt. A complete local fetch emits `local_raw_verification.json`. The separate actual receipt must record its observed outcome; this preparation makes no claim that the running allocation has passed.

The original frozen audit001 rejected the actual completed transient unit because systemd had cleared its live InvocationID. That read-only failed attempt is preserved in the actual results directory. Successor002 changes only the audit identity proof and journal export, leaving every source/input/job/restoration check unchanged. Seven focused tests include missing, replaced and reused historical invocation rejection.
