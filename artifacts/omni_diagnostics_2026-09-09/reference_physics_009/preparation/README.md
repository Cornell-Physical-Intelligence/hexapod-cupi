# Exact reference008 → reference009 qualified-liftoff and angle-observer trial

Immutable preparation only; terminal results and outer guard/restoration are separate. This includes three runtime overlays plus source identity, the25-file frozen wave005 owner, six-file frozen observer owner, both independent PPO review receipts and exact parent reconstruction. No926-file parent duplication is included.

Parent map: `a73075ebdf5d6c986728b96117495b8de85adc8db62de1c41ffbe49eb6176d66`. Target926-file map: `04942c62de1f557d8f117f648f990b6dac69443d61c546529d05a4e2a7f0e01e`.

Fresh32×1000 standing and all32 existing quiet gates still precede1×2400 bounded wave. The wave dependency now distinguishes unloading from qualified two-sample/2mm measured flight and fails at the original nominal apex if unqualified. Early unqualified contact returns remain telemetry and cannot count as steps. Existing touchdown/landing/support/torque/progress/quiet gates and waveform timing/geometry remain unchanged. State-schema compatibility with a policy is not claimed.

The observer adds actual joint angles at every existing400Hz update, stable named order and exact control-endpoint angle/rate equality. No new physics steps, solver settings, targets or substituted metrics are introduced. Source008's rejection and unresolved reported-rate discrepancy remain facts; this preparation is not a new physics outcome.

Integration preflight verified the exact parent/dependency maps, unchanged config/readback function AST, pinned16-file runtime and exact plan. Actual008 admission cannot authorize009; a temporary synthetic unit receipt tests same-source+quiet binding and is explicitly not runtime admission. Wave owner28 tests, independent targeted8 tests and observer local/independent11 tests passed. The preserved earlier test-log failure is a fixture baseline assertion, documented by the owner; runtime bytes did not change for that correction.

Verify without writing a source tree:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B reconstruct_source.py --parent-source /path/to/reference_source008
```

Add `--output /fresh/reference_source009` only when materialization is required. The recipe checks all parent/overlay/target hashes, rejects extras/overwrite and launches no job. Root alone owns guarded dispatch and exact cleanup/restoration. This preparation remains unchanged after a terminal result.
