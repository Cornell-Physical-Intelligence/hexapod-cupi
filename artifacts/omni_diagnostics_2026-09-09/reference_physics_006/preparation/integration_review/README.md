# Reference006: one velocity-iteration standing comparison

This fresh standing-only trial changes **only articulation TGS velocity iterations from 4 to 1 relative to source005**. External forces remain enabled every internal position iteration; position iterations remain16, stabilization remains disabled, and geometry, motors, reset noise, C2 startup, contact sensor settings, physical/quiet gates, source-pinned controller and400Hz observer are unchanged. There is no wave or PPO launch and no production adoption.

The motivation is a mixed actual005 result: the maximum base position/velocity discrepancy fell below5mm for all32 environments, but the unchanged quiet scorer failed2/32 due to reported joint-velocity RMS exceeding0.03rad/s. Source005 therefore is not promoted. Environment6 and29 show RMS0.0350704/0.0343595rad/s in `revolute_2_1`, even though their16-second position ranges are0.000562/0.000927rad. Their reported velocity integrals differ strongly from observed position deltas; those diagnostics do not replace the failed quiet gate.

NVIDIA recommends roughly one TGS velocity iteration and explains the distinct position/velocity solver semantics. This motivates testing the setting, not predicting a pass. See [Omni Physics solver guidance](https://docs.omniverse.nvidia.com/kit/docs/omni_physics/latest/dev_guide/simulation_control/simulation_control.html#physics-solver) and [PhysX solver documentation](https://nvidia-omniverse.github.io/PhysX/physx/5.6.1/docs/RigidBodyDynamics.html#solver-iterations).

`solver_comparison.PROTOCOL` binds exact005 source identity and the4→1 intervention. The inherited005 code still initializes the explicitTrue force-timing option from the SDK constructor'sFalse default; this is the unchanged baseline setup, not a second new intervention. The before/after articulation configuration must be exactly16/4→16/1. After environment construction, readback requires all32 articulation roots at16/1 and enumerates608 rigid-body iteration settings; any missing, unknown or higher body iteration value rejects the comparison. Scene readback still requiresTGS, stabilizationFalse and force timingTrue. These are resolved USD authored/fallback values, not native internal solver iteration counters. Full cloned-stage traversal/schema fallback has not yet been read in a runtime capture; the new readback is an explicit fail-closed first-run gate. If it rejects, a separate failed-readback file preserves available rows and the exact error.

The predeclared analysis compares every environment over control boundaries200–1000: unchanged50Hz quiet metrics and every earlier physical gate;400Hz link/COM left/right/trapezoid and old50Hz velocity integrals; full named-joint reported velocity versus measured position changes; requested/applied torque extrema including initial and early settling samples; and contact/reset evidence. Results are reported against005 and retain004 context. No environment, integrator or metric may be selected for a favorable verdict. The old5mm progress threshold is unchanged; its standing diagnostic remains separate from existing standing admission.

Five focused tests verify the one-field articulation mutation, required baseline, complete body/root readback and standing-only mode. The parent host, entrypoint, observer and all old metrics are preserved byte-for-byte. Root owns fresh source verification, guard006, pause032 and restoration. Any further solver adoption or metric definition requires a separate explicit decision and version.
