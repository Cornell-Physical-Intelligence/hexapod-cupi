# RR diagnostic 002 — host import correction, CPU preparation only

This is the compact, separately frozen correction to the actual001 host preflight failure. It contains **no GPU trial or physical admission**. Root separately preserves001 preparation, the actual preflight rejection and both guard lineages.

The exact proposal constant moves from the NumPy helper into a standard-library-only metadata file. The host imports that file; the Isaac-side helper imports the same constant. **The wave/controller equations, host launcher, physical gates, solver settings, geometry, observation lineage and one-case schedule are byte-identical001.** The helper class AST and all proposal values are equal. See the [owner README](owner/README.md) and [exact delta](owner/DELTA_REVIEW.json).

- Source932 manifest: `bfb0bbde27fdfae1f2ddc7797ff12adf184c7cd68a8e637f5fce87f7e0e3cfa3`.
- Owner17 freeze: `54f216cc4b6f9696372b870d17ef34c3cf79eba25a5d0c7c5459d3f7688fb0c9`.
- [19 tests passed](owner/tests_final.log), including real Python `-S` reproducing the old failure and passing actual new host import/preflight with NumPy explicitly absent.
- [Final no-App preflight](owner/INTEGRATION_PREFLIGHT_FINAL.json) binds source932 and rejects old standing admission and failed quiet. The draft count-display receipt is preserved and explicitly superseded.
- [Import evidence](owner/STDLIB_HOST_IMPORT.json) retains old stderr and new host output; no host dependency was installed.

The underlying proposal remains one fresh32×1000 standing/quiet screen and one1×2400 left-strafe trial, with a single0.5mm C2 RR anchor correction during its existing0.3s first-landing hold. Existing1N/five-support/1.6N m/flight/quiet gates remain. Arc003 lost RM support, so this is not an arc fix. No actor or old846/849 observation schema is adopted.

Use the exact source001 parent reconstructed from its earlier compact bundle; all parent/new hashes are recorded in [REFERENCED_INPUTS.json](REFERENCED_INPUTS.json).

```sh
python3 verify_payload.py --parent-source /path/to/exact/source_rr_preload_001
python3 reconstruct_source.py --parent-source /path/to/exact/source_rr_preload_001 --output /fresh/path/source_rr_preload_002
```

Both utilities use only the standard library. Reconstruction requires a fresh output, verifies all932 output payloads and re-verifies the immutable parent. Root owns any new002 guard, remote preflight and dispatch; those are not claimed by this preparation.
