This successor fixes the host-import failure preserved in [preparation001](../reference_rr_preload_preparation_001/README.md). It moves the unchanged proposal dictionary into a standard-library-only module; the numerical helper and host contract import that metadata separately. The wave controller and host launcher bytes remain identical, the helper class AST and proposal values are exact, and928 parent payloads remain unchanged. No physical equation, gate, torque or geometry changed.

The owner runs19 tests against the assembled932-file source. Root independently repeated the three new host/import/identity checks, including a real `python -S` process without NumPy, and had already repeated the unchanged controller's16 tests. Eight tests exercise the exact guard restorer. [Root review](ROOT_REVIEW.json) distinguishes those test scopes.

The [remote preflight](remote_preflight.json) verifies every932 source file, exact guard, prior diagonal cleanup/restoration and fresh names. Its initial operator check expected a nonempty live invocation ID after the prior transient unit had been collected; that read-only assertion and original script remain preserved. The final check requires inactive/success/exit0, rejects any conflicting live invocation, and binds five exact historical campaign/job/pause payload hashes plus all six owned-name/ID absence checks. Source, guard and physics did not change for that operator check.

Root dispatched `hexapod-rr-preload-002-20260910.service`, invocation `7f32b909f3b5411a8f4a57d0200519ef`, under pause048. Its maximum is1320 s plus180 s stop allowance, with30 min fallback restoration; each of fresh standing and the single strafe case remains bounded to600 s and90 s AppReady. This preparation records dispatch, not physical success. The one-time RR0.5 mm correction and its first-landing hold remain the exact diagnostic described by001; there is no arc or old-policy adoption claim.

`preparation/` preserves the24-file reconstruction bundle; `guard/` is the exact two-file guard. Reconstruct932 source files from the frozen931-file001 parent using its supplied script and verify every hash. `python3 -B verify_payload.py` verifies this outer publication without GPU actions.
