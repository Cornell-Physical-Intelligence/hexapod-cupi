# Paused research handoff for James

Start with [James's handoff guide](../../../docs/JAMES_HANDOFF.md). This archive preserves 32 preparation/review directories, including 618 original files that were previously under local temporary paths. It does not adopt their code or admit a native run. Current main and previously published results remain authoritative.

`PREPARATION_INVENTORY.json` records each source directory, copied path, original freeze where one existed, byte size and SHA-256. Previously unfrozen guards/operator preparations are immutable snapshots here and remain disabled drafts. Old prose describing a then-pending run remains historical; native_standing32_005 ultimately rejected, and research is now paused.

`operations/` preserves the actual pause, worktree inventory and archival-stash checks. The first combined timer/service stop returned nonzero after stopping the timer because its transient service had already disappeared. The separate verification establishes both units absent/inactive. The original error remains preserved. No running training process was killed and no external reservation was released.

The continuation automation is paused; the final Spark readback records no training/GPU workload. The external job blocks remain in place. The large raw traces remain explicitly hash-inventoried on Spark, and the earlier pre-main stash is preserved separately under the remote historical tag identified in the guide. Generated caches, private app configuration, credentials and redundant local raw copies are not added to Git.

Run `python3 -B -S verify_bundle.py` from this directory or invoke its absolute path. It verifies copied bytes and frozen identities without importing the stored runtime, launching a review, contacting Spark or executing any operator. The archive's new hash map does not convert a proposal into tested native behavior.
