# Canonical PPO guard002 — disabled preparation

This local preparation binds frozen PPO source003 (74 payloads, `970cd1766366c9bdba3239205695e998da80819d2283ce0e261a3b265ca48183`), host004 (38 payloads, `efd7169161fa7586fd76741f129a66f66119253e510214080e6d4f33628e2130`), standing source005 and the actual native6 one-robot pass. It has not launched a job or changed Spark. Root is the sole dispatcher.

The prior 32-replica run native_standing32_004 was interrupted by an unrelated CUDA worker after reported control900. Its initial native state remains unfinalized; this is not a physical verdict or a standing admission. The fresh native_standing32_005 invocation c5397bd5a8dd4640899ad3dec2c30f47 is now the pending prior owner. All actual32 admission, cleanup and enabled external binding fields remain null. `require_final_bindings()` refuses before external calls, pause creation or launch. A future actual accepted 32 result requires reviewed bindings; no rejected or interrupted run is substituted.

The allocation remains fresh canonical32×24×2 PPO, preceded by1000 neutral controls in the same process:1048 controls/8384 substeps total. No checkpoint input or automatic continuation is exposed. This is learning integration only, with quality and Stage2 flags false. Host004 independently validates native6/32 same-source admission, actual32/0 solver readback, the full native and PPO inputs, and the real checkpoint reload.

Reserved output `canonical_ppo_smoke_001`, owner `hexapod-canonical-ppo-smoke-001-20260910.service`, pause017 and fallback `hexapod-canonical-forecast-restore-017`. Names must be checked fresh before any eventual allocation. Operational limits remain1200s phase/90s AppReady,1320s owner/180s stop,25-minute fallback. Source009 ownership and exact cleanup remain host-owned; both locks and actual unknown-CUDA checks are unchanged. The mask-aware reservation function is copied exactly from frozen standing32 guard005: it rechecks original13 content hashes,31 user masks,4 system forecast masks, directory entry blocker and live queue-lock inode owner. MASK_ADOPTION.json proves exact function parity and binds the root-passed policy dcb62320; the full prior failed file-gate attempt remains in its owning frozen native guard. The per-job restorer never releases the persistent reservation.

`PIN_DELTA.patch` records the source/host/names/readable-error changes from the preserved draft001 runtime. Historical input manifests and draft hashes remain labelled under inputs. The native controller, gates, rendering and physical model are untouched. The original pending CPU run `tests_pending_001.log` reports23 passing tests; the adopted mask-aware `tests_mask_pending_002.log` reports28 passing tests, covering input forwarding/disabled admissions, original full frozen maps, original reservation/restorer parity, literal NVIDIA CLI, actual main order, and dispatch/failure cleanup using external-effect fakes. No full admitted actual32 preflight is claimed.

This directory is still a draft while root decides the next actual32/reservation bindings. Eventual publication follows `docs/PROJECT_SITE.md`; root owns the central STATUS/registry/update record and Git. No shared documentation was edited here.
