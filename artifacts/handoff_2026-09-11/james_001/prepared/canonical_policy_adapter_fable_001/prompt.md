Implement a small, CPU-only candidate for a new detailed hexapod PPO's observation/action adapter. You are the implementation partner, not the decision maker. Return final implementation code and meaningful tests in fenced blocks, with a concise explanation. No tools/GPU/files/git are available in this call. Do not claim execution. No internal reasoning stream is wanted.

Project: canonical 19-body/18-direct-joint, 7.466 kg robot. Native import/full inertia/limits/all153SDFpaths now pass. Separate small-torque and support admission precedes any learning. Existing C-study actors/tasks/checkpoints must never be loaded or relabeled. Existing user requirement: smooth omnidirectional translation plus both turns/arcs and quiet standing. Future terrain and perception follow; do not put unobservable ground truth into the actor.

Concrete bounded deliverable: two pure NumPy files, adapter.py and test_adapter.py. No Isaac imports, optimizer, rewards, chosen joint scales/gains, navigation, or task registration. This code is only a proposal that root will independently review/test. Future publication must follow docs/PROJECT_SITE.md; this response cannot publish anything.

1. A batched stateful target pipeline with caller-supplied unique18joint names, finite lower/upper limits, neutral positions and positive per-joint action scales, dt=.02 and maximum target step=.040rad. Inputs are normalized actor actions; preserve raw action, clip to[-1,1], derive and limit requested position, apply the slew limit relative to previous executed target and persist actual executed target plus previous clipped action. Do not choose action scales; configuration is explicit. Invalid shapes/nonfinite values must reject before mutating state. Caller may reset a selected row subset to measured finite joint positions, clearing only those rows' previous action; define out-of-limit handling explicitly. Return owned snapshots, never mutable aliases to state or inputs. Do not add deadbands, static hold arbitration, filters, acceleration limits, reward changes, or gates.

2. An observation-history helper: five consecutive81-value actor frames in oldest-to-newest order, each [body angular velocity3, projected gravity3, command(vx,vy,wz)3, joint position error from neutral18, joint rate18, previous clipped action18, actual held/executed target error from neutral18]. Normalize through explicit caller-supplied positive scale vectors, with no guessed physical limits. Critic can separately append3 privileged body linear velocity values to the flattened405 actor history. Clearly distinguish privileged critic data from actor data. No simulated contact or terrain truth in actor. Selective reset repeats the first valid frame only for reset rows; neighbors' history must remain unchanged. Caller order is observe the completed physical interval, then compute/apply the next target. Do not let an observation claim a target that has not been executed.

3. Fixed frame map: native robot body axes are +X left,-Y forward,+Z up; policy command axes are +X forward,+Y left,+Z up. Map native vectors to command frame as (-native_y,native_x,native_z). Apply this only to body-frame vectors, not joint scalars or already-command-frame commands. Explain caller responsibilities for world-to-body rotation and measurement origin; do not hide those in the fixed frame map.

Tests should expose real risks: reject duplicate/misaligned names and invalid/nonfinite arrays transactionally; directional frame fixtures; asymmetric joint limits; slew after saturation; changing a returned snapshot cannot mutate state; selective reset never changes another row/history; actual executed target appears in the correct next observation frame; critic-only velocity cannot change actor history. Use unittest. Keep implementation readable and small. Return precise schema/timing assumptions and issues still needing real native integration. The 405/408 candidate is not an adopted frozen task contract yet.
