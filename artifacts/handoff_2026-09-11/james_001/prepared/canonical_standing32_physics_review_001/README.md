# Independent canonical standing source and dynamics review

The 32-replica result remains rejected: 11 rows pass, 21 fail the unchanged all-post-settle six-toe requirement, and 7 of those also fail SDK joint-velocity RMS. Requested torque never exceeds1.160Nm in the embedded actual report and saturation is zero. More host time cannot admit this completed physical result.

This review independently reads and hashes the complete actual one-origin 8000-step arrays and contact stream. It reconstructs every interval-angle sample and replays every PD requested/applied torque exactly. For 32 replicas it uses the root-audited terminal report only; the separate sensor-agent analyzer owns raw 32 arrays, detailed support events and native root/grid attribution. No full32 replay is claimed here.

The actual single-origin LM/RM tibia rates have signed SDK means−.02295/−.02302rad/s while joint-angle interval RMS is.00167/.00183rad/s and the400Hz position ranges are41/53µrad. Over16seconds the SDK-rate integral differs from net angle displacement by−.367/−.368rad. This is a demonstrated difference between two recorded channels, not evidence that actual angle motion is zero or that all 32 failures are a rate bias. PD derivative torque RMS attributable to these SDK channels is only about.00243Nm. The 32 reports also retain measurable angle excursions and brief six-toe failures, so replacing or ignoring the SDK channel would not establish admission.

The source maps actual articulation paths to each grid reset, names each contact sensor to its corresponding articulation and body, and indexes named gains by native joint order. Contact point and link transforms share global coordinates; no simulation subspace origin is installed. Each400Hz servo reads current nativeq/dq, sets external force usinguint32 articulation rows, confirms readback, steps physics and records the following state. The target is always exactly zero. No concrete row indexing, double-origin subtraction, held-target delay or torque-cap defect was found by this bounded read.

A CPU rounding experiment translates1024 tibia patches from 21 actual one-origin sample times and their corresponding float32 poses to all 32 declared grid coordinates. All 32768 existing classifier results agree: zero toe/shaft flips, maximum shape-coordinate difference0.892µm, minimum sampled accepted cap margin977µm. This rejects the sampled host classification rounding explanation; it does not model native SDF contact generation or prove solver translation invariance. It does not cover all actual32 contact points.

The current source sets TGS external forces every iteration but does not author articulation position/velocity iteration counts. Native scene min1/max255 position and min0/max255 velocity are clamps, not actual articulation iteration counts. The installed PhysxCfg readback says raising velocity iterations with external forces each iteration may improve velocity accuracy. The public [NVIDIA articulation schema](https://docs.omniverse.nvidia.com/kit/docs/omni_usd_schema_physics/latest/physxschema/class_physx_schema_physx_articulation_a_p_i.html) declares fallback32 position/1 velocity. Those online defaults are not an installed/native readback; do not call this16/4 or assume4/1.

The root-selected32/1→32/4 experiment is a reasonable bounded next diagnostic if the installed un-authored fallback is actually verified before physics. Preserve observed position32, write only velocity4 plus explicit position32 in a new runtime layer, confirm every actual articulation path before/after reset/end, and preserve native geometry, material, servo, step size and every physical/quiet gate. Same-source one then32 remains required. No gain change, channel replacement, physical acceptance or causal proof follows from this preparation. Authoring/readback proves the requested resolved configuration; it is not instrumentation of every internal solver iteration. Failed results and unknown runtime behavior must remain visible.

Other untested mechanisms include native SDF/contact numerical sensitivity to placement, different island packing, and reset warmstart history after gravity warmup. A source defect has not been demonstrated. A changedsolver outcome may justify the next same-source screen; it does not isolate those mechanisms or prove hardware behavior. If the solver variant still fails, inspect the already-preserved actual force/angle events before designing a new probe. Do not run a broad gain sweep automatically.

Reproduce from repository root: `.venv/bin/python3 -B tmp/canonical_standing32_physics_review_001/analyze.py`. Inputs are referenced and hashed in `report.json`, not copied. This directory is CPU-only, no native changes or GPU execution. Fable MAX review and future source004 diff review are separate evidence, not included before their results exist. Root owns any publication and the required STATUS/PLAN/NEXT_RUNS/site update record and build.
