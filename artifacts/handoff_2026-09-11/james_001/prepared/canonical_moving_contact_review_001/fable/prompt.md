Review one consequential design choice for a canonical hexapod simulation training bridge. Use no tools. Give only your final critique, at most500 words; distinguish a concrete implementation bug from a proposed experimental choice. Do not suggest a large sweep or hardware-identification wait.

Context: the user wants a steady sensor deck and smooth all-direction walking, then terrain/perception. Current approved detailed direct-drive model7.466088235kg,19bodies18joints153 exact SDF colliders. One-robot supported standing passed under TGS32/0; matching32 acceptance and zero-command2-update PPO integration are separate prerequisites. No old controller/model is reused. Provisional48V software actuation,400Hz PD with50Hz held targets,1.6Nm applied cap,0.040rad/20ms limiter,0.5% per-joint requested saturation allowance. All numerical physical and Stage2 acceptance gates remain unchanged. We are selecting a simulation runtime termination policy, not declaring gait/physical/hardware admission.

Exact current PPO bridge forbids any nonzero command and requires all six distal_contact booleans at every one of the eight substeps. Native session already checks finite raw states, measured joint position/speed limits, plate origin height>=.055m, no nonfoot contact and non-toe mesh floor clearance>=-.001m, caps applied effort, keeps source-bound raw contact/torque/counter/partial failures. Bridge adds all-eight held-target/counter checks and fixed-horizon requested saturation. It has no auto-reset: a failure ends the bounded experiment and preserves the prefix. A next bridge replaces command/reward/six-contact-only seams explicitly.

Exact draft contact_facts(before[N,6],current[N,8,6],valid[N,8],phase,moving_rule) rejects missing/wrong dtype/evidence. neutral_standing requires6. moving_or_stopping accepts a named caller-proposed min count1..6 and returns per-row proposed_count_check. It does not enforce the returned boolean. Contact activity stays the original accepted distal mesh classifier with resultant force>1N; exact zero-force/zero-normal/separation0 slots remain preserved inactive. A force off/on event does not prove geometric flight or landing.

Authoritative docs/TRAINING.md§6 formal Stage2C: deterministic nominal seed60,25warmup/500requested,0.040limiter,commands0/.16/.20/.30 forward; yawRMSE<=.080rad/s,normalized moving deck composite<=1,zero falls/timeouts,achievedspeed>=.24at.30,original motor duty/burst/rawpeak constraints. hexapod_eval/gates.py mirrors exact graders; neither specifies a moving contact count. Existing broader full-direction/quiet evaluation remains additional mission work. docs§Stage2G warns historical five/four/three support scheduling plus heavy deck costs favored shuffling; it used a3-support reward floor as a safety bias, NOT an existing3-contact hard gate.

Proposed smallest executable NEW contract: explicit experiment phase independent of current command value; neutral admission retains6, all moving/stopping controls use a provisional minimum3 accepted distal contacts at EACH400Hz sample, including c=0 while a foot returns. Failure records exact first bad row/env/count/mask and stops before another action/collection commit; existing step_control returns8samples, so this is after-held-interval detection, not falsely immediate400Hz abort. Invalid evidence always fatal. No gait schedule, static support polygon, margin, preidentified swing leg or hard maximum airtime. Both tripod and sequential swing allowed. Exactly-zero raw force alone is only force-support loss telemetry. Qualified lift/landing requires separate geometry/time/slip evidence; no circular mandatory all-leg prequalified gait before first learning. Initial same-source neutral pass is still mandatory. Final dedicated quiet evaluation still uses6contacts and unchanged quiet metrics; zero command never auto-promotes phase into settled standing. All current safety predicates remain independently enforced. Additional severe tilt termination from old docs is considered separately, not silently inherited or changed here.

Question: Is3 a defensible explicit provisional bounded training termination choice versus allowing any nonempty contact (>=1), given steady-deck mission and no proof that count alone guarantees stability? Identify the smallest counterexamples/tests and any way this creates an unnecessary training barrier. Does measured foot lift/return need to precede all moving PPO, or can it remain event qualification within the first bounded no-auto-reset diagnostic without falsely claiming walking? Do not infer3 is an existing acceptance threshold. Recommend one exact choice or a concrete minimally different alternative with its tradeoff.
