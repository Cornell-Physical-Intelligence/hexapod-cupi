# Smallest next moving integration after the canonical smoke

**CPU proposal; no native admission, allocation authorization, performance result, or Stage2 completion.** The immutable parent is PPO003 over detailed direct-drive standing source005. Matching native32 and the zero-command two-update smoke remain prerequisites. This package prepares the next seams without changing either parent. The existing all-direction objective001 remains a proposal; its actual Fable MAX review is preserved separately. This package adds a bounded MAX review of composition/contact/recording decisions.

## What can be reused and what must change

Keep the actual 7.466088235kg model, solver32/0, materials, full collision geometry, named joint order,400Hz servo,50Hz holds,1.6Nm cap,0.5% requested saturation allowance and actual0.040rad/20ms target limiter. Keep405/408, actor exclusion of native contact/linear velocity, empirical normalizers,512/256/128ELU, initial standard deviation0.02 and target scale0.10rad. Narrow exploration may not lift a foot; record sampled/clipped/emitted variance and learned standard deviation before proposing a change. No gait clock, posture clamp, action filter, old actor or fake velocity is introduced.

| Exact parent seam | Required isolated successor |
|---|---|
| `canonical_direct_ppo/native_bridge.py`: zero command check, six contacts at every substep, `quiet_reward`, old command used by post-step history | New moving bridge. Preserve `_validate` and actual `govern` unchanged; replace only explicit command/contact/reward composition. Use private c[t] for reward; install c[t+1] before the next actor frame, after a complete hold. |
| `standing_session.py`: local `contact['patches']` is serialized but not exposed; numeric `observe_control_substeps()` exists | New read-only accessor of all eight original post-classification patch packets, each with sequence/counter/classifier acceptance. Capture the same lists before serialization, retain partial failures and raw output. No recomputation from resultant forces, reclassification or native steps. |
| `runner.py`: imported `UPDATES=2`, final counts48/40, `learn_smoke`/protocol/strict reload | Version allocation and schema; derive every count from the validated allocation, including checkpoint controls, optimizer counts and saturation denominator. Changing `UPDATES` alone is insufficient. Preserve real RSL collection and strict reload; no timeout/reset implementation by implication. |
| `native_entry_adapter.py`/`native_contract.py`: neutral8000 + policy384 solver declaration and smoke receipts | Bind the new source, command/reward/bridge and allocation; retain neutral8000, derive final controlled steps, verify the same32/0 before/after. Reuse exact neutral scorer, lifecycle error sealing and lineage. |

## Runnable CPU seams supplied here

`command_history.py` composes the unchanged observation oracle and command bank. Its initial valid frame repeats five times, the existing explicit initialization convention. It never claims five measured past commands. `begin` checks the private command and previous actual target-minus-q before action. `complete` commits one history push only after eight completed samples and correct c[t] attribution. Program rollover advances selected command epochs without physical or history reset. Pending holds survive a CPU checkpoint; production tensor serialization remains the existing native runner's separate strict lineage seam.

`reward_packet.py` packs all eight samples into unchanged objective001 mathematics. World COM velocity is shifted to the body-root origin, rotated into native body, then mapped(-y,x,z). Accepted native quaternions are numerically normalized for the reward's rigid/unit-vector preconditions; raw actor/native evidence remains unchanged. `neutral_interval_context` obtains the previous actual target delta and initial material-point poses from the final two adjacent neutral controls. Actual interval angle rate uses q endpoints, never rawSDK rate substitution. Earlier terminal flags, incomplete intervals, other held targets and missing slip are rejected with no command advance. This packer is **not** the native physical validator: every source005 joint/torque/contact/clearance check still runs before the next target.

The proposed patch accessor ABI is `{sequence, explicit_counter, classifier_accepted, patches}`; copies are paired to each numeric row. Original classifier acceptance must precede reward normal normalization. All inactive zero tuples remain recorded. Slip follows the same current material point into the previous link pose, not the movement of reported contact locations. Per active foot, the maximum tangent speed among nonzero-force toe patches is an explicit reward aggregation proposal; it is sensitive to small-force outliers and needs native review. No new force threshold is invented. A foot marked active without its accepted force/point evidence is an error. This API is not present in frozen source005.

## Standing, moving and stopping are distinct contracts

Six-foot support remains mandatory in the unchanged fresh neutral admission and original formal quiet window. The new moving rule is **unresolved**, including its numeric minimum and flight/landing qualification. `contact_contract.py` provides counts and force-contact transitions with an explicit caller-supplied *proposed* count. Test counts4/5 are synthetic fixtures only. There is no default, scheduled touchdown or claim of qualified flight. Empty/invalid contact packets cannot become admitted motion. A zero command during a stop transition does not automatically switch back to six-support: a real lifted foot may still be returning. The final declared quiet window is scored independently by the unchanged original gates.

Before a moving pilot, root must review a measured lift/return discriminator using exact canonical distal geometry and raw force/point continuity, then freeze the moving predicate. A static support count alone does not identify distal separation, obstacles, touchdown force or body stability. No historical wave three/four/five-support rule is imported. Complete all original nonfoot, clearance, joint, torque, finite-state and reset checks independently of this decision.

## Useful next job and throughput limit

After matching32 admission and PPO003 smoke, the next proposed job is one fixed-level0 command-conditioned allocation,32×24×50 with no automatic physical reset:1200 controls,38400 transitions,1000 actual Adam steps,9600 policy substeps. This covers two complete12s command programs per row, including stops and reversals, but is not balanced over all25 strata. A shorter two-update nonzero-command packing test can isolate a native seam failure; it is not necessary solely to rerun CPU differences and does not establish locomotion. No automatic promotion/budget increase is proposed. True failures retain the complete attempted prefix and stop the bounded campaign; they are not silently removed or bootstrapped. Learning through falls later requires a separately tested collector/reset contract.

Full forensic recording is a measured cost: the earlier32 neutral session took585.27s, and its contact JSON was4.620GB; PPO's live neutral-prefix snapshot also copies that whole active file. These numbers are historical measurements, not timing predictions for source005. First compare native step/classification/serialization/flush/prefix timing. Use the already proposed lossless closed-block/prefix-hardlink design in a separate reviewed source successor if needed. No record thinning, geometry simplification or zero-patch deletion is adopted here. Training telemetry may later store scalar/component/action statistics and bounded event windows, but it cannot claim complete replay or replace formal acceptance evidence. Define retained fields, online checks, failure retention and exclusions before that mode runs. Initial native moving admission should preserve full evidence.

Evaluate the resulting exact checkpoint on objective001's32 held-out translation/yaw/arc/quiet cases with full reset-free traces and the original final10s quiet checks. Add a separately declared reversal evaluation before claiming reversal quality. Reward/command coverage, integration completion and physical/visual Stage2 acceptance remain different outcomes. Root owns the actual next allocation and dispatch; no native source or GPU state was changed here.
