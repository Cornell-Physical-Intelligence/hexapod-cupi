# Independent disposition of the actual MAX consultation

The one tools-disabled call completed successfully with `--model claude-fable-5-1 --effort max`, session `c220f1f9-e471-4be7-9ad1-a4e9b58133b3`. Exact prompt, selected input hashes, launch/exit metadata and final JSON are preserved in `fable/`. The response's main modelUsage entry is Fable5.1; the CLI also reports a small Haiku helper usage entry. No fallback model was selected by this worker. No internal thinking text or JSONL events are included; usage counters are metadata. The response is an independent critique, not authority or native evidence.

| Partner claim | Independent finding and action |
|---|---|
| `begin_control` can accept a stale command if caller supplies the same stale value everywhere. | True for the original standalone checker. New `CommandHistory.begin` takes no caller command: it reads the owned `CommandBank`, compares the emitted raw81 frame before empirical normalization, and retains a private reward command. Complete advances a copied bank and pushes exactly once. Tests cover nonzero signs, target-q with nonzero q, stale reward rejection and249→250 stop/599→600 rollover. `frames.py` does not construct the observation: the actual adapter frame is float64 then the actual actor cast is float32, matching the original checker. |
| First interval needs actual prelude target memory and pre-hold link poses. | Accepted. `neutral_interval_context` reads two adjacent actual constant-target prelude controls, validates counter/time and unchanged held targets, and provides the measured final poses and actual zero previous delta. Source005 exposes these numeric records. This is not a replacement neutral admission. |
| Inactive zero normals and wrong dt can corrupt slip. | Accepted and already handled in the new composition. Source005 first accepts raw patches; exact inactive zero tuples bypass the unit-normal helper. Active accepted normals are normalized after classification only. Material slip uses fixed0.0025s and counter-bound eight-packet pose pairing. Tests exercise accepted1.0005 norm, zero tuples, missing support evidence, stationary-link patch relocation, translating link and stale packet counter. |
| Resume from a command-bank checkpoint may silently reset physics/history. | Rejected as a requirement for this job. The proposal has no automatic or cross-session physical resume. An aborted campaign is terminal and keeps its prefix; a future fresh session starts a new declared command history. The CPU object snapshot tests only exact in-process command/history continuation, including pending holds. Production checkpoint/native restore is explicitly unimplemented. Native physics checkpoint support is not necessary for a fresh bounded allocation. |
| Fixed slip denominator rewards fewer active feet. | Confirmed. A new counterexample reproduces cost ratio5/6 for five versus six equally slipping feet. This effect is already explicit in objective001. The objective bytes are preserved; no denominator or1.2N threshold is silently substituted. Slip reward cannot supply support admission. The proposed maximum per-foot material speed is an additional unadopted aggregation needing review. |
| Moving predicate should be offline only, not an online gate. | **Rejected.** A reviewed moving safety contract must run online before the next target; original joint, nonfoot, clearance, finite-state, torque and reset checks remain active. Offline replays complement this; they cannot authorize stepping through an invalid support state. The numeric moving count and measured flight/return criteria remain unresolved. |
| Rerun neutral32 with accessor solely to measure its cost; native checkpoint/resume is needed because a600s budget is exceeded. | Not adopted. Root's current admission is independent and already running. Do not repeat expensive native work for CPU-only changes. Verify accessor/JSON parity and cost during the first necessary reviewed composition. Operational deadlines are explicitly versioned, separate from quality gates. The partner projected only9600 policy steps and omitted8000 neutral steps; simply reusing its702s estimate is wrong. No runtime ETA is claimed. |
| Zero-command root jitter versus requested speed is a useful diagnostic. | Accepted as read-only analysis of available same-source native evidence, with rawSDK and interval-angle/root displacement kept distinct. It does not automatically change the reward bandwidth or certify tracking. |
| Record by updating the old objective entry. | Rejected. The central framework requires a **new append-only** bounded record at eventual publication, not modification of a historical update. Root owns that integration. |

The final22 tests independently exercise the accepted seams and reproduce the retained slip incentive. They use exact copied oracles and synthetic state packets, not native physics. Missing source005 patch accessor, reviewed moving contract, native bridge/runner allocation wiring and actual measured lift/return remain explicit. The planned command bands are requests, not admitted speeds.
