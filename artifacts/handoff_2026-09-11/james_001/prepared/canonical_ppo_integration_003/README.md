# PPO003: source005 solver-aware composition, 32-robot admission pending

This CPU preparation binds exact canonical standing source005 (`c87f2d340c935cd947c7aed3f3dcd7c9a9965b6e19d6726546abb560b27ab131`,109 payloads) and the actual single-robot standing state `33920a4af296ed124643fe61a16f4840c19949b5f6e434327d7cd7ea9953cd3f`. **The same-source 32-robot state remains null and native dispatch remains disabled.** The single-robot pass does not admit PPO, larger layouts, hardware or Stage2. Root owns actual input binding and dispatch.

The source005 setup uses authored32 position /0 velocity iterations with before/authoring/reset readback, read-only legacy friction, link-COM velocities and filtered floor-force matrices. This consumer preserves that exact source's scene, geometry, reset, servo, neutral-control loop, exception and finally code. The source005 final tail is matched exactly before composition and reversed exactly in tests; no extra physics step is added.

The new tail verifies and saves `after_neutral_steps`32/0 after 8000 neutral steps, before the live-prefix callback can construct an actor. The immutable neutral prefix copies its own solver, legacy-friction, native-joint-order and contact-view receipts; its `session.json` declares 8000 diagnostic samples. After 48 policy controls, the original session close is followed by source005's `after_controlled_steps`32/0 readback. The final session separately declares 8384 samples. Terminal validation requires both complete root sets/readback phases, both declarations, sealed files and the diagnostic NPZ channel members. It also requires unchanged initial/neutral readbacks. The member checks are standard-library host checks, not a claim of independently recomputing every numerical trace; exact native runtime retains shape/finite checks and full raw capture.

`solver_recipe` and `solver_diagnostics` join the protected native helper imports, including imports made lazily during `main`. An independent reviewer found no runtime blocker and independently passed 18 focused checks plus the full-entry Python `-S` check. Its test-portability note was corrected using preserved parent manifests; all 8 new tests passed again in `TESTS_source005_portable.txt`. The runtime did not change after the 39-test run. The separate review receipt is bound outside this owner bundle.

The private `source005_solver_diagnostics.py` is copied **byte-identically** from source005 solely to reuse its declaration and legacy-friction validator in CPU prefix/host checks. `verify_inputs` checks this equality against the bound native source. No friction setter, zero-friction requirement or replacement physical gate is introduced. Nonzero finite legacy values are accepted and preserved.

Actor, critic, adapter, frame conversion, reward, bridge, rollout, RSL binding, 40 Adam minibatches and strict two-checkpoint reload remain byte-identical to PPO002. The current contract remains 405 actor /408 critic, five oldest-to-newest 81-value frames, actual held-target-minus-q, raw SDK dq, critic-only body linear velocity, [512,256,128] ELU networks, initial standard deviation 0.02 and 0.10 rad target scale. The actual 0.040 rad/20ms limiter, 50Hz hold /400Hz PD, all eight support/torque/clearance/joint checks and full failed prefixes are unchanged. The 48 policy controls use zero commands and no automatic reset: this is learner/serialization integration, not all-direction or quiet-policy qualification.

**39 CPU tests pass**, including all 31 inherited behavior tests and 8 source005 composition/diagnostic tests. The tests execute the real RSL two-update and strict-reload paths with clearly labeled CPU fixtures, inspect exact entry restoration/exception/finally preservation, execute the ordered solver→callback→close→solver tail, reject old/foreign helpers and changed/missing solver/diagnostic evidence, and distinguish 8000 from 8384 samples. `TESTS_source005_attempt_001.txt` preserves two failures caused by inherited synthetic standing fixtures missing the newly required solver receipts (40 of 42 executions passed; three imported tests were also inadvertently repeated). Fixtures were updated to the explicit source005 protocol and duplicate discovery removed; no acceptance rule was relaxed. `TESTS_source005_attempt_002.txt` records the passing 39-test run. `ENTRYPOINT_CPU_CHECK.txt` additionally exercises the actual full source005 module and preflight-only entry under Python `-S`, with a labeled identity fixture and no NumPy/Torch/Isaac imports. It is not actual native input admission.

Replay from the repository with the existing RSL test dependency map:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m unittest discover -s tmp/canonical_ppo_integration_003 -p 'test_*.py' -v
python3 -B -S tmp/canonical_ppo_integration_003/verify_bundle.py
```

`HEXAPOD_CANONICAL_STANDING_SOURCE` may point at an exact published source005 copy. Prior README, bindings, changed tests/runtime and freeze are preserved under `history/ppo002`; other copied historical logs/binding receipts retain their original bytes and concern earlier sources. `SOURCE005_BINDING.json` and `SOURCE005_COMPOSITION_DELTA.patch` identify this successor. No compact codec, recording optimization, geometry simplification or classifier change is adopted here.

All later repository publication must follow `docs/PROJECT_SITE.md`: root supplies the bounded central record and appropriate execution/design evidence. This preparation makes no new execution claim.
