# Actual standing32 rejection: measured support and rate transients

Eleven replicas pass;21 fail measured six-toe support, and seven of those also fail the original SDK-rate RMS threshold. The original native acquisition completed8000steps; its false standing verdict remains separate from the host600s timeout. Neither is removed by a larger wall budget.

All74 missing-support events affect exactly one foot. Each missing foot reports zero resultant and128 inactive exactzero patches, while every other foot retains at least10.74675N. Counts are RM32,LM25,LR9,RF6,RR1,LF1. Thus middle legs account for57/74 events, but the phenomenon is not restricted to them. No event has all six forces zero, and affected-leg SDK velocities are never all zero. The proposed all-body sleep/contact-cache signature is absent; this does not identify what produced128 inactive slots.

For example, env11/RM at4.0100→4.0125→4.0150s reports12.3561→0→17.2886N, with6→128inactive→6 patches. Its tibia angle changes by0.00327935rad during the zero-force step; SDK velocity is2.56532rad/s and interval-angle velocity1.31174rad/s. This is an instantaneous transient, distinct from the maximum0.09957rad/s50Hz RMS score. It is not explained solely by a static joint-rate offset.

Exact named sensor paths cover all608 bodies, reset roots match the declared8-column2m grid, and all measured inertias/masses/limits/gains properties are equal across replicas. The largest translated reset link-position difference is0.894µm. The origin replica has an exactly equal reset to accepted singleenv, yet subsequent q/rate/force trajectories differ slightly even at that unchanged origin. Failure counts by grid-y row are9,29,26,10 events, with4,1,2,4 passing replicas. This is not a monotonic distance relation and does not isolate translation/precision as the cause.

Every selected±1step patch aggregate reproduces the NPZ exactly after the analyzer's product-dtype correction. Independent world-to-shape projection differs by at most5.55e−17m and reproduces categories. Analyzer001's unrounded float64 product failure is preserved; analyzer002 follows the source's float32 product followed by float64 accumulation and records the first rejected calculation explicitly. No threshold or tolerance was widened. Native starts/counts arrays were not exported in the JSONL, so this is not an independent proof of that underlying ABI.

The unchanged SDK channel also shows persistent bias: origin LM tibia400Hz RMS0.0229695rad/s versus angle-interval0.00132590rad/s. Env2 LM tibia additionally has substantial transient angle motion: SDK400Hz RMS0.0480007, angle0.0240769, correlation0.93579. Its original50Hz SDK RMS0.0864103 remains the scored value. Rates/windows are not interchangeable and none replaces the original gate.

A controlled velocity-iteration diagnostic is a reasonable falsifiable next test: keep geometry, reset, gains, material, target, all captures and all gates; explicitly read the original iteration setting, change the velocity-iteration count, verify it after reset and terminal, and require fresh same-source1 then32. Current evidence does not establish a native solver cause or promise32/4 will pass. Root owns this source004 experiment and allocation. The1200s wall budget addresses measurement overhead separately from quality.

This receipt references the root's complete7.8MB analysis result by SHA and does not duplicate the4.97GB raw run. `summarize.py` recreates REPORT.json from that exact result. Original input manifests and full targeted samples remain in root analysis002 plus frozen analyzer002. Root owns eventual publication and central framework updates under docs/PROJECT_SITE.md. No GPU, source or tracked files changed here.
