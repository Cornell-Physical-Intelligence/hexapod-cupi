# Canonical PPO host004 — source005 composition preparation

This host binds frozen PPO003 (74 payloads, `970cd176…`) to standing005 (109 payloads, `c87f2d34…`) and the exact accepted native6 single-robot state `33920a4a…`. Its new declaration checks require source005's explicit 32/0 solver setting and that exact single result. Actual same-source 32-robot admission remains absent; the frozen producer template still rejects on `standing32_state_sha256`. No enabled external binding receipt or dispatch is created here.

The phase remains the single fresh canonical zero-command two-update infrastructure smoke: 1,000 neutral controls followed by 32×24×2 policy controls, with strict reload and no old checkpoint. PPO003 adds the reviewed source005 solver and diagnostic evidence around the neutral prefix and final 8,384 controlled steps. The existing actor, observation/command/action timing, limiter, reward, 400 Hz servo and physical/quiet gates are unchanged. Standing/PPO failures remain genuine failures; this host cannot convert them into admission.

The deadline adapter is byte-identical to host003: exactly three reviewed runtime substitutions give this fixed phase 1,200 seconds, with 90-second AppReady. It compiles the whole exact ownership module before CodeType comparison. Original owned-container identification, both GPU locks, competitor checks, stop handling and cleanup remain unchanged. All launcher function ASTs except the two added `validate_identity` declaration checks are parent-identical. Removing those two checks restores that function's exact parent AST as well. Job receipts retain the adapter proof. Coordination stays at `649ccda1…`; any persistent reservation/guard remains separately owned by root.

All 24 host/deadline tests pass. Actual local Python3.12.12 `-B -S` setup verifies PPO74/standing109/supervisor926 and imports the real standard-library consumer contract without processes, CUDA or Isaac. A second CPU composition verifies the exact nine canonical asset files and actual actuation admission. The actual pending32 template rejects before allocation. These are CPU setup proofs, not a complete dispatch preflight or a 32-robot pass.

The final PPO003 runtime hashes match the independent review; its test-only portability correction uses preserved manifests and was inspected. `inputs/PPO003_REVIEW_FINAL_BINDING.json` records this append-only link to the earlier frozen review.

Root owns fresh external bindings after the current 32-result audit, the final guard and GPU dispatch. Proposed outer budget remains 1,320 seconds plus 180 seconds cleanup and a 25-minute fallback. No frozen predecessor or tracked document was changed. Later publication must update the central framework under `docs/PROJECT_SITE.md`.

Integrity: `python3 -B -S verify_bundle.py`. Tests from the repository root:

```sh
.venv/bin/python3 -B -m unittest discover -s tmp/canonical_ppo_host_004 -p 'test_*.py'
```

CLI and read-only mounts retain host003's interface (`--source`, `--standing-source`, `--asset`, `--admission`, `--standing-one`, `--standing32`, `--bindings`, `--supervisor-source`, fresh `--output`); `--preflight-only` never starts a simulator. Old logs inherited from host003 retain their earlier scope; new proof files contain `source005` in their names.
