# Canonical PPO host003 — unused operational preparation

This successor changes only the operational phase bound to 1200 seconds for the single fixed `canonical_ppo_smoke` phase. AppReady remains 90 seconds. Exact source009 ownership, both GPU locks, competitor checks, stop handling and cleanup remain; the adapted `run_owned` has three documented source substitutions and an unchanged finally AST. Its whole CodeType is therefore intentionally different. `owned_container` retains its exact CodeType. Full verified source-module compilation (without execution) validates the loaded original before installation, including Python3.12 compilation context.

PPOsource002 (50 payloads) and standing003 (87 payloads), all physical/controller/quantitative gates, CLI, actual-state binding, 1000 neutral controls plus32×24×2 zero-command policy updates and strict checkpoint/reload contracts remain unchanged. All host functions except `load_supervisor` are AST-identical to host002; one file-relative helper loader is added. Job receipts retain the precise adapter identity.

This is not launch admission. Actual standing32_002 reached8000 physical steps but rejected native standing checks and also encountered the original600-second host bound. The longer preparation cannot turn that result into admission. No bindings were enabled, no GPU was used, no standing gates changed. A valid completed same-source standing1 and32 plus external binding receipt remain mandatory. Guard budget requested by root is1320seconds plus180seconds cleanup and25-minute fallback; the guard is separately prepared and remains blocked.

24 focused tests pass (`tests_first.log`): actual inherited/adapted run_owned under fake process/clock,700-second completion,1200-second timeout,90-second AppReady, exact owned cleanup and both lock closes, stop/unknown ownership/competitor rejection, source/CodeType tampering, full-module compilation and all existing native/source/checkpoint/terminal integrity checks. Local Python3.12.12 `-B -S` setup imports the real producer contract and verifies50/87/926 source maps without external calls; exact canonical9 assets and actual29 actuation also pass CPU composition. The disabled source template still rejects. These are CPU proofs, not remote runtime or standing admission.

Run `python3 -B -S verify_bundle.py` here for the portable inventory. Tests: `.venv/bin/python3 -B -m unittest discover -s tmp/canonical_ppo_host_003 -p 'test_*.py'` from the repository. Original host002 and standinghost005 remain immutable.
