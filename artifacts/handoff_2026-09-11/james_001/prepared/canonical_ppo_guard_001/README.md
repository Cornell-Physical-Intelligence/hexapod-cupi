# First canonical scratch PPO dispatch guard — pending actual standing32

This is local CPU preparation. It has not launched a job or changed Spark. Root is the sole dispatcher. Actual optimized standing1 (native_standing_004) passed and is pinned; native_standing32_002 is still awaiting its authentic terminal acceptance and cleanup evidence. Pending values are explicit `None` and prevent execution before imports, pause, locks or dispatch.

Fixed intended allocation: fresh canonical32×24×2 PPO updates, after a new same-process1000-control neutral prefix. Source00250 `1e173946…`, standing00387 `e922ff13…`, host00223 `d314bf36…` and coordination `649ccda1…` are fixed. No checkpoint input, old task, walking objective or automatic continuation is exposed. Full totals1048 controls/8384 physics substeps, including48 policy controls/384 substeps. The host and producer retain exact source/count/reload/quiet/actuator contracts.

Reserved names: output `canonical_ppo_smoke_001`, user unit `hexapod-canonical-ppo-smoke-001-20260910.service`, pause012, fallback `hexapod-canonical-forecast-restore-012`. Sensor confirmed current32 uses pause010 and native4 used pause011. Root must still prove names/output absent immediately before actual allocation.

`launch_guarded_remote.py` derives from frozen native_standing32_guard_002, whose reservation logic derives from native guard006. Its read-only13-file persistent reservation check and entire embedded restorer are byte/AST-identical to the adopted parent. The reservation remains active after per-job cleanup; the guard never removes ACTIVE/drop-ins or invokes the separate reservation prototype. Unknown CUDA remains rejected by the unchanged source009 supervisor. No generalized ignore list or service freezer is added.

Unused operational budget successor: outer1320 seconds,180-second stop,25-minute fallback. Host003 uses an exact-source three-seam run_owned adaptation for1200-second phase and unchanged90-second AppReady; owned_container CodeType, finally AST, both existing GPU locks and exact container cleanup remain unchanged. Native/controller/quantitative gate bytes remain unchanged. New CLI paths are `--standing-source`, `--standing-one`, `--standing32` and `--bindings`, with source/asset/actuation/supervisor/output retained. The enabled bindings file is external, immutable, hash-bound and mounted read-only by the host.

Final binding requires actual standing32 state and complete inventory digests; authentic terminal audit; its fixed invocation, exact container name/ID absence; campaign/job/source/readback/raw/cleanup hashes; pause010 restored timestamp and receipt; and the enabled PPO binding file hash. `verify_previous_owner` preserves the parent's success/ownership/cleanup checks with num_envs32 and additionally checks that32 consumed the exact admitted1 result. It verifies the complete standing output manifest's canonical digest and every corresponding prior raw pin. Host preflight independently validates actual per-replica standing reports and every required raw file.

The known native4 state is `318c0bd2500cc954b6eec5464d5a0feca1fc2d570c5dae2e65e0177f18475da0`; inventory `43f799625273680b29d00e3b2f063896dc1591582414b000ef6ba345f5830524`. The currently observed32 invocation is `1a38495bda9f4fefa4e5585574aabc56`; knowing an invocation does not establish completion. `PENDING.json` is planning metadata and is never used as substitute admission.

CPU checks exercise actual main ordering, fallback-before-pause, failure preservation, embedded exact cleanup, persistent reservation readback, literal NVIDIA CLI, true frozen source inventories, exact PPO input forwarding, missing/disabled/wrong standing bindings, propagation of failed native admission, fixed counts and parent-function parity. They use fakes for external effects. No full actual-input preflight is claimed while32 remains pending.

Test command: `python3 -B -S -m unittest discover -s tmp/canonical_ppo_guard_001 -p 'test_*.py' -v`. Root reviews final pins and freezes before remote setup/dispatch. Eventual publication must carry the central `docs/PROJECT_SITE.md` STATUS/plan/registry/update record; no living docs or Git changes were made here.

Actual standing32_002 subsequently completed raw acquisition but rejected standing quality and hit the old600-second host deadline. This draft remains unlaunchable, with actual32 and external binding pins null. Longer operational time cannot admit that rejection. A new admitted source/result requires separately reviewed successors; no rejected state is pinned as a pass. The pre-budget draft runtime remains in inputs/PRE_BUDGET_DRAFT.py.
