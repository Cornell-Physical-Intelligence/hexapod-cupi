# Fresh canonical PPO smoke ownership host

This host launches exactly one `canonical_ppo_smoke` phase: a fresh canonical32 neutral-standing prefix followed by32×24×2 PPO updates. Source45 `cc62938666b7b394e119fe05664767b2a265928ca46c8e15afd97312611f5d63` and standing002 source51 `acb589708fcba8be2f6ef64171795889eca8b6583fdbb4bc2fc25d61717b0467` are pinned. The host is executable preparation, **not dispatch admission**. Its native producer requires a separate enabled binding file with exact completed same-source standing1 and32 states; the frozen null-valued source template rejects.

```
python3 -B launch_ppo_spark.py \
  --source PPO_SOURCE --standing-source STANDING_SOURCE \
  --asset EXACT_ASSET --admission ACTUAL_ACTUATION_DIRECTORY \
  --standing-one ACTUAL_STANDING1_DIRECTORY --standing32 ACTUAL_STANDING32_DIRECTORY \
  --bindings EXTERNAL_VERIFIED_BINDINGS_JSON \
  --supervisor-source OWNERSHIP_SOURCE009 --output FRESH_OUTPUT --preflight-only
```

Root owns the actual receipt selection, external binding file, weather guard, fresh allocation and GPU dispatch. Remove `--preflight-only` only for that separately guarded allocation. All paths are canonicalized; input/output overlap, reused output and dangling output symlinks reject. There is no checkpoint, alternate update count, replica count, evaluation mode or automatic continuation argument. The source creates a new actor only after its same-process1000-control neutral prefix passes all32 unchanged standing gates. The full native count is1048 controls/8384 substeps with one reset; the learner emits two decision checkpoints,40 Adam minibatches and1536 transitions. This is zero-command infrastructure only, with no walking, formal quiet or hardware qualification inferred.

Source, standing source, asset, actuation admission, both standing admissions and external bindings mount read-only. Only fresh output is writable. The full producer contract is imported under its real `canonical_direct_ppo` package; foreign cached package modules reject before import, and temporary import search paths are restored. Every full input manifest and canonical selection is rechecked before the original supervisor creates a native process, after exit and at terminal finalization. Actual standing input states and every declared raw output are validated by the pinned producer contract, including original quiet/contact/actuator gates and strict learner reload/count/saturation evidence.

`run_owned` and `owned_container` remain exact source009 CodeTypes. No deadline adapter is introduced. Existing600-second phase,90-second actual AppReady, dual GPU locks, memory/competitor checks, contact-data log audit, exact owned cleanup and signal behavior remain. Only the command callback, full canonical input verifier, runtime identity and truthful job metadata differ. The legacy supervisor supplies ownership only; its old robot/task/policy are not loaded. Job metadata explicitly declares no checkpoint input and a fresh policy scope instead of incorrectly inheriting a no-policy experiment claim.

After native process exit, the host calls the producer's strict result validator and seals every phase output, including shutdown logs omitted from the pre-close measurement seal. Source/input change, missing reload evidence, rejected prefix, native error, failed process or post-exit output change preserves a failed campaign. Acquisition completion never becomes quality admission. The host does not hide native failure with a reset, load any old actor or modify the provisional servo assumptions.

The frozen CPU evidence includes15 focused tests and actual standard-library composition of source45, standing51 and supervisor926, comparing both ownership CodeTypes and rejecting the disabled binding template with zero external process/GPU calls. Run `python3 -B -m unittest discover -s THIS_DIRECTORY -p 'test_*.py' -v` in this repository; tests locate the existing pinned dependencies. `inspect_setup.py --source SOURCE --standing-source STANDING_SOURCE --supervisor-source SOURCE009` is an explicit CPU-only setup check; it does not replace full actual-input preflight or source-bound Spark verification.

Publication follows `docs/PROJECT_SITE.md`. Root owns living docs, central registry/update record, lineage checks where applicable and publication. This preparation does not change frozen native source or previously failed standing evidence.
