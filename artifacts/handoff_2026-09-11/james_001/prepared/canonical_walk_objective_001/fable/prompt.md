You are an independent implementation/research partner, not the decision authority. Provide a bounded FINAL critique and concrete equations/config suggestions, <=1200 words. No tools, filesystem, GPU or repository edits. Do not invent experiments or measurements. Model: claude-fable-5-1, maximum effort. No internal thinking transcript requested.

Task: propose first all-direction command/reward/curriculum contract for the NEW canonical detailed7.466088235kg,19-body/18-direct-drive-joint hexapod. Native standing1/32 is pending; no new-model PPO has run. User wants smooth omni motion, starts/stops, both yaw signs, simultaneous translation+yaw and quiet at stop. All later publication follows docs/PROJECT_SITE.md append-only central record. Existing C/mock/four-bar policies are historical only.

Approved fresh native PPO core:405 actor,408 critic. Five oldest->newest81 frames: body angular velocity3, projected gravity3, body command(vx,vy,wz)3, q-neutral18, rawSDKqd18, previousclippedaction18, ACTUAL heldtarget-minus-q18. Critic appends native body-root-origin linear velocity3 only. Explicit world->body XYZW then native(-y,x,z) toforward/left/up. MLP512/256/128ELU, empiricalnormalizers, actualfloat32 .04rad/20ms target limiter, .10rad targetscale, initiallyGaussianstd.02 zero mean final actor layer.50Hz holds/400Hz nativePD. No gaitclock/forcedsupportsequence/standingfreeze/filter. Initial32x24x2 zero-command smoke is integration only, source frozen cc629386..., pending physical admission. All future moving learning requires separately declared reset/terminal semantics, not an implicit extension of no-auto-reset smoke.

Physical/source gates remain separate from rewards:1.6Nm appliedcap, .5% requested saturation, exactnamedjointlimits, no nonfoot>1N, no non-toe floor<-1mm, exact nativegeometry. The zero-command smoke requires six supports. That requirement obviously cannot become a walking gait; do not silently waive it. Identify the smallest new support/flight admission needed before moving GPU. Do not invent an optimal support-count floor or universal torque/speed feasibility.

Existing formal quiet thresholds remain unchanged: SDKjointRMS<=.03rad/s,jointspan<=.02rad,targetstepP95<=.002rad/20ms,planar10mm,heading2deg,>=10squiet,noreset/nonfoot/limit violations. Formalmoving/allbearing gates remain source-bound; oldarchivedStage2C0.30m/s headline and itsmodel-specificheight are not evidence for thisnewrobot. Trainingcommands are NOT speed admission. Report requested goal separately if any futureadmission governor limits it; no rewardfullcredit for self-induced zero goals.

Initial proposal for critique: small curriculumlevels linear speeds .02-.04 then .04-.08 then .08-.16m/s and signed yaw .08-.16 then .16-.32 then .32-.50rad/s. Numbers are proposed requests, not feasible maxima. Start everylevel with balanced forward/reverse/left/right/diagonal/pureyaw/combinedtwist, plus20%zero and explicitnonzero->zero. Keep allsigns fromfirstlevel, rather than learning forward-only then repairing. Fixedheldout100%-complete cases include pureyaw withoutnonzerotranslation requirement, diagonals, botharc signs, directionreversals, stop12saftermotion to retain>=10squiet. No automaticcurriculum promotion onmeanreward; exactreviewed perbucket evidence and unchanged safetyquiet gates must guard promotion.

Candidate reward: exp(-||v_nav_xy-command_xy||^2/sigma_v^2) weight5 and exp(-(omega_z-commandyaw)^2/sigma_yaw^2) weight2. Sigma max(.02,.5*||cmdxy||), max(.05,.5*abs(cmdyaw)). Lightboundedcosts: tilt,rollpitchangularrate,verticalvelocity; requestedtorqueexcessabove1.6 (do not reward appliedclamps), appliedeffortsquared; ACTUAL targetdelta andsecond-difference. No large q-neutral penalty while moving, noalivebonus,no rewardclipping that couldhide failures. Quietvelocity/actualangle-rate extra costs onlywhenrequestedcommandexactzero; feedback stillactive. SDKandangle-difference rates retained independently: historicalC500 hadactual4.4Hzqoscillation,87.21% limiter saturation,11.13%torquesaturation; no-noisebarelyhelped and80msfilterworsened earlierbaseline. This motivates observedheldtarget and measurableactualsmoothness, not anotherblindfilter.

Slip must use velocity of the contacting MATERIAL point, fromcurrentcontactpoint inversecurrentlink transform thenprev/currentlinktransform difference, tangential toactualnormal. Do not use displacement of changingnativepatchworldcoordinates (nonsphericaltip contactmigrates). Explicitmissing/invalidcontact/history masks; simulatortruthmayenterrewardbutNOTactor405. Mechanicalpower/SDKqd cannot establish hardwareenergy whenratefidelity remainsuncertain.

Please: (1) identify concrete rewardhacking or causalbugs inproposal, (2) give minimal corrected reward scales/weights and whether costcurriculum is justified, (3) distinguish trainingdistribution vs heldoutevaluation/advancement and reset/timeoutbootstrap, (4) identify smallest nativeintegration/support prerequisities. Prefer a practicalfirst bounded experiment over architecture sprawl. Cite atmost3 alreadyknownprimaryworks ifhelpful, withoutclaimingplatformtransfer: Rudin2022https://proceedings.mlr.press/v164/rudin22a.html, Margolis2022Rapidhttps://arxiv.org/abs/2205.02824, WalkTheseWays2023https://proceedings.mlr.press/v205/margolis23a.html. Theirquadrupedtrainingtimes are NOT SparkETAs.

Curated actualnewcoreREADME follows:
# Canonical PPO integration preparation — inputs pending

This new `canonical_direct_ppo` package targets the detailed 7.466088235 kg direct-drive model. It does not import an old environment, actor, task, checkpoint or four-bar adapter. **Native dispatch is blocked:** exact same-source supported standing1/32 receipt hashes remain null in `BINDINGS.json`. Reviewed standing002 source, servo/geometry and policy-adapter hashes are populated. Root owns final review, native composition, GPU dispatch and publication.

The implemented runner creates a fresh Gaussian [512,256,128] ELU actor and critic, initial normalized-action standard deviation0.02 (stored as log(0.02)) and zero final actor layer. It collects32×24 samples per update for exactly two updates. It counts all40 actual Adam minibatches and stores a checkpoint after each completed update. Each is strictly reloaded into a newly constructed ordinary-buffer algorithm, proving exact actor, critic, empirical normalizers, optimizer and adaptive learning rate, deterministic action and adapter/session snapshot. Snapshot equality is a serialization check, not resumed-session or mixed-reset admission. The checkpoint also binds the full consumer source freeze and exact installed-RSL source map. This preserves the original algorithm's inference buffers and avoids the historically reproduced in-place reload failure. Reconstructing the verifier restores Torch RNG afterward. No checkpoint is accepted as an input to this smoke.

The versioned405/408 observation is five oldest-to-newest81-element frames: body angular velocity3, projected gravity3, body-frame command3, q-minus-neutral18, raw SDK joint rate18, previous clipped action18, actual held-target-minus-q18. Critic appends only native body linear velocity3, evaluated at the body-root origin. The bridge explicitly converts native XYZW world quantities to body coordinates; then (-y,x,z) gives forward/left/up. Linear velocity is transferred from the native body COM to the body-root origin with omega×r before body rotation; that origin choice is declared, not silently inherited. No contact truth or privileged linear velocity enters the actor.

`adapter.py` derives from the actual Fable5.1 MAX candidate with one encoding change: target-minus-neutral becomes target-minus-current-q. The original candidate/response stays unchanged. Existing helper reset behavior is not used to conceal a native violation: the bridge first checks exact model names, native limits and measured q using the native2e-6rad numeric readback tolerance. The smoke has no automatic reset. History/selective-reset logic is independently exercised, but mixed-row learning recovery is not admitted by this first experiment.

`native_bridge.py` consumes the matching `NativeStandingSession`, its private current readback, step_control and observe_control_substeps. It neither creates physics nor changes servo/reset/contact logic. The caller must finish its declared fresh neutral-hold checks before the first action; initial invalid contact/interval data at raw reset is rejected. Targets use0.10rad scale about admitted neutral, exact named joint bounds and the original0.040rad/20ms budget. The actual float32 emitted target is rounded inward when necessary so even a cast cannot cross the bound. Every history frame uses the target the native session actually held. No pre-execution target enters the observation history.

For this **zero-command integration smoke only**, all eight native substeps retain valid six-toe support, nonfoot rejection, applied1.60001Nm numeric cap and requested saturation no more than0.5% per joint over the full384-sample policy interval. Once that final allowance becomes impossible, collection aborts. A native/session error or termination preserves its attempted raw prefix, and the runner distinguishes attempted controls from successful collected transitions. Formal quiet quality remains separate and unchanged; a random fresh actor is not presumed to pass it.

The smoke-only reward records separate negative costs for q-neutral (0.1rad scale), actual20ms joint angle difference (1rad/s scale), body angular rate (0.25rad/s scale), and actual executed target delta (0.040rad scale). Raw SDK qdot remains in the actor and raw telemetry; using interval angle rate for this named reward does not replace formal SDK quiet gates. All feature unit scales are explicitly1 in SI/radians, with actor/critic empirical normalization. This is not the intended walking objective or curriculum. Later work needs a separately reviewed command-tracking objective for body(vx,vy,wz), stability, effort, slip and smoothness, plus motion-to-stop curriculum and unchanged all-direction/path/quiet evaluation.

Current CPU evidence:31 tests passed across the real RSL5 learner, strict reload, injected control27 failure, target limiter/parity, history timing, selected reset, nonzero translation/yaw/frame transforms, and pending-input rejection. The RSL algorithm source map is bound separately; no old actor weights are copied. Synthetic fixtures are explicitly not native physics. The full synthetic native bridge→actual RSL integration fixture passes. The native entry composition also passes its CPU lifecycle tests. Exact native standing1/32 admission bindings remain pending. This is an immutable CPU preparation; `BINDINGS.json` is a disabled template. Once matching actual evidence exists, the host must supply a separate exact binding file rather than edit this frozen directory.

`run_native_smoke.py` is now an executable native entry. Its CLI requires `--asset`, `--admission` (the exact native actuation result), `--standing-source`, `--standing-one`, `--standing32`, `--bindings`, and fresh `--output`; actual runtime additionally requires `--device cuda:0 --headless`. `--preflight-only` validates all source/admission bytes using standard-library code and cannot start Isaac. `canonical_direct_ppo.native_contract.verify_inputs(args)` and `validate_result(directory, identity)` are the host composition APIs. A host should import this unique package with the preparation directory on its path, not load a relative-import module under an unrelated bare name. No host or guard is added here.

The versioned entry adapter currently targets frozen standing source `acb589708fcba8be2f6ef64171795889eca8b6583fdbb4bc2fc25d61717b0467`. It compiles the complete verified module and replaces exactly its post1000-control reporting tail. All native scene/import/reset/servo/400Hz loop and original exception/finally statements remain byte identical. Exact helper paths stay active through lazy imports and foreign cached modules are rejected. This is explicitly an adapted entry, not a claim that the old standing entry itself ran PPO. A changed standing runtime requires a reviewed successor binding before dispatch.

The new tail flushes but does not close the live session. It snapshots the full1000-control/8000-substep neutral prefix in a separate immutable directory, copies the still-active contact stream, and links/copies only already-flushed immutable chunks. The original standing scorer must pass all32 rows before constructing the actor. The learner then uses exactly48 policy controls/384 substeps, with no second reset or counter rebasing, for total1048 controls/8384 steps. Original native session close occurs after learning or on any failure. Every attempted policy endpoint remains available, including native errors discovered during a hold. Measurement files seal before application close; native lifecycle logs stay explicitly unsealed until the host inventories their actual post-exit bytes.

The lower-level `canonical_direct_ppo.run_bound_smoke.run(...)` checks exact admitted helper source and consumer/RSL lineage before creating the bridge/actor. Its caller owns native initialization and shutdown. The native entry above supplies the full fresh-neutral gate; synthetic CPU fixtures use the lower-level runner only and do not stand in for this admission.

Any later tracked integration must follow `docs/PROJECT_SITE.md`: append a bounded central record, update relevant current status/design documentation, verify runtime lineage when covered, and check/build the poster. Proposed eventual source belongs in a new canonical environment/runtime namespace, with a new frozen task ID; existing package contracts remain unchanged. Root owns that adoption and publication.

CPU replay: `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m unittest discover -s <this-directory> -p 'test_*.py' -v`. The tests locate the repository rather than assuming a publication depth. Set `HEXAPOD_CANONICAL_STANDING_SOURCE` to the exact reviewed standing-source directory when replaying a published copy. The installed/test RSL code must match `RSL_SOURCE_SHA256.json`; tests may use the existing root-owned RSL5 test dependency directory but no dependency source is duplicated here.

The independent review at `tmp/canonical_ppo_independent_review_001` (freeze `e77e137a033764e7d961524258d3214ebcabca35cf07ec2d6e4322a96964fb09`) passed31 owner and5 independent tests on the recorded prior source inventory. Subsequent changes are explicitly preserved in `STANDING002_BINDING_DELTA.patch`: source pin to reviewed standing002, portable test-path lookup, readiness/docs and populated known template hashes. Final31 tests pass against standing002 in `TESTS_final_source002.txt`; the actual native entry, servo and scorer in standing002 are unchanged. No standing success is inferred from the contact-metadata correction.
