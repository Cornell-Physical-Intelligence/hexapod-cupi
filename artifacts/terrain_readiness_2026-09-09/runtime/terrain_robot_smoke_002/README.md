# Full-C terrain standing attempt 002

**Flat standing passed; terrain initialization progressed but failed at reset before stepping.** This is preserved infrastructure evidence, not a physical rejection or terrain-standing admission.

The fresh 32-environment × 1,000-control flat check retained the original robot and plan, with no terminations, truncations, nonfoot contacts or post-settle saturation. Its [admission](run/flat/admission.json) SHA-256 is `98b22e8bf7114cce9dcaf667f9a4b91f0ccf922c17ed2ac008f63bd7c452070b`.

The type-preserving Mesh reference corrected attempt 001's import problem. The terrain scene initialized all 19 bodies, 18 named joints and six feet, then failed because the installed configclass's `copy()` drops dynamically attached settings such as `omni_observation_noise_scale`. The [terrain log](run/logs/terrain.log), [state](run/terrain/state.json), [campaign](run/campaign.json) and [source manifest](campaign_source_hashes.json) retain the failure. A new source, configuration-copy test and fresh admission are required for a retry.

Source manifest SHA-256: `e969141e7afac6e44963a50e0bd87c1d3f4095a4856644f92b75642f3724cfad` (660 files). The 16-file C runtime, full-review plan, robot and original terrain catalog remain unchanged. Four host-generated bytecode files were incidental cache outputs, not changes to manifested source. Future host launches disable bytecode writing.

The selected `train_ramp_1103` check starts on its **flat entry pad**. Even a future standing pass there will not establish ramp traversal, standing on a slope or terrain walking.

Both owned containers exited, CUDA processes and locks were cleared, and [pause 014 restoration](forecast_pause_014/restored.json) restored both forecasting timers. The interrupted forecast remains eligible for scheduler retry; it is not claimed to have finished or resumed from an internal checkpoint.

[Exact result hashes](SHA256SUMS.json) bind 17 payloads. [Method](METHOD.md), [Mesh correction](fixture_adapter.patch) and [guarded dispatch](launch_guarded_remote_v2.py) preserve the executed procedure. Attempt 001 remains unchanged.
