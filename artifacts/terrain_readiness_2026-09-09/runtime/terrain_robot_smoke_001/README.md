# Full-C terrain entry attempt 001

The fresh exact-plan flat admission passed: 32 environments × 1,000 controls, zero terminations/truncations/nonfoot contacts/post-settle saturation, peak settled computed torque 0.5367 N·m, mean root height 0.12953 m. This is the C-study serial runtime and 1.6 N·m cap, not the production four-bar asset.

The terrain-entry process then **failed before robot stepping**. The installed TerrainImporter referenced a default-root Mesh onto a stronger Xform prim, and the fixture adapter rejected the resulting non-Mesh collider. There is no full-C terrain standing admission from this attempt. The original 30-fixture direct-Mesh runtime smoke remains separate valid evidence; this failure concerns the additional importer path.

The frozen source and admitted writable-copy asset bytes were unchanged. Both owned containers exited, both GPU locks were released, no CUDA workload remained, and the two StormScope timers were restored under pause 013. The source used published commit 9d6107794c8d329dded8997b6c88f7ea0d8695dd with the exact admitted full-review plan and pinned 16-file study runtime; see [source origin](source_origin.json) and [complete source hashes](campaign_source_hashes.json).

[Evidence hashes](SHA256SUMS.json) bind the 17 original run/pause files, including the flat admission, failed terrain state and cleanup evidence. The `run/` tree retains original logs and job outputs. A corrected importer requires a new source and attempt; do not rewrite this one. Live state belongs in [STATUS](../../../../STATUS.md).
