"""Pure, simulator-free reward and gait primitives for the hexapod task.

Every module here depends only on ``math`` and ``torch`` so the shaping
terms stay unit-testable without Isaac Lab or a GPU.
"""
