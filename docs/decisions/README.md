# Architecture decision records

One file per decision, numbered, in the form status / context / decision /
consequences. A record is `proposed` until the team accepts it, then
`accepted`; it is never edited after that. A later decision that reverses it
gets its own number and links back. This directory is append-only in the same
sense as `docs/incidents/`.
